# Ruthlessness — existence check findings (revises the add-plan)

- **File:** research/investigations/wa-add-ruthless-existence-findings-v1-20260628.md · 2026-06-28 · Author: Claude Code.
- **Trigger:** researcher comments (a) check existence first; (c) Tier-3 should already exist; (d) STEP services the term family, don't hand-pick.
- **Effect:** overturns the "new cluster" decision — cruelty/ruthlessness already has a home.

## 1. Existence of every candidate (a)
| term | gloss | in study? | cluster | owner registry |
|---|---|---|---|---|
| **H6531** `perek` | ruthless/harsh treatment | **NO** | — | **none** |
| H6184 `arits` | ruthless one | yes | **M06** | dread |
| H0393/0394/0395 `akzar`… | cruel / cruelty | yes | **M06** | strength |
| G0415 `aneleemon` | merciless | yes | M05 | mercy |
| H7186 `qasheh` | hard/severe | yes | M24 | distress |
| H7185 `qashah` | to harden | yes | FLAG/T2 | distress |
| G4642 `skleros` | hard | yes (delete) | — | hardness |
| G0663 `apotomia` | severity | **NO** | — | none |

## 2. The decisive finding — cruelty/ruthlessness is already a characteristic of M06
**M06 "Hate — Hate, Contempt and Hostility" [Analysis Completed]** already carries:
- **char 4 — "Cruelty/Ruthlessness — Character disposition of merciless destructiveness."**
- **char 106 — "F — Cruelty and ruthlessness — Not an episode but a trait: the settled cruelty of a person."**
- member terms incl. `arits` H6184, `akzar`/`akzari`/`akzeriuth` H393/4/5.

→ **The concept has a home.** A new "Ruthlessness" cluster would duplicate M06's existing characteristic. The earlier new-cluster decision rested on my un-checked picture and should be reversed.

## 3. Tier-3 existence (c) — mostly present, but a real gap
- **Present:** anger (`charon` M02, `aph` M02), evil (`ra` M03/M27), bitter (`mar` M03), strength (`az` M23), oppression-partial (`osheq` T2).
- **ABSENT (genuine gap):** **violence** — `chamas` H2555 (60×), `shadad` H7703, `shod` H7701, `lachats` H3906 (oppression), `chamats` H2556. Violence has **no study home** — the "serious issue" anticipated.

## 4. On (d) — STEP services the family
The cruelty family is already spread across many registries (dread, strength, mercy, hatred, contempt…) and landed in M06 by servicing those registry words. So the family is largely *already pulled*; what is missing is the single term `perek` (and the concept "violence"). Servicing the ruthlessness family via STEP pulls `perek` through `arits`'s related cluster (your 6-term list confirms `perek` sits in that cluster).

## 5. Revised approach (for researcher re-decision)
- **Do NOT create a new cluster.** Cruelty/Ruthlessness = **M06 char 4/106**.
- **Add `perek` H6531** to the study, cluster-assigned **M06** (characteristic Cruelty/Ruthlessness). It is the only genuinely-missing cruelty term.
- **`reg216 "Ruthlessness"`** (already created) → make it **`perek`'s OWNER registry**, `cluster_assignment` pointing into M06's frame — *not* a new M-cluster. (Optionally, later, re-home the scattered `arits`/`akzar` from dread/strength into this dedicated registry — a tidy-up, not required now.)
- **Flag VIOLENCE as a separate registry gap** — `chamas` + kin have no home; candidate for its own new registry word (a distinct concept from cruelty).
- `apotomia` G0663 (severity, NT) — minor; add as part of servicing if it comes through.

## 6. Decision needed
1. Confirm: **`perek` → M06 (Cruelty/Ruthlessness), via reg216 as its owner** — *no new cluster*. (Reverses the earlier new-cluster call.)
2. Re-home `arits`/`akzar` into reg216 now, or leave them where they are (dread/strength) and just XREF? *(recommend leave for now)*
3. Open a separate **"Violence" registry-gap** item? *(recommend yes)*
