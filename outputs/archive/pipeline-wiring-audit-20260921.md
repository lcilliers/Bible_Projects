# Pipeline wiring audit — every catalogue question, 4 dimensions

> Escalation #1806, researcher instruction, this chat turn, verbatim: "work through the catalogue once again, and make sure that each questions is presented to llm at the right point with the correct input data and that llm is not overloaded with unnecessary data; that llm know exactly what need to be done with each question and that the output from llm is correctly presented to CC to update the tables with all the relevant columns that need to completed." All 103 rows, 4 dimensions each, joining two already-live audits (`catalogue-coverage-audit-20260921-v2` for placement, `catalogue-question-wording-review-20260921-v5` for instruction clarity) plus two NEW dimensions (payload sizing, write-back correctness) built this session by reading all 4 stage-generator modules and `recordingpass.py` directly.

## NEW finding this pass: M83's stuck subgroup has a concrete, predictable blocker

Finding 6 (escalation #1806) already established that M83's `A_general_seeking` subgroup (328 of M83's 496 verses, 66%) is stuck at `cluster_subgroup.status='ready_for_reading'` while its siblings are `answer_complete`. This pass found the concrete, code-level reason: its 2 member strongs, `G2212` (116 corpus occurrences) and `H1245` (225 occurrences), **both individually exceed Stage 3's own 60-occurrence-per-strong cap simultaneously.** `charreadinggenerate.py`'s own `MultipleOverCapStrongs` exception exists exactly for this case and states plainly it is "not yet designed" — Stage 3's splitting mechanism (`_partition_occurrences_by_surface`) only handles ONE over-cap strong per subgroup, riding every other member strong's full profile along unpartitioned; two simultaneously-over-cap strongs has no packing order defined and will raise, not silently proceed. Confirmed live: **zero run attempts exist for this subgroup in the `run` table** — it has never actually been tried, so this is a predictable blocker waiting to fire, not yet an observed failure. Compare `M49/D_yadah_praise_confess` (run ids 3109/3117/3132 in the run log): that subgroup had exactly ONE over-cap strong (`H3034`, 114 occurrences) and the existing split logic handled it correctly (2 passes, succeeded). M83's case needs the same mechanism extended to handle 2+ simultaneously-over-cap strongs — not designed yet, your call on the packing/splitting approach.

## The 4 dimensions, in brief

1. **Placement** — is this question presented at a stage at all, and the right one? (from the #1806 coverage audit)
2. **Payload** — what does the LLM actually receive at that stage, and is it right-sized, not overloaded? (NEW this pass)
3. **Instructions** — does the question's own wording tell the LLM exactly what to do? (from the #1814 wording review)
4. **Write-back** — does `recordingpass.py` correctly populate every relevant DB column for this question's answer? (NEW this pass)

## Summary — placement

| placement_verdict | count |
|---|---|
| CORRECT | 68 |
| WRONG -- NO STAGE SELECTS THIS SCOPE | 27 |
| BLOCKED -- KNOWN, TRACKED | 4 |
| N/A -- DELETED | 4 |

## Summary — payload sizing

| payload_verdict | count |
|---|---|
| RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) | 52 |
| N/A | 35 |
| RIGHT-SIZED | 16 |

## Summary — write-back correctness

| writeback_verdict | count |
|---|---|
| CORRECT | 54 |
| N/A | 35 |
| CONFIRMED BUG -- #1815 (tracked, not yet fixed) | 14 |

Full CSV (all 103 rows, every dimension, no truncation): `pipeline-wiring-audit-20260921.csv` (same folder). Instruction-clarity counts (COMPLIANT/REVISE/RETIRED) are unchanged from `catalogue-question-wording-review-20260921-v5` — not repeated here, see that report.

## Full list — every question, all 4 dimensions

### D1.1.1 — Characteristic (HIB behaviour)

- **current text:** In this verse, what does the characteristic reveal about the person's perception, belief, or judgement -- what do they perceive, believe true, or conclude? Record it, or record none.
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** COMPLIANT (none) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D1.1.2 — Characteristic (HIB behaviour)

- **current text:** Across the verses, does the characteristic consistently precede a judgement or belief, follow one, or both?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** COMPLIANT (none) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D10.1.1 — Verse-context

- **current text:** Does this verse state any purpose, role, or effect the characteristic serves in the person — what it leads the person to be, do, or become? Record it if stated; otherwise record none.
- **1. Placement:** WRONG -- NO STAGE SELECTS THIS SCOPE — No stage's query includes this question's scope at all (Verse-context/The verse/most of Word-term lexical) -- structurally invisible regardless of how many clusters get processed. This is #1806's own ORIGINAL ask, still not redesigned.
- **2. Payload:** N/A — Never presented to any stage -- no payload exists to audit.
- **3. Instructions:** COMPLIANT (none) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### D10.1.2 — Other non-human beings

- **current text:** Across the evidence, is there any orientation toward a future fullness — something the person moves toward, not only what they currently are? Record it, or record none.
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (subject-missing;grain-vague(evidence)) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D10.2.1 — Verse-context

- **current text:** What first or most immediate inner-being response does this verse show following the characteristic? Record it, or record none.
- **1. Placement:** WRONG -- NO STAGE SELECTS THIS SCOPE — No stage's query includes this question's scope at all (Verse-context/The verse/most of Word-term lexical) -- structurally invisible regardless of how many clusters get processed. This is #1806's own ORIGINAL ask, still not redesigned.
- **2. Payload:** N/A — Never presented to any stage -- no payload exists to audit.
- **3. Instructions:** COMPLIANT (none) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### D10.2.2 — Characteristic (HIB behaviour)

- **current text:** Across the verses, is that immediate response consistent or varied?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (subject-missing;grain-vague(evidence)) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D10.3.1 — Verse-context

- **current text:** What does the characteristic produce in the inner being over time in this verse — what states, qualities, capacities, or orientations does it establish? Record it, or record none.
- **1. Placement:** WRONG -- NO STAGE SELECTS THIS SCOPE — No stage's query includes this question's scope at all (Verse-context/The verse/most of Word-term lexical) -- structurally invisible regardless of how many clusters get processed. This is #1806's own ORIGINAL ask, still not redesigned.
- **2. Payload:** N/A — Never presented to any stage -- no payload exists to audit.
- **3. Instructions:** COMPLIANT (none) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### D10.3.2 — Characteristic (HIB behaviour)

- **current text:** How does the sustained effect differ from the immediate response (D10.2)?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (subject-missing;grain-missing) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D10.4.1 — Verse-context

- **current text:** In this verse, does the characteristic produce transformation in the person, and if so does it change the person's condition, their orientation to their condition, or both? Record none if no transformation is shown.
- **1. Placement:** WRONG -- NO STAGE SELECTS THIS SCOPE — No stage's query includes this question's scope at all (Verse-context/The verse/most of Word-term lexical) -- structurally invisible regardless of how many clusters get processed. This is #1806's own ORIGINAL ask, still not redesigned.
- **2. Payload:** N/A — Never presented to any stage -- no payload exists to audit.
- **3. Instructions:** COMPLIANT (none) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### D10.4.2 — Characteristic (HIB behaviour)

- **current text:** Is the transformation reversible or irreversible in the evidence?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (subject-missing;grain-vague(evidence)) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D10.5.1 — Verse-context

- **current text:** Does this verse describe a sequence of inner states the characteristic moves the person through — a before, during, and after — and what are those states? Record none if no sequence is shown.
- **1. Placement:** WRONG -- NO STAGE SELECTS THIS SCOPE — No stage's query includes this question's scope at all (Verse-context/The verse/most of Word-term lexical) -- structurally invisible regardless of how many clusters get processed. This is #1806's own ORIGINAL ask, still not redesigned.
- **2. Payload:** N/A — Never presented to any stage -- no payload exists to audit.
- **3. Instructions:** COMPLIANT (none) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### D10.6.1 — Verse-context

- **current text:** In this verse, by what mechanism does the characteristic produce change — discipline, encounter, gradual formation, sudden transformation, or other? Record none if no mechanism is shown.
- **1. Placement:** WRONG -- NO STAGE SELECTS THIS SCOPE — No stage's query includes this question's scope at all (Verse-context/The verse/most of Word-term lexical) -- structurally invisible regardless of how many clusters get processed. This is #1806's own ORIGINAL ask, still not redesigned.
- **2. Payload:** N/A — Never presented to any stage -- no payload exists to audit.
- **3. Instructions:** COMPLIANT (none) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### D10.6.2 — Characteristic (HIB behaviour)

- **current text:** Does the mechanism differ across contexts in the evidence; if so, how?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (subject-missing;grain-vague(evidence)) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D11.1.1 — Other non-human beings

- **current text:** Across the evidence, does the characteristic's role read as belonging to created design, to the fallen condition, to both, or as not determinable?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (grain-vague(evidence)) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D11.2.1 — Characteristic (HIB behaviour)

- **current text:** Per the cluster's science extract, does genetics/evolutionary biology treat this characteristic (or its precursor) as part of innate human endowment, and how does that relate to D11.1's created-design/fallen-condition finding?
- **1. Placement:** BLOCKED -- KNOWN, TRACKED — Deliberately excluded from Stage 4's own query (question_text LIKE '%science extract%') because the science-extract source data isn't wired in yet. NOT a wording or placement defect -- a real, prose-grounded (escalation #1805 v3) build gap, 0/4 answered live.
- **2. Payload:** N/A — Never presented to any stage -- no payload exists to audit.
- **3. Instructions:** COMPLIANT (science-extract-family) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### D12.1.1 — Characteristic (HIB behaviour)

- **current text:** Per the cluster's science extract, what does behavioural science show about how the characteristic expresses socially -- cooperatively or competitively -- and does the verse evidence show a comparable social pattern?
- **1. Placement:** BLOCKED -- KNOWN, TRACKED — Deliberately excluded from Stage 4's own query (question_text LIKE '%science extract%') because the science-extract source data isn't wired in yet. NOT a wording or placement defect -- a real, prose-grounded (escalation #1805 v3) build gap, 0/4 answered live.
- **2. Payload:** N/A — Never presented to any stage -- no payload exists to audit.
- **3. Instructions:** COMPLIANT (science-extract-family) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### D2.1.1 — Characteristic (HIB behaviour)

- **current text:** In this verse, what is the felt quality of the characteristic -- is it shown as a settled disposition or a transient response, and what emotion-language or bodily-feeling language, if any, accompanies it? Record it, or record none.
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** COMPLIANT (none) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D2.1.2 — Characteristic (HIB behaviour)

- **current text:** Across the verses, is the felt quality consistent, or does it vary by context?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (subject-missing) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D3.1.1 — Verse-context

- **current text:** In what distinct mode(s) does the characteristic operate within the inner person in this verse — the manner of its functioning?
- **1. Placement:** WRONG -- NO STAGE SELECTS THIS SCOPE — No stage's query includes this question's scope at all (Verse-context/The verse/most of Word-term lexical) -- structurally invisible regardless of how many clusters get processed. This is #1806's own ORIGINAL ask, still not redesigned.
- **2. Payload:** N/A — Never presented to any stage -- no payload exists to audit.
- **3. Instructions:** COMPLIANT (none) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### D3.1.2 — Characteristic (HIB behaviour)

- **current text:** Does the mode of operation vary by context, direction, or constitutional level; if so, how?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (subject-missing;grain-missing) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D3.1.3 — Characteristic (HIB behaviour)

- **current text:** Does the characteristic operate through a communicative or speech-based mode (commanded, addressed, spoken); if so, how? Record it, or record none.
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (grain-missing) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D3.2.1 — Characteristic (HIB behaviour)

- **current text:** Under what inner conditions does the characteristic take hold or operate rightly?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (grain-missing) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D3.2.2 — Characteristic (HIB behaviour)

- **current text:** Under what inner conditions is the characteristic blocked, distorted, resisted, or not taken up — including, where the evidence shows it, distortion or interference by another spirit (adversarial or angelic)?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (grain-missing;grain-vague(evidence)) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D3.2.3 — The HIB

- **current text:** What is the inner-being state of the person in whom the characteristic is present but does not take hold?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (grain-missing;subject-partial) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D4.1.1 — Characteristic relational

- **current text:** Does the evidence show the characteristic operating differently within existing relational bonds versus across relational distance or difference; if so, how?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (grain-vague(evidence)) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D4.1.2 — Characteristic relational

- **current text:** Does the characteristic operate within covenantal contexts only, or does it cross covenantal boundaries, as the evidence shows?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (grain-vague(evidence)) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D4.1.3 — Characteristic relational

- **current text:** What does the evidence show about the relational scope of the characteristic — who is included and who is not?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (grain-vague(evidence)) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D5.1.1 — Word/term (lexical)

- **current text:** Across the characteristic's verses, is the characteristic ever predicated of God himself (not just present in a verse where God is also mentioned)?
- **1. Placement:** WRONG -- NO STAGE SELECTS THIS SCOPE — No stage's query includes this question's scope at all (Verse-context/The verse/most of Word-term lexical) -- structurally invisible regardless of how many clusters get processed. This is #1806's own ORIGINAL ask, still not redesigned.
- **2. Payload:** N/A — Never presented to any stage -- no payload exists to audit.
- **3. Instructions:** COMPLIANT (scope-mismatch-note) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### D5.1.2 — Other non-human beings

- **current text:** What does the pattern of presence/absence found in D5.1.1 indicate for the characteristic's place in the human person and in the divine image?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (grain-missing) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D5.2.1 — Word/term (lexical)

- **current text:** In this verse, does the characteristic operate in the person's movement toward God — seeking, supplication, worship, covenant — and if so how? Record none if it does not.
- **1. Placement:** WRONG -- NO STAGE SELECTS THIS SCOPE — No stage's query includes this question's scope at all (Verse-context/The verse/most of Word-term lexical) -- structurally invisible regardless of how many clusters get processed. This is #1806's own ORIGINAL ask, still not redesigned.
- **2. Payload:** N/A — Never presented to any stage -- no payload exists to audit.
- **3. Instructions:** COMPLIANT (scope-mismatch-note) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### D5.2.2 — Other non-human beings

- **current text:** What inner posture does this movement require, as the evidence shows?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (subject-missing) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D5.2.3 — Other non-human beings

- **current text:** What does the human-to-God direction of the characteristic show about the person's relationship with God?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (grain-missing) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D5.3.1 — Other non-human beings

- **current text:** From the characteristic's God-relation (D5.1/D7.6) and its role (D10.1/D11.1), what aspect of the divine likeness, if any, does it instantiate in the person? Record the aspect, or record none.
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (grain-missing) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D5.3.2 — Other non-human beings

- **current text:** Across the evidence, is the characteristic shared between God and the person, or an exclusively creaturely analogue to something in God?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (grain-vague(evidence)) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D5.3.3 — Other non-human beings

- **current text:** Where the characteristic is present or absent in a person, what does that indicate about the condition of the divine image in them — or is no such indication evidenced?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (grain-vague) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D6.1.1 — Verse-context

- **current text:** At which constitutional level(s) does this verse locate the characteristic — from {spirit, soul, heart, mind, other soul-subset, a named body part} — and how is each engaged? Record every level evidenced, or none.
- **1. Placement:** WRONG -- NO STAGE SELECTS THIS SCOPE — No stage's query includes this question's scope at all (Verse-context/The verse/most of Word-term lexical) -- structurally invisible regardless of how many clusters get processed. This is #1806's own ORIGINAL ask, still not redesigned.
- **2. Payload:** N/A — Never presented to any stage -- no payload exists to audit.
- **3. Instructions:** COMPLIANT (none) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### D6.1.2 — The HIB

- **current text:** Across the verses, what does the pattern of engaged and absent levels indicate — the characteristic's depth and seat, the levels it never engages, and (for any body link) whether the link is emphatic, functional, expressive, indicative, or mediating?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** COMPLIANT (none) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D6.2.1 — The HIB

- **current text:** Where a body link exists (from the D6.1.1 audit), in which direction does it run — soul/spirit expressing through the body, the body feeding back to the soul, or both — and what follows from that direction? If no body link, record none.
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (grain-vague(evidence)) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D6.3.1 — The HIB

- **current text:** Does the characteristic move across constitutional levels (spirit→soul→body), or onto the person from an external source — including another spirit (angelic or adversarial) — or in another direction; and if so in what sequence or pattern? If no movement, record none.
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (grain-missing) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D7.1.1 — Word/term (lexical)

- **current text:** In this verse, does the characteristic operate from God toward the human person, and if so how? Record none if it does not.
- **1. Placement:** WRONG -- NO STAGE SELECTS THIS SCOPE — No stage's query includes this question's scope at all (Verse-context/The verse/most of Word-term lexical) -- structurally invisible regardless of how many clusters get processed. This is #1806's own ORIGINAL ask, still not redesigned.
- **2. Payload:** N/A — Never presented to any stage -- no payload exists to audit.
- **3. Instructions:** COMPLIANT (none) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### D7.1.2 — Other non-human beings

- **current text:** On what basis does God extend the characteristic — conditional, unconditional, covenantal, or responsive — as the evidence shows?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (grain-vague(evidence)) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D7.1.3 — Other non-human beings

- **current text:** What does God's extension of the characteristic show about his disposition toward the human person?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (grain-missing) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D7.2.1 — Word/term (lexical)

- **current text:** In this verse, is the characteristic extended by one person toward another, and if so how does it operate in that extension? Record none if it is not.
- **1. Placement:** WRONG -- NO STAGE SELECTS THIS SCOPE — No stage's query includes this question's scope at all (Verse-context/The verse/most of Word-term lexical) -- structurally invisible regardless of how many clusters get processed. This is #1806's own ORIGINAL ask, still not redesigned.
- **2. Payload:** N/A — Never presented to any stage -- no payload exists to audit.
- **3. Instructions:** COMPLIANT (none) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### D7.2.2 — Characteristic relational

- **current text:** What inner conditions or orientations in the giver accompany genuine extension of the characteristic?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (grain-missing) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D7.2.3 — Characteristic relational

- **current text:** What does the evidence show a person must have received or become before they extend the characteristic?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (grain-vague(evidence)) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D7.3.1 — Word/term (lexical)

- **current text:** In this verse, is the characteristic taken up by a person from another, and if so how does it operate in that uptake? Record none if it is not.
- **1. Placement:** WRONG -- NO STAGE SELECTS THIS SCOPE — No stage's query includes this question's scope at all (Verse-context/The verse/most of Word-term lexical) -- structurally invisible regardless of how many clusters get processed. This is #1806's own ORIGINAL ask, still not redesigned.
- **2. Payload:** N/A — Never presented to any stage -- no payload exists to audit.
- **3. Instructions:** COMPLIANT (none) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### D7.3.2 — Characteristic relational

- **current text:** What inner conditions accompany or block uptake of the characteristic from another person?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (grain-missing) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D7.3.3 — Characteristic relational

- **current text:** What is the inner-being state of the person who meets the characteristic from another but does not take it up?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (grain-missing) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D7.4.1 — Word/term (lexical)

- **current text:** In this verse, does the characteristic operate in relation to other spiritual beings — angelic or adversarial — and if so how? Record none if it does not.
- **1. Placement:** WRONG -- NO STAGE SELECTS THIS SCOPE — No stage's query includes this question's scope at all (Verse-context/The verse/most of Word-term lexical) -- structurally invisible regardless of how many clusters get processed. This is #1806's own ORIGINAL ask, still not redesigned.
- **2. Payload:** N/A — Never presented to any stage -- no payload exists to audit.
- **3. Instructions:** COMPLIANT (none) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### D7.4.2a — Word/term (lexical)

- **current text:** Does an adversarial-being code ever appear as an acting party in a verse carrying this characteristic?
- **1. Placement:** WRONG -- NO STAGE SELECTS THIS SCOPE — No stage's query includes this question's scope at all (Verse-context/The verse/most of Word-term lexical) -- structurally invisible regardless of how many clusters get processed. This is #1806's own ORIGINAL ask, still not redesigned.
- **2. Payload:** N/A — Never presented to any stage -- no payload exists to audit.
- **3. Instructions:** REVISE (jargon;grain-vague) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### D7.4.2b — Other non-human beings

- **current text:** What does that pattern show about the characteristic being a site of adversarial activity?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (subject-missing;grain-missing) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D7.4.3a — Word/term (lexical)

- **current text:** Does an angelic-being code ever appear as an acting party in a verse carrying this characteristic?
- **1. Placement:** WRONG -- NO STAGE SELECTS THIS SCOPE — No stage's query includes this question's scope at all (Verse-context/The verse/most of Word-term lexical) -- structurally invisible regardless of how many clusters get processed. This is #1806's own ORIGINAL ask, still not redesigned.
- **2. Payload:** N/A — Never presented to any stage -- no payload exists to audit.
- **3. Instructions:** REVISE (jargon;grain-vague) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### D7.4.3b — Other non-human beings

- **current text:** What does that pattern show about the characteristic being communicated, strengthened, or mediated through angelic ministry?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (subject-missing;grain-missing) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D7.5.1 — Verse-context

- **current text:** Where does this verse say the characteristic originates — generated within the person, received from another person, bestowed by God, carried generationally, introduced by another spirit (angelic or adversarial), or not stated?
- **1. Placement:** WRONG -- NO STAGE SELECTS THIS SCOPE — No stage's query includes this question's scope at all (Verse-context/The verse/most of Word-term lexical) -- structurally invisible regardless of how many clusters get processed. This is #1806's own ORIGINAL ask, still not redesigned.
- **2. Payload:** N/A — Never presented to any stage -- no payload exists to audit.
- **3. Instructions:** COMPLIANT (none) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### D7.5.2 — Characteristic (HIB behaviour)

- **current text:** Across the verses, is the origin single or multiple, and does it change with context?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (subject-missing) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### D7.6.1 — Word/term (lexical)

- **current text:** In this verse, is the characteristic predicated of God or otherwise related to God; if so, in what relation (God as the one who bears it, acts, gives it, or is its object)? Record the relation, or record that it is not related to God here.
- **1. Placement:** WRONG -- NO STAGE SELECTS THIS SCOPE — No stage's query includes this question's scope at all (Verse-context/The verse/most of Word-term lexical) -- structurally invisible regardless of how many clusters get processed. This is #1806's own ORIGINAL ask, still not redesigned.
- **2. Payload:** N/A — Never presented to any stage -- no payload exists to audit.
- **3. Instructions:** COMPLIANT (none) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### D7.7.1 — Characteristic (HIB behaviour)

- **current text:** Where this verse contains an action or movement word tagged as an Operation in the cluster's role classification (the tag is `role-T3` in `cluster.cluster_code` -- consult that tagging directly, not this question's own memory of it), what is that word's relation to the parties present -- who initiates it and toward whom or what is it directed? Record none if no such operation word is present.
- **1. Placement:** CORRECT — Answered at Stage 1 by exact question_code pin (D7.7.1/M0.6.5) -- deliberate, not a hardcoded-list bug (confirmed live 2026-09-21, escalation #1806 v8).
- **2. Payload:** RIGHT-SIZED — Same Stage 1 payload as DYNAMIC-STAGE1 (same batch, same call) -- no separate payload construction.
- **3. Instructions:** REVISE (jargon;compound) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — M0.6.5/D7.7.1 stay keyed to the calling pass's own cluster_code by design (`_effective_cluster_code` explicitly does NOT override these -- they are that cluster's own vantage point on the verse, not a strong-level fact). `cluster_subgroup_id`/`cluster_subgroup_code` correctly NULL (pre-subgroup stage).

### D9.1.1 — Characteristic (HIB behaviour)

- **current text:** Per the cluster's science extract, what does neuroscience or physiology show about the mechanism underlying this characteristic, and does the verse evidence engage or diverge from that mechanism?
- **1. Placement:** BLOCKED -- KNOWN, TRACKED — Deliberately excluded from Stage 4's own query (question_text LIKE '%science extract%') because the science-extract source data isn't wired in yet. NOT a wording or placement defect -- a real, prose-grounded (escalation #1805 v3) build gap, 0/4 answered live.
- **2. Payload:** N/A — Never presented to any stage -- no payload exists to audit.
- **3. Instructions:** COMPLIANT (science-extract-family) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### D9.2.1 — Characteristic (HIB behaviour)

- **current text:** Per the cluster's science extract, does the scientific literature show the characteristic (or its pattern) transmitted generationally, and does the verse evidence show anything comparable (covenant, inheritance, generational language)?
- **1. Placement:** BLOCKED -- KNOWN, TRACKED — Deliberately excluded from Stage 4's own query (question_text LIKE '%science extract%') because the science-extract source data isn't wired in yet. NOT a wording or placement defect -- a real, prose-grounded (escalation #1805 v3) build gap, 0/4 answered live.
- **2. Payload:** N/A — Never presented to any stage -- no payload exists to audit.
- **3. Instructions:** COMPLIANT (science-extract-family) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### F0.1.1 — Verse-context

- **current text:** In this verse, does the characteristic engage or is affected by specific faculties — the inner senses (hearing, sight, taste, touch, smell), spiritual discernment, the cognitive faculty (knowing, understanding, discerning), the memory faculty (the holding and retrieving of inner-being reality across time), the affective faculty (feeling and emotional experience), the creative faculty (imagination and the capacity to originate), the volitional faculty (the capacity to choose), the agency faculty (the capacity to act, initiate, and make happen), the moral-evaluation faculty (the capacity to assess against a standard of right, wrong, good, and true), conscience (the acute inner witness of sin, guilt, and conviction), conscientiousness (the integrated response of moral awareness, volition, and action), or relational capacity (the constitutional equipment for genuine connection with another person) — and if so, which faculty/faculties and how? Record none if it does not.
- **1. Placement:** WRONG -- NO STAGE SELECTS THIS SCOPE — No stage's query includes this question's scope at all (Verse-context/The verse/most of Word-term lexical) -- structurally invisible regardless of how many clusters get processed. This is #1806's own ORIGINAL ask, still not redesigned.
- **2. Payload:** N/A — Never presented to any stage -- no payload exists to audit.
- **3. Instructions:** REVISE (run-on;jargon-density) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### F0.1.2 — Characteristic (HIB behaviour)

- **current text:** Across the verses, what does the pattern of engagement and non-engagement with the faculties indicate about the characteristic's nature?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** COMPLIANT (none) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### M0.1.1 — Word/term (lexical)

- **current text:** What is the characteristic called in the programme, and what does the name signal about its essential nature?
- **1. Placement:** CORRECT — Answered at Stage 1 (verse-reading), prefix-selected (M0.1%/M0.5%) so it auto-tracks catalogue changes -- no drift risk.
- **2. Payload:** RIGHT-SIZED — Stage 1 batches per `passage.max_verses` (bounded, never a full-cluster push). Per verse: base text + every live verse_lexical row's role array (deliberately including unwired T-code tags, per cfg_method_rule role-list-includes-unwired-tags -- over-inclusion is a stated, deliberate design choice, not accidental bloat) + meaning_sources for the cluster's own home strongs only (not every M-code strong in the verse) + prior M0.6.5 relational context for the SAME verses only. Bounded and scoped correctly.
- **3. Instructions:** COMPLIANT (definitional-category;front-loaded-exposure) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CONFIRMED BUG -- #1815 (tracked, not yet fixed) — `cluster_code` is resolved via `_effective_cluster_code()`'s LIKE 'M%' fallback against live `cluster_strong` -- correct for a strong with a real M-code home, but for a front-loaded NON-MEMBER strong whose only home is a T-code, the fallback silently misattributes the answer to the calling pass's own cluster_code instead of leaving it unhomed. #1817 closed the population of T-code-only strongs this can hit (fully enumerable now); the attribution logic itself is unfixed. Applies to M0.1.x/M0.5.x only (the front-loaded word-level battery) -- confirmed by code: `_WORD_LEVEL_QUESTION_PREFIXES = ('M0.1', 'M0.5')` is the only trigger for this override path.

### M0.1.2 — Word/term (lexical)

- **current text:** What do the primary Hebrew and Greek terms show at the definitional level?
- **1. Placement:** CORRECT — Answered at Stage 1 (verse-reading), prefix-selected (M0.1%/M0.5%) so it auto-tracks catalogue changes -- no drift risk.
- **2. Payload:** RIGHT-SIZED — Stage 1 batches per `passage.max_verses` (bounded, never a full-cluster push). Per verse: base text + every live verse_lexical row's role array (deliberately including unwired T-code tags, per cfg_method_rule role-list-includes-unwired-tags -- over-inclusion is a stated, deliberate design choice, not accidental bloat) + meaning_sources for the cluster's own home strongs only (not every M-code strong in the verse) + prior M0.6.5 relational context for the SAME verses only. Bounded and scoped correctly.
- **3. Instructions:** COMPLIANT (definitional-category;front-loaded-exposure) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CONFIRMED BUG -- #1815 (tracked, not yet fixed) — `cluster_code` is resolved via `_effective_cluster_code()`'s LIKE 'M%' fallback against live `cluster_strong` -- correct for a strong with a real M-code home, but for a front-loaded NON-MEMBER strong whose only home is a T-code, the fallback silently misattributes the answer to the calling pass's own cluster_code instead of leaving it unhomed. #1817 closed the population of T-code-only strongs this can hit (fully enumerable now); the attribution logic itself is unfixed. Applies to M0.1.x/M0.5.x only (the front-loaded word-level battery) -- confirmed by code: `_WORD_LEVEL_QUESTION_PREFIXES = ('M0.1', 'M0.5')` is the only trigger for this override path.

### M0.1.3 — Word/term (lexical)

- **current text:** What directional, relational, or constitutional implication does the name carry?
- **1. Placement:** CORRECT — Answered at Stage 1 (verse-reading), prefix-selected (M0.1%/M0.5%) so it auto-tracks catalogue changes -- no drift risk.
- **2. Payload:** RIGHT-SIZED — Stage 1 batches per `passage.max_verses` (bounded, never a full-cluster push). Per verse: base text + every live verse_lexical row's role array (deliberately including unwired T-code tags, per cfg_method_rule role-list-includes-unwired-tags -- over-inclusion is a stated, deliberate design choice, not accidental bloat) + meaning_sources for the cluster's own home strongs only (not every M-code strong in the verse) + prior M0.6.5 relational context for the SAME verses only. Bounded and scoped correctly.
- **3. Instructions:** COMPLIANT (definitional-category;front-loaded-exposure) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CONFIRMED BUG -- #1815 (tracked, not yet fixed) — `cluster_code` is resolved via `_effective_cluster_code()`'s LIKE 'M%' fallback against live `cluster_strong` -- correct for a strong with a real M-code home, but for a front-loaded NON-MEMBER strong whose only home is a T-code, the fallback silently misattributes the answer to the calling pass's own cluster_code instead of leaving it unhomed. #1817 closed the population of T-code-only strongs this can hit (fully enumerable now); the attribution logic itself is unfixed. Applies to M0.1.x/M0.5.x only (the front-loaded word-level battery) -- confirmed by code: `_WORD_LEVEL_QUESTION_PREFIXES = ('M0.1', 'M0.5')` is the only trigger for this override path.

### M0.2.1 — Characteristic (HIB behaviour)

- **current text:** What kind of inner-being phenomenon is the characteristic — an act, a disposition, a condition/status, a quality, or something else?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** COMPLIANT (definitional-category;stage4-not-exposed) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### M0.2.2 — Characteristic (HIB behaviour)

- **current text:** Is the characteristic simple in structure, or does it combine constituent elements; if compound, which?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** COMPLIANT (definitional-category;stage4-not-exposed) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### M0.3.1 — Characteristic (HIB behaviour)

- **current text:** What stands against the characteristic as its structural opposite — the inner-being reality that excludes it?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** COMPLIANT (definitional-category;stage4-not-exposed) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### M0.3.2 — Characteristic (HIB behaviour)

- **current text:** What does the characteristic exclude or resist at its edge?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** COMPLIANT (definitional-category;stage4-not-exposed) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### M0.3.3 — Characteristic (HIB behaviour)

- **current text:** Where does the characteristic end and another thing begin — what is it not?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** COMPLIANT (definitional-category;stage4-not-exposed) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### M0.4.1 — Word/term (lexical)

- **current text:** What is the grammatical/stem form of the characteristic's primary term in this verse?
- **1. Placement:** WRONG -- NO STAGE SELECTS THIS SCOPE — No stage's query includes this question's scope at all (Verse-context/The verse/most of Word-term lexical) -- structurally invisible regardless of how many clusters get processed. This is #1806's own ORIGINAL ask, still not redesigned.
- **2. Payload:** N/A — Never presented to any stage -- no payload exists to audit.
- **3. Instructions:** COMPLIANT (none) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### M0.5.1 — Word/term (lexical)

- **current text:** What are the primary Hebrew and Greek terms for this characteristic, and what do their root meanings show?
- **1. Placement:** CORRECT — Answered at Stage 1 (verse-reading), prefix-selected (M0.1%/M0.5%) so it auto-tracks catalogue changes -- no drift risk.
- **2. Payload:** RIGHT-SIZED — Stage 1 batches per `passage.max_verses` (bounded, never a full-cluster push). Per verse: base text + every live verse_lexical row's role array (deliberately including unwired T-code tags, per cfg_method_rule role-list-includes-unwired-tags -- over-inclusion is a stated, deliberate design choice, not accidental bloat) + meaning_sources for the cluster's own home strongs only (not every M-code strong in the verse) + prior M0.6.5 relational context for the SAME verses only. Bounded and scoped correctly.
- **3. Instructions:** COMPLIANT (definitional-category;front-loaded-exposure) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CONFIRMED BUG -- #1815 (tracked, not yet fixed) — `cluster_code` is resolved via `_effective_cluster_code()`'s LIKE 'M%' fallback against live `cluster_strong` -- correct for a strong with a real M-code home, but for a front-loaded NON-MEMBER strong whose only home is a T-code, the fallback silently misattributes the answer to the calling pass's own cluster_code instead of leaving it unhomed. #1817 closed the population of T-code-only strongs this can hit (fully enumerable now); the attribution logic itself is unfixed. Applies to M0.1.x/M0.5.x only (the front-loaded word-level battery) -- confirmed by code: `_WORD_LEVEL_QUESTION_PREFIXES = ('M0.1', 'M0.5')` is the only trigger for this override path.

### M0.5.10 — Word/term (lexical)

- **current text:** What does the full vocabulary arc show about the characteristic's complete semantic range?
- **1. Placement:** CORRECT — Answered at Stage 1 (verse-reading), prefix-selected (M0.1%/M0.5%) so it auto-tracks catalogue changes -- no drift risk.
- **2. Payload:** RIGHT-SIZED — Stage 1 batches per `passage.max_verses` (bounded, never a full-cluster push). Per verse: base text + every live verse_lexical row's role array (deliberately including unwired T-code tags, per cfg_method_rule role-list-includes-unwired-tags -- over-inclusion is a stated, deliberate design choice, not accidental bloat) + meaning_sources for the cluster's own home strongs only (not every M-code strong in the verse) + prior M0.6.5 relational context for the SAME verses only. Bounded and scoped correctly.
- **3. Instructions:** COMPLIANT (definitional-category;front-loaded-exposure) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CONFIRMED BUG -- #1815 (tracked, not yet fixed) — `cluster_code` is resolved via `_effective_cluster_code()`'s LIKE 'M%' fallback against live `cluster_strong` -- correct for a strong with a real M-code home, but for a front-loaded NON-MEMBER strong whose only home is a T-code, the fallback silently misattributes the answer to the calling pass's own cluster_code instead of leaving it unhomed. #1817 closed the population of T-code-only strongs this can hit (fully enumerable now); the attribution logic itself is unfixed. Applies to M0.1.x/M0.5.x only (the front-loaded word-level battery) -- confirmed by code: `_WORD_LEVEL_QUESTION_PREFIXES = ('M0.1', 'M0.5')` is the only trigger for this override path.

### M0.5.11 — Word/term (lexical)

- **current text:** Where this occurrence's surface form/contextual sense diverges from the base lemma's general stepGloss, what nuance is that divergence carrying in THIS context, what is its impact here, and how does it compare against the word's other occurrences? Record none if this occurrence sits squarely within the base gloss with no notable shift.
- **1. Placement:** CORRECT — Answered at Stage 1 (verse-reading), prefix-selected (M0.1%/M0.5%) so it auto-tracks catalogue changes -- no drift risk.
- **2. Payload:** RIGHT-SIZED — Stage 1 batches per `passage.max_verses` (bounded, never a full-cluster push). Per verse: base text + every live verse_lexical row's role array (deliberately including unwired T-code tags, per cfg_method_rule role-list-includes-unwired-tags -- over-inclusion is a stated, deliberate design choice, not accidental bloat) + meaning_sources for the cluster's own home strongs only (not every M-code strong in the verse) + prior M0.6.5 relational context for the SAME verses only. Bounded and scoped correctly.
- **3. Instructions:** REVISE (jargon;compound;mixed-grain) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CONFIRMED BUG -- #1815 (tracked, not yet fixed) — `cluster_code` is resolved via `_effective_cluster_code()`'s LIKE 'M%' fallback against live `cluster_strong` -- correct for a strong with a real M-code home, but for a front-loaded NON-MEMBER strong whose only home is a T-code, the fallback silently misattributes the answer to the calling pass's own cluster_code instead of leaving it unhomed. #1817 closed the population of T-code-only strongs this can hit (fully enumerable now); the attribution logic itself is unfixed. Applies to M0.1.x/M0.5.x only (the front-loaded word-level battery) -- confirmed by code: `_WORD_LEVEL_QUESTION_PREFIXES = ('M0.1', 'M0.5')` is the only trigger for this override path.

### M0.5.2 — Word/term (lexical)

- **current text:** What is the grammatical range of the primary term (noun, verb, adjective, participle), and what does that range show about how the characteristic operates?
- **1. Placement:** CORRECT — Answered at Stage 1 (verse-reading), prefix-selected (M0.1%/M0.5%) so it auto-tracks catalogue changes -- no drift risk.
- **2. Payload:** RIGHT-SIZED — Stage 1 batches per `passage.max_verses` (bounded, never a full-cluster push). Per verse: base text + every live verse_lexical row's role array (deliberately including unwired T-code tags, per cfg_method_rule role-list-includes-unwired-tags -- over-inclusion is a stated, deliberate design choice, not accidental bloat) + meaning_sources for the cluster's own home strongs only (not every M-code strong in the verse) + prior M0.6.5 relational context for the SAME verses only. Bounded and scoped correctly.
- **3. Instructions:** COMPLIANT (definitional-category;front-loaded-exposure) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CONFIRMED BUG -- #1815 (tracked, not yet fixed) — `cluster_code` is resolved via `_effective_cluster_code()`'s LIKE 'M%' fallback against live `cluster_strong` -- correct for a strong with a real M-code home, but for a front-loaded NON-MEMBER strong whose only home is a T-code, the fallback silently misattributes the answer to the calling pass's own cluster_code instead of leaving it unhomed. #1817 closed the population of T-code-only strongs this can hit (fully enumerable now); the attribution logic itself is unfixed. Applies to M0.1.x/M0.5.x only (the front-loaded word-level battery) -- confirmed by code: `_WORD_LEVEL_QUESTION_PREFIXES = ('M0.1', 'M0.5')` is the only trigger for this override path.

### M0.5.3 — Word/term (lexical)

- **current text:** What is the semantic range of the primary term — across what breadth of meaning does it operate, including any idiomatic, analogical, or otherwise implied meaning carried by its use in combination with other terms?
- **1. Placement:** CORRECT — Answered at Stage 1 (verse-reading), prefix-selected (M0.1%/M0.5%) so it auto-tracks catalogue changes -- no drift risk.
- **2. Payload:** RIGHT-SIZED — Stage 1 batches per `passage.max_verses` (bounded, never a full-cluster push). Per verse: base text + every live verse_lexical row's role array (deliberately including unwired T-code tags, per cfg_method_rule role-list-includes-unwired-tags -- over-inclusion is a stated, deliberate design choice, not accidental bloat) + meaning_sources for the cluster's own home strongs only (not every M-code strong in the verse) + prior M0.6.5 relational context for the SAME verses only. Bounded and scoped correctly.
- **3. Instructions:** COMPLIANT (definitional-category;front-loaded-exposure) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CONFIRMED BUG -- #1815 (tracked, not yet fixed) — `cluster_code` is resolved via `_effective_cluster_code()`'s LIKE 'M%' fallback against live `cluster_strong` -- correct for a strong with a real M-code home, but for a front-loaded NON-MEMBER strong whose only home is a T-code, the fallback silently misattributes the answer to the calling pass's own cluster_code instead of leaving it unhomed. #1817 closed the population of T-code-only strongs this can hit (fully enumerable now); the attribution logic itself is unfixed. Applies to M0.1.x/M0.5.x only (the front-loaded word-level battery) -- confirmed by code: `_WORD_LEVEL_QUESTION_PREFIXES = ('M0.1', 'M0.5')` is the only trigger for this override path.

### M0.5.4 — Word/term (lexical)

- **current text:** Does the vocabulary include terms distinguishing distinct aspects — disposition versus act, received versus given, condition versus quality? Record which, or none.
- **1. Placement:** CORRECT — Answered at Stage 1 (verse-reading), prefix-selected (M0.1%/M0.5%) so it auto-tracks catalogue changes -- no drift risk.
- **2. Payload:** RIGHT-SIZED — Stage 1 batches per `passage.max_verses` (bounded, never a full-cluster push). Per verse: base text + every live verse_lexical row's role array (deliberately including unwired T-code tags, per cfg_method_rule role-list-includes-unwired-tags -- over-inclusion is a stated, deliberate design choice, not accidental bloat) + meaning_sources for the cluster's own home strongs only (not every M-code strong in the verse) + prior M0.6.5 relational context for the SAME verses only. Bounded and scoped correctly.
- **3. Instructions:** COMPLIANT (definitional-category;front-loaded-exposure) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CONFIRMED BUG -- #1815 (tracked, not yet fixed) — `cluster_code` is resolved via `_effective_cluster_code()`'s LIKE 'M%' fallback against live `cluster_strong` -- correct for a strong with a real M-code home, but for a front-loaded NON-MEMBER strong whose only home is a T-code, the fallback silently misattributes the answer to the calling pass's own cluster_code instead of leaving it unhomed. #1817 closed the population of T-code-only strongs this can hit (fully enumerable now); the attribution logic itself is unfixed. Applies to M0.1.x/M0.5.x only (the front-loaded word-level battery) -- confirmed by code: `_WORD_LEVEL_QUESTION_PREFIXES = ('M0.1', 'M0.5')` is the only trigger for this override path.

### M0.5.5 — Word/term (lexical)

- **current text:** Does the vocabulary include a term for the structural opposite or absence of this characteristic? Record it, or none.
- **1. Placement:** CORRECT — Answered at Stage 1 (verse-reading), prefix-selected (M0.1%/M0.5%) so it auto-tracks catalogue changes -- no drift risk.
- **2. Payload:** RIGHT-SIZED — Stage 1 batches per `passage.max_verses` (bounded, never a full-cluster push). Per verse: base text + every live verse_lexical row's role array (deliberately including unwired T-code tags, per cfg_method_rule role-list-includes-unwired-tags -- over-inclusion is a stated, deliberate design choice, not accidental bloat) + meaning_sources for the cluster's own home strongs only (not every M-code strong in the verse) + prior M0.6.5 relational context for the SAME verses only. Bounded and scoped correctly.
- **3. Instructions:** COMPLIANT (definitional-category;front-loaded-exposure) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CONFIRMED BUG -- #1815 (tracked, not yet fixed) — `cluster_code` is resolved via `_effective_cluster_code()`'s LIKE 'M%' fallback against live `cluster_strong` -- correct for a strong with a real M-code home, but for a front-loaded NON-MEMBER strong whose only home is a T-code, the fallback silently misattributes the answer to the calling pass's own cluster_code instead of leaving it unhomed. #1817 closed the population of T-code-only strongs this can hit (fully enumerable now); the attribution logic itself is unfixed. Applies to M0.1.x/M0.5.x only (the front-loaded word-level battery) -- confirmed by code: `_WORD_LEVEL_QUESTION_PREFIXES = ('M0.1', 'M0.5')` is the only trigger for this override path.

### M0.5.6 — Word/term (lexical)

- **current text:** Does the vocabulary include a person-type term — one for the person who habitually possesses or exercises this characteristic? Record it, or none.
- **1. Placement:** CORRECT — Answered at Stage 1 (verse-reading), prefix-selected (M0.1%/M0.5%) so it auto-tracks catalogue changes -- no drift risk.
- **2. Payload:** RIGHT-SIZED — Stage 1 batches per `passage.max_verses` (bounded, never a full-cluster push). Per verse: base text + every live verse_lexical row's role array (deliberately including unwired T-code tags, per cfg_method_rule role-list-includes-unwired-tags -- over-inclusion is a stated, deliberate design choice, not accidental bloat) + meaning_sources for the cluster's own home strongs only (not every M-code strong in the verse) + prior M0.6.5 relational context for the SAME verses only. Bounded and scoped correctly.
- **3. Instructions:** COMPLIANT (definitional-category;front-loaded-exposure) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CONFIRMED BUG -- #1815 (tracked, not yet fixed) — `cluster_code` is resolved via `_effective_cluster_code()`'s LIKE 'M%' fallback against live `cluster_strong` -- correct for a strong with a real M-code home, but for a front-loaded NON-MEMBER strong whose only home is a T-code, the fallback silently misattributes the answer to the calling pass's own cluster_code instead of leaving it unhomed. #1817 closed the population of T-code-only strongs this can hit (fully enumerable now); the attribution logic itself is unfixed. Applies to M0.1.x/M0.5.x only (the front-loaded word-level battery) -- confirmed by code: `_WORD_LEVEL_QUESTION_PREFIXES = ('M0.1', 'M0.5')` is the only trigger for this override path.

### M0.5.7 — Word/term (lexical)

- **current text:** Does the vocabulary include a supplication or seeking term — one for the act of seeking this characteristic from another? Record it, or none.
- **1. Placement:** CORRECT — Answered at Stage 1 (verse-reading), prefix-selected (M0.1%/M0.5%) so it auto-tracks catalogue changes -- no drift risk.
- **2. Payload:** RIGHT-SIZED — Stage 1 batches per `passage.max_verses` (bounded, never a full-cluster push). Per verse: base text + every live verse_lexical row's role array (deliberately including unwired T-code tags, per cfg_method_rule role-list-includes-unwired-tags -- over-inclusion is a stated, deliberate design choice, not accidental bloat) + meaning_sources for the cluster's own home strongs only (not every M-code strong in the verse) + prior M0.6.5 relational context for the SAME verses only. Bounded and scoped correctly.
- **3. Instructions:** COMPLIANT (definitional-category;front-loaded-exposure) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CONFIRMED BUG -- #1815 (tracked, not yet fixed) — `cluster_code` is resolved via `_effective_cluster_code()`'s LIKE 'M%' fallback against live `cluster_strong` -- correct for a strong with a real M-code home, but for a front-loaded NON-MEMBER strong whose only home is a T-code, the fallback silently misattributes the answer to the calling pass's own cluster_code instead of leaving it unhomed. #1817 closed the population of T-code-only strongs this can hit (fully enumerable now); the attribution logic itself is unfixed. Applies to M0.1.x/M0.5.x only (the front-loaded word-level battery) -- confirmed by code: `_WORD_LEVEL_QUESTION_PREFIXES = ('M0.1', 'M0.5')` is the only trigger for this override path.

### M0.5.8 — Word/term (lexical)

- **current text:** What does the relationship between the OT Hebrew and NT Greek vocabulary show about continuity or development of the characteristic across the Testaments?
- **1. Placement:** CORRECT — Answered at Stage 1 (verse-reading), prefix-selected (M0.1%/M0.5%) so it auto-tracks catalogue changes -- no drift risk.
- **2. Payload:** RIGHT-SIZED — Stage 1 batches per `passage.max_verses` (bounded, never a full-cluster push). Per verse: base text + every live verse_lexical row's role array (deliberately including unwired T-code tags, per cfg_method_rule role-list-includes-unwired-tags -- over-inclusion is a stated, deliberate design choice, not accidental bloat) + meaning_sources for the cluster's own home strongs only (not every M-code strong in the verse) + prior M0.6.5 relational context for the SAME verses only. Bounded and scoped correctly.
- **3. Instructions:** COMPLIANT (definitional-category;front-loaded-exposure) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CONFIRMED BUG -- #1815 (tracked, not yet fixed) — `cluster_code` is resolved via `_effective_cluster_code()`'s LIKE 'M%' fallback against live `cluster_strong` -- correct for a strong with a real M-code home, but for a front-loaded NON-MEMBER strong whose only home is a T-code, the fallback silently misattributes the answer to the calling pass's own cluster_code instead of leaving it unhomed. #1817 closed the population of T-code-only strongs this can hit (fully enumerable now); the attribution logic itself is unfixed. Applies to M0.1.x/M0.5.x only (the front-loaded word-level battery) -- confirmed by code: `_WORD_LEVEL_QUESTION_PREFIXES = ('M0.1', 'M0.5')` is the only trigger for this override path.

### M0.5.9 — Word/term (lexical)

- **current text:** Is there a term newly coined in the NT period for this characteristic; if so, what does the coinage show? Record it, or none.
- **1. Placement:** CORRECT — Answered at Stage 1 (verse-reading), prefix-selected (M0.1%/M0.5%) so it auto-tracks catalogue changes -- no drift risk.
- **2. Payload:** RIGHT-SIZED — Stage 1 batches per `passage.max_verses` (bounded, never a full-cluster push). Per verse: base text + every live verse_lexical row's role array (deliberately including unwired T-code tags, per cfg_method_rule role-list-includes-unwired-tags -- over-inclusion is a stated, deliberate design choice, not accidental bloat) + meaning_sources for the cluster's own home strongs only (not every M-code strong in the verse) + prior M0.6.5 relational context for the SAME verses only. Bounded and scoped correctly.
- **3. Instructions:** COMPLIANT (definitional-category;front-loaded-exposure) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CONFIRMED BUG -- #1815 (tracked, not yet fixed) — `cluster_code` is resolved via `_effective_cluster_code()`'s LIKE 'M%' fallback against live `cluster_strong` -- correct for a strong with a real M-code home, but for a front-loaded NON-MEMBER strong whose only home is a T-code, the fallback silently misattributes the answer to the calling pass's own cluster_code instead of leaving it unhomed. #1817 closed the population of T-code-only strongs this can hit (fully enumerable now); the attribution logic itself is unfixed. Applies to M0.1.x/M0.5.x only (the front-loaded word-level battery) -- confirmed by code: `_WORD_LEVEL_QUESTION_PREFIXES = ('M0.1', 'M0.5')` is the only trigger for this override path.

### M0.6.1 — The verse

- **current text:** What is the function of the primary term within its primary verse -- (a) what role does it play in the sentence, and (b) what role does it play in the verse's own argument, if a connective/chain edge shows one?
- **1. Placement:** WRONG -- NO STAGE SELECTS THIS SCOPE — No stage's query includes this question's scope at all (Verse-context/The verse/most of Word-term lexical) -- structurally invisible regardless of how many clusters get processed. This is #1806's own ORIGINAL ask, still not redesigned.
- **2. Payload:** N/A — Never presented to any stage -- no payload exists to audit.
- **3. Instructions:** COMPLIANT (none) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### M0.6.2 — The verse

- **current text:** What is the logical structure of the key arguments in the verse evidence — premises and conclusions?
- **1. Placement:** WRONG -- NO STAGE SELECTS THIS SCOPE — No stage's query includes this question's scope at all (Verse-context/The verse/most of Word-term lexical) -- structurally invisible regardless of how many clusters get processed. This is #1806's own ORIGINAL ask, still not redesigned.
- **2. Payload:** N/A — Never presented to any stage -- no payload exists to audit.
- **3. Instructions:** REVISE (grain-vague) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### M0.6.3 — The verse

- **current text:** Does any verse function as the primary anchor — the one most fully and directly expressing the characteristic's essential character? Record it, or none.
- **1. Placement:** WRONG -- NO STAGE SELECTS THIS SCOPE — No stage's query includes this question's scope at all (Verse-context/The verse/most of Word-term lexical) -- structurally invisible regardless of how many clusters get processed. This is #1806's own ORIGINAL ask, still not redesigned.
- **2. Payload:** N/A — Never presented to any stage -- no payload exists to audit.
- **3. Instructions:** COMPLIANT (none) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### M0.6.4 — The verse

- **current text:** What does the primary anchor verse show that no other verse shows?
- **1. Placement:** WRONG -- NO STAGE SELECTS THIS SCOPE — No stage's query includes this question's scope at all (Verse-context/The verse/most of Word-term lexical) -- structurally invisible regardless of how many clusters get processed. This is #1806's own ORIGINAL ask, still not redesigned.
- **2. Payload:** N/A — Never presented to any stage -- no payload exists to audit.
- **3. Instructions:** COMPLIANT (none) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### M0.6.5 — The verse

- **current text:** What role does this characteristic play in relation to the OTHER M-code characteristics present in this verse? Build on whatever earlier cluster-passes over this SAME verse already found (given as prior context) -- do not re-derive or duplicate their findings; the focus shifts to what THIS characteristic's own vantage point adds. Record none if no other M-code characteristic is present.
- **1. Placement:** CORRECT — Answered at Stage 1 by exact question_code pin (D7.7.1/M0.6.5) -- deliberate, not a hardcoded-list bug (confirmed live 2026-09-21, escalation #1806 v8).
- **2. Payload:** RIGHT-SIZED — Same Stage 1 payload as DYNAMIC-STAGE1 (same batch, same call) -- no separate payload construction.
- **3. Instructions:** REVISE (process-instruction-mixed;compound) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — M0.6.5/D7.7.1 stay keyed to the calling pass's own cluster_code by design (`_effective_cluster_code` explicitly does NOT override these -- they are that cluster's own vantage point on the verse, not a strong-level fact). `cluster_subgroup_id`/`cluster_subgroup_code` correctly NULL (pre-subgroup stage).

### T7.3.1 — Science

- **current text:** Which human-science framework (psychology, moral philosophy, developmental psychology, sociology, anthropology, or other) serves as the most useful interpretive lens for this characteristic?
- **1. Placement:** N/A -- DELETED — deleted=1 in the live catalogue; not presented to any stage by design, not a gap.
- **2. Payload:** N/A — deleted=1 -- never presented, no payload.
- **3. Instructions:** RETIRED (deleted=1) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### T7.3.2 — Science

- **current text:** Where the framework illuminates the verse evidence — making a finding more coherent or complete — what does it show?
- **1. Placement:** N/A -- DELETED — deleted=1 in the live catalogue; not presented to any stage by design, not a gap.
- **2. Payload:** N/A — deleted=1 -- never presented, no payload.
- **3. Instructions:** RETIRED (deleted=1) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### T7.3.3 — Science

- **current text:** Where the verse evidence and the framework diverge, what does the divergence show?
- **1. Placement:** N/A -- DELETED — deleted=1 in the live catalogue; not presented to any stage by design, not a gap.
- **2. Payload:** N/A — deleted=1 -- never presented, no payload.
- **3. Instructions:** RETIRED (deleted=1) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### T7.3.4 — Science

- **current text:** Does the framework surface any aspect of the characteristic the verse evidence has not yet addressed, and does that absence call for further verse investigation?
- **1. Placement:** N/A -- DELETED — deleted=1 in the live catalogue; not presented to any stage by design, not a gap.
- **2. Payload:** N/A — deleted=1 -- never presented, no payload.
- **3. Instructions:** RETIRED (deleted=1) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### X0.1.1 — Characteristic relational

- **current text:** Which adjacent characteristics appear alongside this one in the verse evidence, and how frequently? Record none if no significant co-occurrence appears.
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (grain-vague(evidence)) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### X0.1.2 — Characteristic relational

- **current text:** What does the co-occurrence pattern show about this characteristic's place in the inner-being landscape?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (grain-missing) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### X0.2.1 — Characteristic relational

- **current text:** Does the evidence show this characteristic consistently preceding, following, or accompanying another in a sequence; if so, which and how? Record none if no sequence appears.
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (grain-vague(evidence)) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### X0.2.2 — Characteristic relational

- **current text:** What does the sequence show — is the relationship causal, developmental, or correlational?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (grain-missing) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### X0.3.1 — Characteristic relational

- **current text:** Does this characteristic produce another in the evidence, and if so which, and by what mechanism? Record none if none is shown.
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (grain-vague(evidence)) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### X0.3.2 — Characteristic relational

- **current text:** Is this characteristic produced by another, and if so which?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (grain-missing) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### X0.3.3 — Characteristic relational

- **current text:** Is this characteristic a constituent element of another, or another a constituent of this one?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (grain-missing) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### X0.4.1 — Word/term (lexical)

- **current text:** Which vocabulary terms, if any, does this characteristic share with other characteristics in the programme? Record none if none is shown.
- **1. Placement:** WRONG -- NO STAGE SELECTS THIS SCOPE — No stage's query includes this question's scope at all (Verse-context/The verse/most of Word-term lexical) -- structurally invisible regardless of how many clusters get processed. This is #1806's own ORIGINAL ask, still not redesigned.
- **2. Payload:** N/A — Never presented to any stage -- no payload exists to audit.
- **3. Instructions:** COMPLIANT (none) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### X0.4.2 — Word/term (lexical)

- **current text:** Does the sharing extend to root-level architecture — a shared root generating terms across two or more characteristics?
- **1. Placement:** WRONG -- NO STAGE SELECTS THIS SCOPE — No stage's query includes this question's scope at all (Verse-context/The verse/most of Word-term lexical) -- structurally invisible regardless of how many clusters get processed. This is #1806's own ORIGINAL ask, still not redesigned.
- **2. Payload:** N/A — Never presented to any stage -- no payload exists to audit.
- **3. Instructions:** COMPLIANT (none) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### X0.4.3 — Word/term (lexical)

- **current text:** What does the vocabulary sharing show about the conceptual relationship between the characteristics?
- **1. Placement:** WRONG -- NO STAGE SELECTS THIS SCOPE — No stage's query includes this question's scope at all (Verse-context/The verse/most of Word-term lexical) -- structurally invisible regardless of how many clusters get processed. This is #1806's own ORIGINAL ask, still not redesigned.
- **2. Payload:** N/A — Never presented to any stage -- no payload exists to audit.
- **3. Instructions:** COMPLIANT (none) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** N/A — Never answered by any stage -- no write-back to audit.

### X0.5.1 — Characteristic relational

- **current text:** Which adjacent characteristic most closely resembles this one, and what precisely distinguishes them?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** COMPLIANT (none) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### X0.5.2 — Characteristic relational

- **current text:** Where the evidence shows apparent overlap, what is the precise boundary?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (subject-vague;grain-vague(evidence)) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

### X0.5.3 — Characteristic relational

- **current text:** Is the distinction from the nearest neighbour one of degree, kind, direction, or constitutional level?
- **1. Placement:** CORRECT — Answered at Stage 4 (char-answers), scope-selected live from wa_obs_question_catalogue -- auto-tracks catalogue changes.
- **2. Payload:** RISK -- NO OVER-CAP PROTECTION (NEW FINDING, 2026-09-21) — Stage 4 (`charanswergenerate.py`) sends ALL ~52 characteristic-grain questions in ONE call per subgroup, with EVERY corpus-wide occurrence for EVERY member strong (uncapped) plus the full verse-reading + char-subgroup + char-reading observation history for every member strong. Unlike Stage 3, there is NO per-strong occurrence cap and NO splitting mechanism at all -- `charanswergenerate.py` has no equivalent of Stage 3's `_MAX_OCCURRENCES_PER_STRONG`/`_partition_occurrences_by_surface`. Confirmed this session the largest live Stage-4 payload so far (M83/D_consultative_inquiry, 164 occurrences, 1 strong) succeeded at $0.69/call with 6/52 questions left unanswered -- so the mechanism is not broken, but there is no ceiling if a much larger subgroup ever reaches this stage, and no signal before the fact that a given subgroup's payload is approaching a real risk. Flagged, not fixed here -- a genuine build gap for #1806/#1805.
- **3. Instructions:** REVISE (grain-missing) — see catalogue-question-wording-review-20260921-v5 for full rationale/revised text.
- **4. Write-back:** CORRECT — `cluster_code` = the pass's own cluster_code always (Stage 4 is never front-loaded, `_effective_cluster_code` only overrides M0.1/M0.5 prefixes) -- correct for a genuinely per-cluster/subgroup-scoped stage. `cluster_subgroup_id`/`cluster_subgroup_code` correctly populated from the caller's own known subgroup (confirmed fixed 2026-09-18, previously NULL for all 4 live stages until the researcher's own review caught it). `window` correctly derived from the catalogue's own `window` column and validated against live cfg_enum. Completeness is CODE-computed after write (`compute_question_checks`), not LLM-self-reported -- confirmed live: M83/D_consultative_inquiry and M83/B_intensified_seeking both surfaced real 'N of 52 unanswered' counts in their own run outcome, not silently absorbed.

