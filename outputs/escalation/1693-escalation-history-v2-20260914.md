# Escalation deep history

## #1693 — Design: cluster-reading load/reconcile pass
type=task source=researcher

**v1** (2026-09-12T04:19:28Z, Claude) state=raised next_action=review assigned_to=Researcher
> **short description (set this version):** Design: cluster-reading load/reconcile pass
> **comment (set this version):** Not one of the researcher's 3 named components (family/observation/trace), but a 4th, genuinely separate design problem this chat turn's correction surfaced: turning a family's freshly-generated JSON into DB rows now requires deciding, per citation, whether it supports an EXISTING observation (write trace only), needs an existing observation's statement BROADENED (edit statement, then trace), or is genuinely NEW (insert observation, then trace) -- a similarity/matching judgment, not a mechanical load. Open questions: (1) how is 'same observation' actually decided -- text similarity, semantic/LLM comparison, a human-reviewed queue, some combination? (2) what happens on an ambiguous match (candidate could plausibly be 2+ existing observations) -- auto-pick the best, always flag for review, never auto-decide? (3) is this pass itself an LLM-assisted session (matching narrative claims is a judgment task) or can a first cut use simple heuristics (exact strong+verse match, say) and only fall back to judgment when that's inconclusive? (4) how does re-running the SAME family's JSON avoid creating duplicate trace rows -- ties directly into #1692's uniqueness-constraint question.
> **context (set this version):** Parent: escalation #1682. Data model doc: iba/docs/1682-cluster-reading-data-model-v1-20260911.md §6 item 1. Related: #1692 (trace).

**v2** (2026-09-12T06:16:30Z, Claude) state=in-progress next_action=review assigned_to=Researcher
> **comment (set this version):** New concrete requirement added, from a real, live-checked example: this pipeline's own verse-reference risk is confirmed real, not hypothetical -- checked iba.db's canonical verse.reference for 4 verses cited in this document's own prose (1John 3:8, 1Cor 11:27, 2Pet 2:4, Isaiah 53:5): 3 of my 4 examples used a non-canonical form (DB has '1Jo 3:8', '2Pe 2:4', 'Isa 53:5'). Process (a) pulls references mechanically by SQL (safe); processes (c)/(d)/(e) are LLM-authored JSON with nothing currently guaranteeing the exact string survives. Recommended fix, now part of this escalation's scope: the load/reconcile pass should RESOLVE each citation's verse_reference fresh against iba.db.verse.reference (matched via strong+occurrence), never trust the string the LLM wrote -- closes the risk structurally rather than needing a corpus-wide free-text audit. Also inherits #1691's numbering-scheme question (this pass has to consume whatever identity scheme the generation script produces).
> **context (set this version):** Full detail: iba/docs/1682-cluster-reading-data-model-v1-20260911.md §5.

**v3** (2026-09-12T07:50:05Z, Claude) state=in-progress next_action=review assigned_to=Researcher
> **comment (set this version):** Verse-reference validation scope confirmed by you (upload must validate/align all references). Checked the broader question live: finding_verse_link.reference, verse_span_index.reference, and the legacy bible_research.db.verse.reference were all spot-checked against iba.db's canonical form for the same verses -- every sample matched exactly. No second, divergent canonical format found living in another table (not exhaustively checked, but no contrary evidence either) -- the risk is confined to LLM/human-authored prose, not any existing structured column. This confirms the recommended fix (resolve verse_reference fresh at load time against iba.db, never trust LLM-typed text) is sufficient on its own, no broader reconciliation needed. Full detail: iba/docs/1682-cluster-reading-data-model-v1-20260911.md §5.

**v4** (2026-09-12T16:50:55Z, Claude) state=in-progress next_action=review assigned_to=Researcher
> **comment (set this version):** Consolidated per your instruction ('create a separate section in the plan to handle the table updates -- table update is a separate operation from the LLM session'): this is now the one place where cluster_subgroup/IB_obs/IB_node's separate table-update responsibilities converge into one operational spec. What's settled (verse-reference resolution, denormalization, id assignment, LLM never touches the DB) is pulled together, not re-decided. The one real gap across all 4 escalations is isolated clearly here: the same/broaden/new matching logic that decides whether an incoming citation is a new IB_obs row, broadens an existing one, or just adds a citation -- still completely undesigned, and this document's own §3 is where that design needs to happen next.
> **context (set this version):** Full detail: iba/docs/1693-table-update-procedure-finalization-v1-20260912.md

**v5** (2026-09-12T17:23:10Z, Claude) state=in-progress next_action=review assigned_to=Researcher
> **comment (set this version):** Researcher question this chat turn: should (c)/(d)/(e) continue as separate JSONs, or be merged? My view, given: (1) synthesis is not per-family at all -- structurally can't share a per-family file with (c)/(d); (2) the review cycle already used this session depends on independent stage boundaries -- (c) went through a full round-1/round-2 rewrite without touching any (d)/(e) file, which a merged-file approach would complicate; (3) it matches IB_obs.stage, a column that already exists specifically to distinguish these three -- a document boundary aligned with it isn't arbitrary. Recommendation: keep separate. One addition, not a replacement: once the numbering scheme (source_json_serial + generation-run id) exists, a thin per-family index of which (c)/(d) runs exist could help human browsing -- an index alongside separate files, not a merge. Not yet a settled decision -- researcher's call.

**v6** (2026-09-13T08:13:25Z, Claude) state=in-progress next_action=review assigned_to=Researcher
> **comment (set this version):** Cross-ref from #1691 (9.4/9.6, researcher's chat turn 2026-09-13): run structure confirmed -- each cluster-process run is three components (assemble input JSON / run the sub-process c-d-e / run this DB update), and the DB update fires immediately after the sub-process's JSON is produced, not on a separate deferred trigger. Section 3 (same/broaden/new) partially resolved: outer structure is search-first, then either attach a new ib_node to an existing observation or create a new observation+node; the actual similarity test underneath is still undesigned. Flagged, not resolved: a reconciliation gap between this two-outcome structure and section 2's three listed ib_observation outcomes (new/broaden obs_text/add citation).
> **context (set this version):** Doc updated in place: iba/docs/1693-table-update-procedure-finalization-v1-20260912.md (new section 0, section 3).

**v7** (2026-09-13T15:32:09Z, Claude) state=in-progress next_action=review assigned_to=Researcher
> **comment (set this version):** DB fork applied: this procedure operates entirely within iba.db, explicit rule added -- must never read or write bible_research.db. Fixed stale cross-references left over from before the rename/fork: section 2's table now says cluster_subgroup_strong/strong/iba.strong throughout (was mti_term_subgroup/mti_term_id/mti_terms.id). One open exception carried over from #1691: question_code's source catalogue (wa_obs_question_catalogue) is still bible_research.db-only -- this procedure doesn't need to write there, but the read in process (a)/(b) input assembly is unresolved, tracked at #1691 section 7a, not duplicated here.
> **context (set this version):** Doc updated in place: iba/docs/1693-table-update-procedure-finalization-v1-20260912.md (status banner, section 2).

**v8** (2026-09-13T16:03:59Z, Claude) state=in-progress next_action=review assigned_to=Researcher
> **comment (set this version):** Cross-ref note added per new escalation #1697 (iba.cluster.status lifecycle, same sign-off pack): this procedure is where the actual check/advance logic lands -- a status precondition check added before step (b) of the run structure (section 0), and a status advance added as part of step (c), once this procedure's own writes succeed. Not designed further here -- full rule and enum at #1697.
> **context (set this version):** Doc updated in place: iba/docs/1693-table-update-procedure-finalization-v1-20260912.md (section 0).

**v9** (2026-09-13T16:18:39Z, Claude) state=in-progress next_action=review assigned_to=Researcher
> **comment (set this version):** Corrected the section 0 note to a two-level model per your rollup instruction: process (b) and synthesis check/advance cluster.status directly (genuinely cluster-grain); reading/answer check/advance cluster_subgroup.status (#1690 3a) instead. On every successful write, this procedure also recomputes the cluster-level rollup (#1697 section 3) -- advancing or regressing cluster.status as a consequence of the subgroup-level change, in the same write.
> **context (set this version):** Doc updated in place: iba/docs/1693-table-update-procedure-finalization-v1-20260912.md (section 0).

**v10** (2026-09-14T03:55:01Z, Claude) state=in-progress next_action=review assigned_to=Researcher
> **comment (set this version):** Two additions this chat turn: (1) placement_note promotion responsibility added -- a real observation in cluster_subgroup_strong.placement_note must be promoted to ib_observation/ib_node under the new stage='subgroup' value; the same/broaden/new-adjacent question of telling a bookkeeping placement_note from an observational one is left undesigned, folded into this doc's existing sec3 gap. (2) Flagged, not designed: synthesis's cluster.status precondition (sec0) is single-cluster, but synthesis is now confirmed cross-cluster (#1691 sec9 item9) -- raised as its own escalation #1698.
> **context (set this version):** Doc updated in place: iba/docs/1693-table-update-procedure-finalization-v1-20260912.md (table in section2, section0).

researcher comments

we need to get a nice name for this routine : load/reconcile pass is clumsy. what do you suggest.

this routine records the analytic work into a coherent and consistent set of knowledge elements as observations.

it is separate from the generation of knowledge (analytics stages) to force a division of duties and improve overall quality control.

(1) how is 'same observation' actually decided -- text similarity, semantic/LLM comparison, a human-reviewed queue, some combination? : I don't want to over complicate this test. if in doubt, create it as separate. differences matter, don't fuzz them away.  superficial differences create noise - if its possible, then align the differences into a single observation ( CC has the right to edit an existing item for alignment).  It is not a direct text match only, it does include judgement, mainly use heuristics and simple meaning. it is ok to ask researcher for a judgement, but only if CC considers a judgement call as material (don't as me just because you are trained to always ask at least 3 items for clarification - if you know the answer, dont ask.)

(4) how does re-running the SAME family's JSON avoid creating duplicate trace rows -- ties directly into #1692's uniqueness-constraint question:   there is a risk of duplication in such cases - and would rather have a duplicate, that none at all.  however, if the wording of the observation, and the ib_node references are all the same, then it is an obvious duplicate.

a observation that expands on an existing observation is regarded as a new observation.  I can't remember of the related observation column was created in ib_node, but this is how that related observation is used in ib_node.

