# Escalation deep history

## #1767 — Two-tier (map-reduce) redesign for cluster.reading
type=task source=researcher

**v1** (2026-09-18T13:39:36Z, Claude) state=raised next_action=review assigned_to=Researcher
> **short description (set this version):** Two-tier (map-reduce) redesign for cluster.reading
> **comment (set this version):** Researcher's own proposal this chat turn, triggered by M83 subgroup A (G2212 + H1245, BOTH simultaneously over the occurrence cap -- the MultipleOverCapStrongs case #1761's surface-split fix explicitly doesn't cover, since that fix assumes exactly one over-cap strong per subgroup with the others riding along in full for cross-strong comparison): 'this problem is just going to get worse as we go along. maybe we must simply batch verses in a sub group, and then read the results for the second tier verse observations.' Researcher confirmed my elaboration of this as a genuine two-tier/map-reduce redesign of cluster.reading itself (Stage 3), not a patch for today's one case -- explicitly: 'create a escalation for this, so we can deal with it outside the pipeline. don't try to fix it in this run. we will likely rerun the tree clusters.'
> **context (set this version):** Proposed shape (Claude's elaboration, researcher confirmed the direction, exact mechanics still to be designed properly, not built): TIER 1 (batch reading) -- resolve the WHOLE subgroup's occurrence set across ALL member strongs together (not siloed per-strong like #1761's surface-split), batch by VERSE the same way Stage 1 (lexical.meaning) already does (small chunks, ~10-20 verses), read each batch, write properly-grounded observations per batch (stage='char-reading' or a new sub-stage). Scales regardless of subgroup size, since no single call ever holds the whole thing -- this structurally also closes the #1765 grounding-gap risk as a side effect, since small per-batch reads make citing real occurrences far easier than one call trying to cite hundreds. TIER 2 (synthesis over Tier 1) -- a SEPARATE call reading Tier 1's OWN observations (not raw verse text again) for this subgroup, producing the actual cross-strong synergy narrative -- today's single Stage 3 call's whole job, but working from condensed Tier-1 findings instead of a raw occurrence dump, so it should comfortably fit in one call no matter how many strongs/occurrences the subgroup has. This is the structural fix for BOTH problems at once: subgroup A's two-simultaneously-over-cap-strongs deadlock (#1761's surface-split can't handle it), and the general scaling concern as more/larger clusters are processed. Real design work before building, not mechanical: exact Tier-1 batch sizing, whether Tier-1 observations get their own stage/tag value or reuse char-reading's, how Tier 2's own output relates to Tier 1's in the schema (does Tier 2 supersede/summarize Tier 1, or do both stand as permanent records), and the same batchcontrol resume/skip integration #1756 already established for other multi-batch steps. Explicitly NOT to be fixed/built this session -- researcher's own instruction, deal with outside the pipeline. M83 subgroup A stays blocked/unresolved until this is designed and built; M49 and M83 (and likely other already-processed clusters) will probably need re-reading once it lands, since the current single-call Stage 3 design is what's being superseded, not just supplemented.

**v2** (2026-09-18T17:03:09Z, Researcher) state=in-progress next_action=review assigned_to=Claude
> **comment (set this version):**  propose a build for this two tier process 
> **context (set this version):**   
