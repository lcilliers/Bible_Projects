# Escalation deep history

## #1782 — ib_observation.status hardcoded, lifecycle never wired up
type=issue source=researcher

**v1** (2026-09-20T07:16:50Z, Claude) state=raised next_action=review assigned_to=Researcher
> **short description (set this version):** ib_observation.status hardcoded, lifecycle never wired up
> **comment (set this version):** Researcher instruction, this chat turn (on #1778): 'create an escalation for fixing the bug that ib_observation.status was never built and active as intended. this utility will obviously use this status also.' Root cause confirmed live: iba/app/lib/recordingpass.py:213 (_insert_observation) hardcodes status='resolved' literally on every INSERT -- there is no code path anywhere that writes needs-corroboration/open/silent. Confirmed: all 7046 live ib_observation rows have status='resolved'; cfg_enum ib_observation.status correctly lists all 4 intended values (resolved/needs-corroboration/open/silent, from register_ib_observation_enums_v1_20260917.py), so the enum was built but never actually driven by the write path. First flagged as a design gap during tonight's #1753 B2 audit but not escalated on its own at the time.
> **context (set this version):** Directly coupled to #1778 (observation_enhancer utility, formerly misnamed 'correction'): the researcher has now confirmed the enhancer utility will set/use this status field as one of its update targets (e.g. a rule setting status=needs-corroboration instead of editing obs_text directly). Fixing this bug is a prerequisite/companion to that utility's build -- deciding (a) whether recordingpass.py's write-time default should change from a hardcoded 'resolved' to something else (e.g. 'open' pending review), and (b) which transitions between the 4 values are write-time (LLM pipeline) vs enhancer-time (rule-driven), is a design call, not a one-line fix. Proposal doc: iba/docs/1778-correction-rule-utility-proposal-v1-20260919.md (being revised this round to fold this in).

**v2** (2026-09-20T07:36:52Z, Claude) state=in-progress next_action=review assigned_to=Researcher
> **comment (set this version):** Mechanical part fixed and applied live, per your 5-value definitions this chat turn: (1) cfg_enum gained 'withdrawn' (#1784, applied); (2) cfg_column.use for ib_observation.status corrected -- it was actively wrong, describing a different, unused provisional/corroborated/superseded scheme (#1786, applied); (3) recordingpass.py:213's write-time default changed from hardcoded 'resolved' to 'open', matching 'open - the default setting'. BUILD.md #301.
> **context (set this version):** NOT done, and this is the reason the escalation stays open rather than closing: none of the 4 places that read ib_observation for downstream analysis (subgroupgenerate.py/charreadinggenerate.py/charanswergenerate.py/versereadinggenerate.py) filter on status. Your own definition says resolved is 'only set once the observation is cleared for inclusion in analysis' -- read literally, every other status (open/needs-corroboration/silent/withdrawn) should be EXCLUDED from those reads. But nothing yet promotes open -> resolved (that's #1778, observation-enhancer, proposed not yet approved) -- if I add that gate now, every newly-written observation defaults to open and would silently vanish from subgroup/reading/answer stages for M49/M83 (both actively running) with no way back to resolved yet. Recommendation: hold off gating the 4 read sites until #1778 exists (or some other promotion mechanism), then add the gate in the same unit of work as whatever first sets status=resolved -- not before. Your call whether to proceed that way or something else.
> **resolution (set this version):** Partial: write-time default + enum + definitions fixed and live. Downstream gating on status='resolved' deliberately deferred pending #1778 -- flagging back to you rather than deciding silently.

**v3** (2026-09-20T08:53:00Z, Claude) state=in-progress next_action=review assigned_to=Researcher
> **comment (set this version):** Per your instruction this chat turn: (1) recordingpass.py's insert path (_insert_observation) now defaults to draft, not open. (2) The aligned-superficial-edit path (the only existing routine that EDITS an ib_observation row's obs_text in place, recordingpass.py ~line 325) now also resets status to draft on every edit -- an edited observation should be re-reviewed, not silently keep a stale status. (3) cfg_enum ib_observation.status gained draft (6th value); cfg_column.use updated to define it and flag that open now has no writer. (4) Used the new observation_enhancer utility for real, on real data: rule 1782-bulk-reset-to-draft (selector = all of ib_observation, update = status:draft) -- proposed, previewed (confirmed exact match: 7046/7046 rows), confirmed, applied. Verified live: all 7046 rows now status=draft, 7046 ib_observation_enhancer_log rows written, rule promoted to active. BUILD.md #301 updated inline is not done -- this is tracked under this escalation's own history, not a new BUILD.md entry, since it directly continues #301's own topic.
> **resolution (set this version):** Insert/edit default is draft; all existing observations reset to draft via the enhancer, verified. open remains a defined-but-unused value -- flagging again: still your call whether to retire it. Also still open: whether/when to gate the 4 downstream analysis reads (now that everything sits at draft, this is more pressing than before -- nothing currently reads ib_observation with any status filter, so draft rows flow into subgroup/reading/answer stages exactly as resolved rows did).

**v4** (2026-09-20T12:06:20Z, Researcher) state=in-progress next_action=review assigned_to=Claude
> **comment (set this version):**  there are a number of different observation add/edit events through the pipeline.  Two things are critically important regarding status.  A) the input into the llm session must include all the relevant observations for the scope of the particular operation, and that includes all observations related to it, irrespective of status. llm must consider an existing observation for the same point before creating a new observation. if the existing observation need updating or correction then that must get preference to a new observation.  the status of the observation changed will be reset to draft.  b) existing observations that is not changed must retain their current status. c) all new observations are set to draft  
> **context (set this version):**   
