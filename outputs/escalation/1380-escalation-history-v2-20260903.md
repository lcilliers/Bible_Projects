# Escalation deep history

## #1380 — Developer Mode: session-boundary enforcement flag
type=task source=claude

**v1** (2026-09-02T02:27:33Z, Claude) state=raised next_action=review assigned_to=Researcher
> **short description (set this version):** Developer Mode: session-boundary enforcement flag
> **comment (set this version):** Researcher instruction (2026-09-02): raise an escalation for Developer Mode operation, seeded with everything known of Developer Mode's own build (2026-08-31), to be picked up and designed/built out from inside a future Developer Mode session -- researcher will grant access to this escalation there. Carries a specific flag for that build: Developer Mode must be prevented from running unless declared at the start of a genuinely clear/fresh new session, and once active must not be exitable except via an explicit session-end -- not by /exit-developer-mode alone, not by continuing the same conversation in App Mode afterward. Today neither half of that is actually enforced -- see context for what exists, what is missing, and why an earlier in-app bypass-mechanism design was already rejected once (relevant precedent for how NOT to build this).
> **context (set this version):** SEED -- everything known of Developer Mode's build, gathered live 2026-09-02 (not from memory alone; files re-read, DB queried, code grepped).

WHAT EXISTS TODAY (built 2026-08-31, two same-day sessions):
1. Session 1 (commit af7ab393, "escalation system bugs found+fixed... Developer Mode/App Mode operating split established per researcher correction"). Trigger: researcher spent a session routing an ordinary escalation.py bug fix through configmaint.propose's per-row approval gate one row at a time (5 sequential patch round-trips) -- verbatim: "why is it that you cannot rewrite a piece of code to suit the requirements because since day one you have been patching this code and forever creating more problems." Researcher then re-stated (verbatim, capitalized) the two-mode split as having been established at the app's ORIGINAL design discussion, not new. First draft of the mechanism (escalations #1342/#1343): a per-cfg_*-table classification Claude would self-apply mid-session (some tables "mechanism", writable directly; others "operations", still gated) -- REJECTED OUTRIGHT: "you should not be allowed to swap between developer mode and standard mode on the fly." Corrected same day to a pure session-level model: CHARTER.md sec4 + GOVERNANCE.md sec69 written same session with the corrected text.
2. Session 2 (commit 790d0cfc, "Developer Mode protocol built, escalation CLI friction found+fixed, backlog cleared"). Researcher explicitly opened this session NOT via start-project, specifically to establish the real protocol. Built: .claude/commands/developer-mode.md + exit-developer-mode.md (session-level declaration commands); the visual indicator (.claude/.developer-mode-active marker file, gitignored, drives a mandatory chat banner + .claude/statusline-devmode.ps1 wired via .claude/settings.json statusLine); .claude/hooks/stop_check_escalation_backlog.py (a Stop hook -- adjacent, not the same mechanism: checks next_action_assigned_to='Claude' every turn so backlog items can't silently go stale, built because #1312/#1314 had just done exactly that even with a written rule in place -- same failure-mode shape as what this new flag is meant to prevent, just for a different control).

CONCRETE GAPS, CONFIRMED LIVE THIS SESSION (2026-09-02), NOT ASSUMED:
A. No code anywhere reads the marker file or a developer-mode flag. Grepped iba/app for "developer-mode-active", "developer_mode", "DEVELOPER_MODE" -- zero hits. configmaint.propose's approval gate is one fixed code path, unconditional; Developer Mode "bypasses" it only because Claude, by convention, chooses to write directly instead of calling that tool. Nothing in .claude/settings.json (checked: only a Stop hook + an AskUserQuestion deny) or anywhere else gates entry, gates exit, or checks session freshness.
B. exit-developer-mode.md (18 lines) does exactly one thing: delete the marker file. No session-end check, no confirmation a real boundary occurred -- the same conversation can carry on immediately in "App Mode" with nothing having actually changed except the banner disappearing.
C. developer-mode.md's own self-check ("Do not invoke this mid-session to switch modes... If this session did not start this way, stop and say so") is Claude's own instruction-following, not a system enforcement -- proven live this session (2026-09-02): this exact conversation ran start-project in standard mode, then discussed switching to Developer Mode mid-conversation, and nothing external stopped that discussion from happening; the only thing that would have stopped an actual switch is Claude choosing to honour the written rule.
D. The mode-split rule itself was NEVER captured in cfg_* despite governance.rules_must_be_config_driven ("no operational or process rule may exist only in GOVERNANCE.md... without a referenced cfg_* row... Any deviation discovered requires escalation"). GOVERNANCE.md sec69's own text still cites "escalation #1342, corrected content pending" as the intended record -- but #1342 was WITHDRAWN by the researcher on 2026-08-31 ("not approved, to be redesigned") and never re-proposed. Live query confirms: cfg_behaviour_rule class='development' currently has 11 active rows (root-fix-not-one-off, simple-steps-not-engineered-designs, open-items-route-through-escalation, every-interactive-module-needs-ps-script, user-guide-updated-same-unit-of-work, every-active-ps-script-dispatches-through-run-py, decision-points-are-terminal-not-inline, decision-required-answered-via-update-not-answerrun, test-plan-per-module-utility, config-updated-same-unit-of-work-as-change, claude-held-item-must-progress-or-bounce-back) -- NONE of them is the developer-mode-vs-app-mode rule itself. The whole two-mode split lives only in CHARTER.md/GOVERNANCE.md/the two slash commands/memory. This is itself a live instance of the governance rule it is violating -- worth fixing in the same pass as the new flag, not separately.
E. The withdrawn #1342 draft's own content is relevant precedent for where THIS flag row should be proposed from: it judged the mode-split rule as belonging on the "App Operations" side of the line ("governs the researcher/Claude working relationship project-wide... not the app internal dispatch mechanism"), i.e. it argued the row should go through the ordinary configmaint.propose cycle in a STANDARD session, not be written ad hoc from inside Developer Mode. Worth re-testing that judgement when this item is built, including for whatever new cfg_behaviour_rule row this escalation's own flag produces.

PRIOR REJECTED DESIGN -- DO NOT REPEAT THE SAME SHAPE OF MISTAKE:
Before the session-level model, an in-app bypass mechanism was proposed (a "devmode.write" tool letting Claude override configmaint.propose's gate from inside a nominally-standard session) -- rejected by the researcher: "I don't know how and what you should do to allow you to override the controls. so I cannot really approve the detail." The failure mode named at the time: self-classification/in-session judgement calls are exactly what Claude has shown it rationalises inconsistently under pressure. Any enforcement design for THIS escalation's flag (session-freshness check on entry, session-end requirement on exit) needs to avoid recreating that shape -- i.e. avoid a check Claude itself performs and can talk itself past mid-session; lean toward something structural (harness/process-level, or at minimum a hard external artifact Claude cannot silently satisfy) instead.

TRUST FRAMING (researcher, 2026-08-31, verbatim, load-bearing for how carefully this should be built): "I cannot evaluate your permissions, or how you should be constraint. All I can say is the moment you abuse this power you are gone." The removed configmaint.propose gate is held open by trust alone; this flag is about hardening the ENTRY/EXIT boundary around that trust, not the gate itself.

FULL SOURCE MATERIAL (read this, not re-derived from precedent, per governance.past_precedent_investigation_signals_missing_config -- the flag design itself is the missing mechanism, this context is not a substitute for building it):
- iba/app/CHARTER.md sec4 (governing text)
- iba/app/GOVERNANCE.md sec69 (documents CHARTER.md sec4, cites #1342)
- .claude/commands/developer-mode.md (137 lines, full entry protocol as it stands today)
- .claude/commands/exit-developer-mode.md (18 lines, full exit protocol as it stands today -- this is the thin part the flag targets)
- .claude/hooks/stop_check_escalation_backlog.py + .claude/settings.json hooks.Stop (adjacent mechanism, same failure-mode shape, working example of a structural check already built)
- Logs/SESSION-LOG-20260831-escalation-system-bugs-and-developer-app-mode-established.md (session 1 full record, includes the rejected #1342/#1343 draft and the researcher's verbatim correction)
- Logs/SESSION-LOG-20260831-developer-mode-established-escalation-cli-fixed.md (session 2 full record, the actual build)
- iba/docs/escalation-operational-friction-review-v1-20260831.md (living record referenced by session 2)
- escalations #1342/#1343 (rejected first-draft cfg_behaviour_rule/task content, full history read this session)
- memory feedback_developer_mode_vs_app_mode_operating_model, feedback_developer_mode_trust_is_the_only_constraint (both re-read live this session, not recalled from summary)

NOT YET DECIDED (left for the Developer Mode session that picks this up):
- The actual enforcement mechanism for "prevented from running unless started in a clear new session" -- candidates not evaluated yet: a harness/settings-level check, a required fresh-process marker, something else. Needs design + test plan before build, per governance.module_utility_test_plan.
- The actual enforcement mechanism for "cannot exit unless a session-end is performed" -- what "session-end" means operationally (a session log written? a specific command? process termination?) is not defined yet.
- Whether the cfg_behaviour_rule gap (item D above) gets fixed as part of this same escalation or split out separately.

**v2** (2026-09-02T02:47:49Z, Claude) state=re-assigned next_action=ready_for_approval assigned_to=Researcher
> **comment (set this version):** Entry-boundary half built + unit-tested per your instruction this chat; live end-to-end hook-firing confirmation still needed on next real use; exit-boundary half still open.
> **resolution (set this version):** Entry-boundary half of #1380 built and unit-tested (researcher instruction, this chat: focus on 1380, "ensure ... it does not start up unless it is a new session"). Full design + test results: iba/docs/developer-mode-session-freshness-gate-v1-20260902.md.

Mechanism: 3 hook scripts under .claude/hooks/ (session_boundary_track.py = SessionStart, all matchers; track_prompt_submit.py = UserPromptSubmit, all; gate_developer_mode_entry.py = UserPromptExpansion, matcher "developer-mode"), sharing .claude/.session-boundary-state.json (gitignored). The gate fires from the harness BEFORE /developer-mode's own text is read, and blocks unless this session began via a genuine SessionStart "startup"/"clear" event AND no user prompt has occurred since -- fails closed on any uncertainty (missing state, session_id mismatch, unrecognised source). developer-mode.md updated to document this as the real mechanism (prior prose kept only as non-structural fallback).

7 unit-level scenarios tested via synthetic stdin against each script (fresh-startup allow, later-prompt block, resume-block, session_id-mismatch-block, missing-state-fail-closed) -- all passed, results in the doc. Real end-to-end hook firing (does UserPromptExpansion actually engage for a custom .claude/commands/*.md command, and is "developer-mode" the right matcher string) is NOT yet confirmed -- Claude Code's public docs do not state this explicitly. Every hook run logs its raw payload to .claude/.session-boundary-debug.jsonl specifically so the next real fresh-session /developer-mode attempt can confirm or correct it -- flagged plainly in the doc, not glossed over.

Exit-boundary half of #1380 (preventing App Mode continuing in the same conversation after /exit-developer-mode without an explicit session-end) is NOT built -- still open.

**v3** (2026-09-02T14:33:58Z, Researcher) state=completed next_action=approved assigned_to=Claude
> **comment (set this version):**  completed 

**v4** (2026-09-03T04:07:41Z, Claude) state=completed next_action=approved assigned_to=Claude
> **resolution (set this version):** Entry-boundary half of #1380 built and unit-tested (researcher instruction, this chat: focus on 1380, "ensure ... it does not start up unless it is a new session"). Full design + test results: iba/docs/1380-developer-mode-session-freshness-gate-v1-20260902.md.

Mechanism: 3 hook scripts under .claude/hooks/ (session_boundary_track.py = SessionStart, all matchers; track_prompt_submit.py = UserPromptSubmit, all; gate_developer_mode_entry.py = UserPromptExpansion, matcher "developer-mode"), sharing .claude/.session-boundary-state.json (gitignored). The gate fires from the harness BEFORE /developer-mode's own text is read, and blocks unless this session began via a genuine SessionStart "startup"/"clear" event AND no user prompt has occurred since -- fails closed on any uncertainty (missing state, session_id mismatch, unrecognised source). developer-mode.md updated to document this as the real mechanism (prior prose kept only as non-structural fallback).

7 unit-level scenarios tested via synthetic stdin against each script (fresh-startup allow, later-prompt block, resume-block, session_id-mismatch-block, missing-state-fail-closed) -- all passed, results in the doc. Real end-to-end hook firing (does UserPromptExpansion actually engage for a custom .claude/commands/*.md command, and is "developer-mode" the right matcher string) is NOT yet confirmed -- Claude Code's public docs do not state this explicitly. Every hook run logs its raw payload to .claude/.session-boundary-debug.jsonl specifically so the next real fresh-session /developer-mode attempt can confirm or correct it -- flagged plainly in the doc, not glossed over.

Exit-boundary half of #1380 (preventing App Mode continuing in the same conversation after /exit-developer-mode without an explicit session-end) is NOT built -- still open.
> **tried (set this version):** File rename per governance (escalation-tied working files carry their escalation-id prefix, researcher instruction 2026-09-03): developer-mode-session-freshness-gate-v1-20260902.md -> 1380-developer-mode-session-freshness-gate-v1-20260902.md; corrected the stale path in this item's own resolution text.
