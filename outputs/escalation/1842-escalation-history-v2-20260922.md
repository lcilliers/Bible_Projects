# Escalation deep history

## #1842 — M49 Stage 1 batch failed: chunk cap too small
type=issue source=researcher

**v1** (2026-09-22T18:24:14Z, Claude) state=raised next_action=review assigned_to=Researcher
> **short description (set this version):** M49 Stage 1 batch failed: chunk cap too small
> **comment (set this version):** Launched Stage 1 (lexical.meaning) for M49 per your instruction (preview: 76 batches, 193 verses, ~\.22 estimated). Chunk 1/76 failed: model reply truncated mid-JSON (BadModelResponse, 'Expecting value: line 1 column 1'). Confirmed via _analytics/lexical-extracts/lexical-llm-usage.csv this was genuine max_tokens truncation, not a formatting quirk: 27044 input / 40000 output (full cap) / real cost \.6811 -- input token count is ~2x a typical M67 chunk (13-16k). The batch controller correctly stopped immediately (never continued past a real error) and no further spend occurred, but its own report/CSV wrongly shows actual_cost_usd=0 for this failed chunk despite the real \.68 spend already logged in the usage CSV -- a separate reporting bug, flagging alongside this. Should chunk sizing for lexical.meaning become expected-item-count-bounded rather than distinct-strong-count-bounded, now that M0.7/M0.8.1/D7.7.1/M0.6.5/M0.6.6 are cluster-agnostic (#1832/#1834)? Not building without your call -- M49 stays stopped until decided, since chunk 1 will fail identically on an unchanged retry.
> **context (set this version):** Root cause (assessed, not fully code-traced): lexical.meaning_max_strongs_per_batch=8 bounds DISTINCT strong count per chunk, not total question/occurrence volume. M0.7/M0.8.1/D7.7.1/M0.6.5/M0.6.6 became cluster-agnostic this session (#1832/#1834) -- a chunk's strongs now pull in every occurrence of that strong anywhere in the corpus, not just within the cluster, so a 'small' 8-distinct-strong chunk can still generate far more observation content than the old cluster-scoped design assumed. BUILD.md's own note on that build explicitly flagged this as 'a separate, unconfirmed concern' at the time -- this failure confirms it live. Distinct from #1840 (position-duplicate rows) though likely compounding it.

**v2** (2026-09-22T18:30:36Z, Claude) state=re-assigned next_action=ready_for_approval assigned_to=Researcher
> **comment (set this version):** Correcting my own error and adding what was actually missing: a concrete recommendation. Two things wrong with v1: (1) a PowerShell string-escaping bug silently ate every dollar figure ($49.22 became \.22, $0.68 became \.68) -- correct figures: preview estimated $49.22 total for the run; chunk 1 alone actually cost $0.6811 real spend before failing. (2) I asked an open architecture question with nothing to approve. Concrete recommendation now: approve lowering cfg_setting lexical.meaning_max_strongs_per_batch from 8 to 3 via the standard configmaint.propose cycle (quick, reversible, no code change), then retry M49 to confirm chunk 1 no longer truncates. Checked the mechanism live: this is NOT primarily the position-duplicate issue (#1840) -- corpus-wide only 8 of 201 M49-strong occurrences share a verse with another occurrence of the same strong, too small to explain a 40000-token output. The real driver: chunking counts ANY M-role-tagged strong in a scope verse toward the "8" cap, not just M49-owned ones -- a verse can carry other clusters incidental M-strongs too. Since M0.7/M0.8.1/D7.7.1/M0.6.5/M0.6.6 went cluster-agnostic this session (#1832/#1834), EVERY one of those strongs -- not just cluster members -- now gets the full front-loaded question battery, so "8 strongs" now produces materially more output than the cap was calibrated for pre-#1832. Lowering the cap is a direct, proportionate compensation, not a guess. Longer-term correct fix (expected-item-count-bounded chunking, a real rebuild) stays a separate follow-on, not blocking this stopgap.
> **context (set this version):** Evidence: chunk 1 usage-log row (run RUN-20260922_191434_564-STAGE1-BATCH-M49, 1/76): 27044 input / 40000 output (full cap) / $0.6811. M49 itself has only 5 member strongs across 193 verses (201 verse_lexical rows, only 8 verses with a repeat occurrence) -- ruling out #1840-style duplication as the dominant driver here. Recommend: approve the max_strongs_per_batch 8->3 change; if chunk 1 still truncates at 3, that would newly implicate #1840-style duplication or something else not yet seen, and is worth re-checking then, not assumed now.
> **resolution (set this version):** Not built -- pending your approval. On approval: configmaint.propose to set lexical.meaning_max_strongs_per_batch=3 (cfg_setting), apply via the standard cycle, then re-preview and re-run M49 live to confirm chunk 1 succeeds.

**v3** (2026-09-22T18:31:56Z, Researcher) state=in-progress next_action=review assigned_to=Claude
> **comment (set this version):**  batching based on strongs is inappropriate, the units of operation is verses not strongs 
> **context (set this version):**   
