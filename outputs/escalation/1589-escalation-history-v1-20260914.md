# Escalation deep history

## #1589 — 6 of 15 note_types have zero operational definition
type=issue source=researcher

**v1** (2026-09-07T15:40:59Z, Claude) state=raised next_action=review assigned_to=Researcher
> **short description (set this version):** 6 of 15 note_types have zero operational definition
> **comment (set this version):** cfg_method_rule (step=lexical.enrich, 13 rules) genuinely defines only 5 of the 15 live note_type values: related_word, structural_pattern, recurrence_role_shift, cross_lemma_shared_gloss, verb_argument. 3 more (chain, entity_link, connective) get only an incidental one-clause gloss buried as CONTRAST text inside verb_argument's own rule ('distinct from chain -- sequence/waw-consecutive only -- entity_link -- party/pronoun identity only -- and connective -- clause-linking only'). inert is mentioned only via the completeness rule, never actually defined. The remaining 6 -- idiom, pronoun_resolution, noun_relational, noun_severity, polarity, compound_unit -- have NO rule anywhere in cfg_method_rule. Checked every written source, not just the config: the design genealogy doc (iba/docs/1527-layer1-layer2-specification-by-value-v1-20260906.md) traces each note_type's own origin but only gives a one-clause characterization for these 6 too ('idiom sense-selection', 'pronoun/entity resolution', 'negation-as-a-structured-finding beyond the mechanical is_negator flag', etc.) -- never a real criterion for WHEN to apply it or what a valid finding looks like. compound_unit's only real definition anywhere is in a markdown guide doc (lexical-enrich-payload-guide-v1-20260905.md), never migrated into cfg_method_rule -- the LLM brief deliberately does not read that doc (built from config only, to avoid the exact doc-vs-config drift #1548 found), so even that gets lost. Net effect: the LLM is currently asked to judge 40% of the note_type vocabulary with nothing but the bare name to go on -- not 'doing more than the catalogue asks' (the opposite risk: filling an unspecified gap with its own invented interpretation, inconsistently across calls). Not writing rules for these myself -- what each of these 6 should actually require is a real content decision, same as every other note_type's rule was your own call.
> **context (set this version):** Found live answering researcher's own request to map the LLM brief against the catalogue it answers. Full evidence in comment.
