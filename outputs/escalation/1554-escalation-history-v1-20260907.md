# Escalation deep history

## #1554 — Add lexical.run step (cluster-driven front door)
type=issue source=configmaint

**v1** (2026-09-07T11:29:56Z, Claude) state=raised next_action=review assigned_to=Researcher
> **short description (set this version):** Add lexical.run step (cluster-driven front door)
> **comment (set this version):** coherence-checked against the live cfg_* schema — awaiting researcher decision via `Escalation.ps1 -Action Update -Id <id> -NextAction ready_for_approval` then `-NextAction approved` (or reject/revise), then re-run this exact Config-Maintenance.ps1 command with -RunId to apply
> **context (set this version):** {"table": "cfg_step", "op": "insert", "where": {}, "set": {"work_package": "verse-lexical", "ordinal": 5, "step": "lexical.run", "handler": "iba.app.handlers.lexical:run", "scope": "verse", "kind": "operations", "inactive": 0, "does": "The real front door for Window 1 (escalation #1549 rework, 2026-09-07 -- researcher: 'it is fundamentally built on books and passages which contradicts the analytic operation for clusters and groups of strongs'). Exactly one selector (-ClusterCode via cluster_strong, -StrongList direct, -Word via word_strong, or -VerseList a known OSIS list) resolves to a strong-list then a verse-list via span.strong_variant (exact-token match, NOT strong_verse -- that table undercounts, verified live). -Mode picks the work: Layer1AndLayer2, Layer1Only, or Layer2Only -- verse-list resolution happens once regardless of mode; Layer 2 always sources its scope from Layer 1's own resolved ids, never re-resolves. Layer 1's write (build_for_verse_ids) is identity-stable -- safe no-op on unchanged verses, never orphans a live Layer 2 note. lexical.run_max_verses caps the resolved scope before any work starts; passage.max_verses (existing) still separately caps the Layer 2 write block, unchanged from lexical.enrich's own convention."}, "full_message": "Escalation #1549 rework: researcher confirmed the old -Book+one-contiguous-range input model is stale for cluster/strong-group-driven analysis. New step lexical.run replaces it as the sole entry point for both Layer 1 and Layer 2, with 3 modes (Layer1AndLayer2/Layer1Only/Layer2Only). Code: handlers/lexical.py:run + new lib/lexicalscope.py (selector resolution). Selector-resolution tested live read-only: cluster M01 -> 100 strongs -> 933 verses; word 'Cursing' -> 29 strongs -> 1547 verses; explicit strong-list and verse-ref-list paths both correct; a bare/unsuffixed strong code (a known project gotcha) is now caught with a concrete suggestion rather than silently returning 0 verses. Reuses existing, already-tested engine functions (lib/lexical.py:build_for_verse_ids, lib/lexicalenrich.py:enrich_passage/check_completeness) unchanged -- this is a new resolution+orchestration layer in front of them, not new write logic. lexical.build/lexical.enrich (old signature) left in place, not deleted -- their fate is a separate decision. lexical.notes (the read-only payload-request generator from the earlier #1549 pass) is NOT being re-proposed here -- its role under this architecture (assembling the analytical request at the point Layer 2 is invoked) is still open, per the researcher's own 'I don't know what lexical.notes is doing' -- deferred, not built into this step."}
> **tried (set this version):** coherence-checked against the live cfg_* schema — awaiting researcher decision via `Escalation.ps1 -Action Update -Id <id> -NextAction ready_for_approval` then `-NextAction approved` (or reject/revise), then re-run this exact Config-Maintenance.ps1 command with -RunId to apply

**v2** (2026-09-07T11:31:21Z, Claude) state=re-assigned next_action=ready_for_approval assigned_to=Researcher
> **resolution (set this version):** Part of the lexical.run batch (escalation #1549 rework) -- see #1554 for the full design/test summary. Approve to apply.

**v3** (2026-09-07T11:35:36Z, Researcher) state=re-assigned next_action=approved assigned_to=Claude
> **comment (set this version):**  proceed 

**v4** (2026-09-07T11:48:01Z, Claude) state=re-assigned next_action=approved assigned_to=Claude
> **comment (set this version):** Code updated since this was filed (not yet applied, no re-approval needed for this correction): the -StrongList validation guard I originally wrote checked against the strong catalogue table, which itself undercounts (409 real span-attested codes have no strong row yet). Now validates against actual span.strong_variant presence instead -- the true source of truth. Also added: cluster_strong/word_strong-resolved strongs with zero span occurrence now surface as a named WARNING in the result rather than silently narrowing the verse-set. See #1560 for the concrete defect this surfaced (3 cluster_strong rows with a bare, unmatchable code).
