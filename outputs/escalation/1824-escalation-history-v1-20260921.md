# Escalation deep history

## #1824 — Same-observation dedup never fires: 0/378 pairs merged
type=task source=Claude

**v1** (2026-09-21T11:29:45Z, Claude) state=raised next_action=review assigned_to=Researcher
> **short description (set this version):** Same-observation dedup never fires: 0/378 pairs merged
> **comment (set this version):** Researcher challenge, this chat turn, verbatim: 'I thought the rule is that the same observation should never be repeated, if another verse or instance in the same verse refers to the same observation then it creates another node, not another observation. also a word observation can never just be a repeat of the lexical meaning, it need to state what is the meaning in the context. I doubt if these rules have ever been applied.' Checked precisely, with real similarity-score computation, not impression -- both rules are real, pre-existing, and both are being systematically violated.
> **context (set this version):** RULE 1 (same observation -> node, not new observation) is real, from escalation #1692's ib_node finalization doc (2026-09-12), researcher's own words verbatim: 'if ib_node refers to the same observation... then a row is created, for each item referencing the same observation.' recordingpass.py implements this via a difflib.SequenceMatcher text-similarity check (threshold 0.85) -- above threshold, align into the SAME observation with a new node; below, a genuinely new observation. Computed the ACTUAL similarity scores for all 28 M0.1.1 observations recorded for H3034 (378 pairwise comparisons): maximum similarity ever seen was 0.51, mean 0.19, ZERO pairs reached 0.85. The mechanism has never once merged these 28 restatements of the same underlying word-level fact into a shared observation -- not a near-miss, a systemic failure to trigger. Root cause: independently-generated LLM paraphrases of the same claim vary in wording/structure every time; character-level text similarity between paraphrases is intrinsically low (0.1-0.5 range) even when the semantic content is identical -- the 0.85 threshold is calibrated for near-verbatim repeats (typos, trivial edits), not paraphrase-level semantic equivalence, so it essentially never fires against real LLM output. RULE 2 (meaning in context, not lexical repetition) is also real and pre-existing -- WA-verse-reading-technique-v4-2026-08-05.md, the foundational method doc, verbatim: 'Verse reading is not pattern recognition, it is about the meaning in context of each element.' Rereading the 8 previously-sampled H3034/M0.1.1 answers against this standard: 6 of 8 restate the word's generic root meaning/essential nature with no real verse-specific content; only 2 (2Chr.7.3's corporate context, Ps.76.10's 'arising from human wrath' angle) derive anything from that specific verse. Not retroactively fixed by the #1820 skip-check applied this same session -- that stops NEW redundant LLM calls going forward (a strong now gets its word-level battery answered at most once, ever), but does not clean the existing duplicate rows already written (H3034/M0.1.1 alone has 28, and Finding 13 already found ~30 observations per question per high-frequency strong corpus-wide, suggesting this scale is systemic, not isolated to one strong), and does not fix the underlying similarity-threshold bug itself, which would still affect any OTHER stage or question type where the same/broaden/new mechanism is relied on to consolidate genuinely-restated content (M0.6.5/M0.6.6's progressive relational chain, Stage 3/4's own same/broaden/new usage) -- not checked here, scope not yet established.

**v2** (2026-09-21T15:00:54Z, Claude) state=in-progress next_action=review assigned_to=Researcher
> **comment (set this version):** Researcher context, this chat turn, verbatim: 'and we discovered this some days ago. that is why I said that LLM simply need to do the interpretation without trying to figure out if this is a new or existing observation. LLM output is filed in Json. then as a separate operation from LLM, CC will check if this observation already exist. Where it failed in the clusters already analysed is because LLM output was not generic nonsense, and not concide and specific to the verse, so it was impossible to make any differentiation visible.' Confirms recordingpass.py's own #1693 architecture (LLM never decides new-vs-existing, code does it as a separate operation) is already correct -- not the defect. The actual root cause of this escalation's own 0/378 merge-rate finding: obs_text is generic, padded prose that varies in sentence structure every time, even when restating the same fact -- confirmed directly against this escalation's own H3034 sample text.
> **context (set this version):** This means the fix is a prompt/rule requirement, not a similarity-algorithm redesign: require obs_text to be concise and tied to the specific verse's actual content, not a free-standing dictionary-style restatement. This also resolves this escalation's Rule 2 violation (meaning-in-context) with the SAME single fix -- two verses with the same underlying pattern will produce similar terse text (dedup catches it correctly); two verses with genuinely different content will naturally diverge (correctly staying separate). Searched cfg_method_rule and the docs for an existing written version of this rule -- none found live; treating this as the researcher's own recollection from an earlier (unwritten-up) conversation, not something missed. Not yet applied -- asked whether to bake this into the new verse-context questions only (where it's load-bearing for the per-occurrence variant-consolidation design being worked on #1806), or also retrofit onto the existing M0.1/M0.5/M0.6.5/M0.6.6/D7.7.1 prompt instructions now. Not yet answered.

**v3** (2026-09-21T17:40:54Z, Researcher) state=in-progress next_action=revise assigned_to=Claude
> **comment (set this version):** similarity is not matching sentences. Similarly to matching keys.  For instance to check for matching word meaning: match a) surface b) meaning extraction keywords c) morph
