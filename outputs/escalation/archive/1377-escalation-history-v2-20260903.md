# Escalation deep history

## #1377 — Vocabulary/glossary definition
type=task source=researcher

**v1** (2026-09-01T10:44:48Z, Researcher) state=raised next_action=review assigned_to=Researcher
> **short description (set this version):** Vocabulary/glossary definition
> **comment (set this version):** Seeded from the now-closed #1007 (catalogue exploration) and the open #1376 (characteristic-model reconciliation) per researcher instruction (2026-09-01). Researcher's own framing: not sure whether this intersects with cfg_enum -- initially assumed it would, but there is a case for it to be separate. This escalation starts by pulling together a comprehensive candidate list of project vocabulary/glossary terms that need a settled definition, drawn from both source threads -- not yet defining each one, and not yet deciding the cfg_enum question.
> **context (set this version):** Workflow/Catalogue/vocabulary-glossary-seed-v1-20260901.md -- seed list: 14 terms already given a working definition (carried from #1376's own glossary in characteristic-tables-cross-db-inventory-v2-20260901.md), plus 15 candidate terms with no settled definition yet (scope, Scope focus buckets, source, tier/T-code, span, surface, term, word, content/function, resolved/unregistered/content_resolved, inner-being-related, Layer A/Layer B, Phase 1/Phase 2, verse-context [ambiguous across two databases], Model A/B/C/D).

**v2** (2026-09-01T10:51:04Z, Researcher) state=in-progress next_action=review assigned_to=Researcher
> **comment (set this version):** Second pass per researcher instruction (2026-09-01): read the full current programme prose (wa-programme-prose-extract-20260827.md) and scanned all 1,799 cfg_column rows across both databases for missed vocabulary and same-concept/different-name collisions. Found several concrete, evidenced ones, not just wording overlaps: (1) the soft-delete marker has 4 different spellings across the two DBs (delete_flagged / deleted / delete_flag / retired deprecated), one of which (wa_obs_question_catalogue.deleted) actively disagrees with that same table's status column, matching a gap the prose itself independently names; (2) a bare 'cluster' column holds an M-code in 3 tables but a C-code in a 4th (wa_dim_review_cluster_log) -- the exact administrative-vs-analytical collision the prose says these two things are supposed to be kept apart; (3) 'scope' as a column name has 4 unrelated live meanings, not the 2 this thread mostly discussed; (4) several prose-level same-word-different-thing collisions with no DB column involved at all -- cluster (C-code vs M-code), characteristic (Ch.1 concept vs Model A table vs Model B ib_characteristic), dimension (the prose's own Ch.4 section heading vs its body, which says the live mechanism is actually the cluster/characteristic model), inner being (whole-programme subject vs HIB vs inner-being characteristic), anchor (passage-level vs hib.first_verse_id, which the prose says plays an analogous role but isn't named anchor), registry (iba.db's thin live word_registry vs bible_research.db's heavier legacy word_registry CLAUDE.md still documents), and passage (iba.db's passage/verse_passage vs the still-cited 2026-07-02 verse-first method's verse.passage_id).
> **context (set this version):** Workflow/Catalogue/vocabulary-glossary-seed-v2-20260901.md -- supersedes v1 (archived to Workflow/Catalogue/archive/). v2 Part 3 adds the prose-derived collision list, Part 4 adds the confirmed-live schema findings (delete-marker family, the cluster column split, the scope column split) with the exact table.column and cfg_column.use evidence for each.

**v3** (2026-09-01T11:00:53Z, Researcher) state=in-progress next_action=review assigned_to=Researcher
> **comment (set this version):** Researcher decision, verbatim: 'I will work through this list. From I can see it all belongs in enums.' Resolves the open cfg_enum-vs-separate-mechanism question from v1/v2 -- this vocabulary work goes into cfg_enum, not a separate mechanism. Researcher will work through the seed list (Workflow/Catalogue/vocabulary-glossary-seed-v2-20260901.md) themselves; staying in-progress, not closed, since that work is ongoing.
> **resolution (set this version):** Decided: cfg_enum is the mechanism, not a separate one. Escalation stays open/in-progress while the researcher works through the seed list; the actual per-term cfg_enum entries are that follow-on work.

**v4** (2026-09-01T11:03:44Z, Researcher) state=in-progress next_action=review assigned_to=Researcher
> **comment (set this version):** Refinement on v3's decision, verbatim: 'I see enum in its strict definition. that is fine. it is likely we need then a new cfg for it. but not now.' -- cfg_enum stays strict (column-value vocabularies only), covers the delete-marker/cluster/scope-type findings. The prose-level concept collisions (characteristic, dimension, HIB, Phase 1/2, inner being, etc.) likely need a new, separate cfg_* mechanism -- not designed or built now, deferred. Staying active/in-progress, no further work this session.

**v5** (2026-09-02T02:28:41Z, Claude) state=in-progress next_action=review assigned_to=Researcher
> **comment (set this version):** Completeness check ahead of Developer Mode build (2026-09-02): the seed material itself (vocabulary-glossary-seed-v2-20260901.md) is thorough and live-evidenced -- Part 1/2 candidate list (29 terms), Part 3 prose-derived collisions (7 same-word/different-thing pairs with exact citations), Part 4 confirmed schema findings (4-way delete-marker split, cluster column M-code/C-code split, 4-way scope split). But one real gap found just now, not previously checked: v4 said 'it is likely we need then a new cfg for it' for the prose-level concept glossary (characteristic, dimension, HIB, inner being, Phase 1/2, etc.) -- a live query just now found a cfg_prose_concept table ALREADY EXISTS (2 rows: verse_primacy, inner_being_definition; columns concept_key/chapter/section_hint/description/source/added_at), built 2026-08-18 (escalation #714) for exactly this purpose per CLAUDE.md governance.prose_canonical_authority. Nobody checked for this before v4's 'likely need a new cfg' judgement. The Developer Mode session should evaluate extending/populating this existing table before designing a new one -- may turn out to need only column additions (if any) plus data capture, not a new table at all. Also unresolved: the seed list's ~40 candidate terms are not yet individually sorted into 'cfg_enum' (column-value vocab) vs 'cfg_prose_concept or new mechanism' (prose-level concept) buckets -- only ~6 example terms were named in v4, not the full list. Both points left for the build session to resolve, not decided here.

**v6** (2026-09-02T03:13:16Z, Claude) state=re-assigned next_action=ready_for_approval assigned_to=Researcher
> **comment (set this version):** Design proposal filed for the vocabulary mechanism per your instruction this chat (Developer Mode, full plan-design-propose-testplan-buildplan-build cycle). Reviewed live schema (cfg_enum, cfg_prose_concept, prose_section) before proposing, not from memory. See the linked doc for the full architecture, worked examples, and 4 open points needing your call. Stopped here for approval, per the cycle you specified.
> **context (set this version):** Design proposal filed (DESIGN/PROPOSE stage of the plan-design-propose-testplan-buildplan-build cycle researcher asked this item to follow): Workflow/Catalogue/vocabulary-mechanism-design-v1-20260902.md.

Grounded against live schema, not assumed: cfg_enum confirmed bare (name/value/ordinal/inactive, no room for a definition -- matches v4's own read); cfg_prose_concept confirmed thin (2 rows, no status/aliases/supersedes, duplicates definition text inline); prose_section confirmed to already carry version/status/approved_by/approved_at/metadata_json plus existing link-table precedent (prose_section_verse_link etc.) and FTS5 search -- i.e. researcher's own instinct that prose is the right home for the definition-with-lifecycle is backed by real schema, not just plausible.

Proposed architecture: Tier 1 = cfg_enum unchanged (strict column-value vocab, per the v4 decision, no schema change). Tier 2 = new cfg_vocabulary_index (iba.db, supersedes cfg_prose_concept -- one row per DISTINCT SENSE of a term, not per word, because cluster/characteristic/scope etc. are exactly the same-word-different-things case the whole mechanism exists to catch) + a new prose book (book_label=Vocabulary, reusing prose_section/prose_section_type, no new content-storage or versioning mechanism needed) + a new cfg_vocabulary_usage link table (generic junction recording which cfg_*/table/column depends on which term_key -- this IS the conflicts/fallout mechanism: query it before approving a retraction/redefinition, no separate detection tooling to build).

Worked against 4 real hard cases from the seed (4-way delete-marker spelling, cluster mcode/ccode, characteristic's 3 grains, scope's 4 meanings) to sanity-check the schema, not left abstract.

4 open points left for researcher decision, not assumed: book name (Vocabulary vs Glossary), migrating cfg_prose_concept's 2 rows, whether the full ~40-term seed sort happens in this build or as tracked follow-on, and whether the proposed status enum vocabulary is right.

NOT built. Stopped at design per decision-points-are-terminal-not-inline -- awaiting approval/correction before test plan -> build plan -> build.

**v7** (2026-09-02T03:31:21Z, Claude) state=re-assigned next_action=ready_for_approval assigned_to=Researcher
> **comment (set this version):** v1 design corrected per your feedback this chat -- properly mapped the prose engine this time (found it already does everything the index/usage tables were for, and found the #918 precedent directly against what v1 proposed). Renamed vocabulary to glossary throughout. See linked doc for the full self-correction and the no-new-tables architecture. Stopped at design again for your approval.
> **context (set this version):** v1 design REJECTED by researcher same-day, correctly: proposed cfg_vocabulary_index + cfg_vocabulary_usage without properly mapping what prose already does -- "just building more tables around it." Old file archived: Workflow/Catalogue/archive/vocabulary-mechanism-design-v1-20260902.md.

v2 (renamed Glossary throughout, per instruction) filed: Workflow/Catalogue/glossary-mechanism-design-v1-20260902.md. Owns 4 concrete misses in v1, checked and confirmed live: (1) cited prose_section_dimension_link/prose_section_finding_link as cross-ref precedent -- their own cfg_column.use says "Never populated", dead scaffolding; (2) claimed a supersedes_id column exists on prose_section -- it does not (checked full column list live); that name is a patch-file field (prosestore.py run_import_chapter), not a DB column -- prose versioning is patch-based, not an extra live column; (3) never read cfg_setting governance.prose_canonical_authority before designing -- it already states the intended pattern (cfg_prose_concept points AT the prose section, does not restate the definition); (4) missed that the same setting cites escalation #918, where a sibling table (cfg_prose_chapter) was REMOVED for exactly the mistake v1 repeated -- workflow data about prose content wrapped in cfg_*, forcing ordinary content edits through the full config-approval cycle.

Corrected architecture: NO new tables, NO new columns. Glossary = a new prose book (book_label='Glossary', ordinary data insert + cfg_prose.book_stage_map/book_output_dir additions). One prose_section per term-SENSE (heading = term+sense, body = narrative definition/disambiguation/history). Aliases go in metadata_json (precedented -- other rows already carry structured extras there). Lifecycle reuses existing prose_section_status enum (draft/in_review/approved/archived) and the existing edit/patch cycle for versioning -- no new mechanism. Cross-referencing = direct citation of the specific prose_section written into the CITING cfg_column.use/cfg_setting.use text itself (the pattern governance.prose_canonical_authority already describes), not a link table. cfg_prose_concept retired, its 2 rows migrated in. Conflicts/fallout = a read-only report scanning live cfg_column.use/cfg_setting.use/prose bodies for citations (reusing the same scan method #1377 itself already used twice), not a separately-maintained usage table that could drift out of sync.

4 open points left for researcher decision: lifecycle enum granularity, cfg_prose_concept migration, full 40-term sort now vs follow-on, fallout report's exact matching strategy. NOT built -- stopped at design again per decision-points-are-terminal-not-inline, awaiting approval.

**v8** (2026-09-02T03:50:14Z, Claude) state=re-assigned next_action=ready_for_approval assigned_to=Researcher
> **comment (set this version):** 7.1-7.4 done per your instructions this chat. Fallout scan run and fully developed (7.4) before drafting entry wording (7.3), per your prediction -- it changed real content, not just confirmed the design. JSON review file ready, nothing written to any table yet. wa_vocab_set/wa_vocab_member (7.2) found and flagged as a live open call, not decided unilaterally.
> **context (set this version):** 7.1-7.4 addressed. 7.1: lifecycle enum (draft/in_review/approved/archived) confirmed fine, no change. 7.2: ran a full fallout scan (script filed: scripts/discovery/_check_glossary_fallout-20260902.py, raw output: Workflow/Catalogue/glossary-fallout-raw-20260902.json, all ~40 seed terms) which doubled as the search for other configs holding glossary-shaped definitions -- found wa_vocab_set/wa_vocab_member (bible_research.db, 8 vocabularies/39 members, full unused supersession mechanism) but its content is entirely the RETIRED Dimension Review stage; disposition (migrate as archived entries vs leave as untouched legacy) left as an open call, not decided. 7.3: draft entries filed as JSON for review BEFORE any table write, per your instruction: Workflow/Catalogue/glossary-draft-entries-v1-20260902.json (21 entries) -- nothing written to prose_section or any cfg_* table yet. 7.4: fully developed, not deferred -- full writeup: Workflow/Catalogue/glossary-fallout-findings-v1-20260902.md.

Confirmed your prediction directly: fallout findings changed 7.3's wording in real, not cosmetic, ways. The 4-way delete-marker spelling was wrong as ONE sense in the prior design -- fallout proved it's at least THREE (plain soft-delete / iba.db's version-aware supersession-on-write / an unrelated folder-existence enum value), now three separate entries. Also surfaced and drafted: a related-but-distinct cfg_table/cfg_column/cfg_enum.inactive convention never named in the seed; two more characteristic grains (ib_characteristic_legacy, verse_span_index's span-level free text) beyond the seed's three; operation as its own same-word-different-things case (iba.db's structured debate entity vs bible_research.db's free-text description) never flagged before; family splitting into a thematic sense and an unrelated linguistic-root sense. Also recorded a pure methodology artefact worth keeping in mind: case-insensitive matching conflates the FLAG cluster code with 50+ generic "flag" mentions -- handled by titling that entry's heading to explicitly rule out the confusion, not by merging it into the noise.

NOT drafted, deliberately, pending your steer (open point 3, now with real hit-volume evidence behind it): the high-volume generic-English terms (term/word/span/surface/content/function/status/resolution/source/registry/passage/anchor/dimension/model), each 20-150+ raw hits -- genuinely pervasive ordinary words, not bounded collisions; cataloguing every hit would not produce a usable entry.

Still NOT built: no prose_section rows, no cfg_prose.book_stage_map/book_output_dir additions, no cfg_prose_concept migration. Awaiting your review of the JSON before any of that happens.

**v9** (2026-09-02T04:07:34Z, Claude) state=re-assigned next_action=ready_for_approval assigned_to=Researcher
> **comment (set this version):** 7.2 done (full 39-value migration drafted). 7.3 corrected -- the scan approach was too narrow exactly as you said, proved it by reading one document and finding ~15 terms invisible to the scan. Set up a living discovery register since this is genuinely a multi-session scale (510 real docs), not something one pass finishes -- see the register for exactly what's covered and what isn't. Waiting on your steer on pacing before continuing the sweep.
> **context (set this version):** 7.2 DONE: full migration of all 8 wa_vocab_set groups / all 39 wa_vocab_member values drafted as 8 archived Glossary entries (glossary-draft-entries-v1-20260902.json, wa_vocab_dimension_label through wa_vocab_stage2c_structural_relationship). Proposal: once approved and written to prose_section, mark wa_vocab_set.inactive=1 and wa_vocab_member... via cfg_table.inactive=1 on both table registrations (config_registration_inactive_flag convention). Still a proposal, not applied.

7.3 CORRECTED per your instruction this chat: the fallout-scan approach (cfg_column/cfg_setting text matching) is structurally blind to any term that exists only in narrative method prose with no DB column -- proven live: read ONE document (Workflow/Instructions/wa-verse-analysis-method-v1-20260702.md, 164 lines) end to end and it alone surfaced ~15 new terms the scan could never have found (grain, the Gate-1/Gate-2 span-admission scheme, a second collision on "role" against verse_span_index.role, cohabitation's status contested between two authoritative sources, segmentation-unit types, oracle, an owner-term/OWNER-registry collision) -- now drafted into glossary-draft-entries-v1-20260902.json (36 entries total: 8 archived migration + 28 draft, up from the original 21).

Set up Workflow/Catalogue/glossary-discovery-register-v1-20260902.md as the living register for this: states the scale honestly (510 real .md files project-wide across Instructions/methodology/Catalogue/Programme/reference/registry/Global_rules/Tiers/Clusters/Sciences/Chat_responses, current+archive), records what's actually been read (1 of 25 current Instructions docs so far, everything else 0), and names the next document. This is NOT a one-sitting task -- flagged plainly rather than pretending a shallow full-sweep would meet the bar you're asking for. Not deciding pacing/prioritisation (full continuous campaign vs current-docs-first vs highest-density-first) unilaterally -- that's your call.

Still NOT built: no prose_section rows, no cfg_prose registration, no wa_vocab_set/member table-inactive flip, no cfg_prose_concept migration. All still JSON-review-file stage per your 7.3 instruction.

**v10** (2026-09-02T04:16:37Z, Claude) state=re-assigned next_action=ready_for_approval assigned_to=Researcher
> **comment (set this version):** Continuous-validation addendum filed per your instruction this chat -- read/write-time checking instead of a one-off sweep. Split into a mechanical surfacing hook (can't forget) + a judgement rule (only Claude can tell if usage actually differs). 4 open questions need your call before this is buildable alongside the base design.
> **context (set this version):** Continuous-validation design filed per your instruction this chat (a config/rule triggering glossary validation at read/write time, instead of relying on a one-time file sweep): Workflow/Catalogue/glossary-continuous-validation-design-v1-20260902.md. Addendum to the still-unapproved base design, not a replacement.

Split proposed: a mechanical PostToolUse hook (Read/Edit/Write, scoped to narrative/doc paths only -- Workflow/docs/iba-docs/root governance .md files, not every file touch project-wide) surfaces which live Glossary terms appear in a touched file (cheap, can't be forgotten, matches the same pattern already built for the Developer Mode session gate and the escalation-backlog Stop hook). A new cfg_behaviour_rule governs what Claude does with what's surfaced -- confirm usage matches the cited sense (silent success, the common case) or route a real difference/gap through the existing propose->JSON-review->approve->write cycle, never patched silently.

Explicitly NOT a full substitute for the discovery register -- a document never reopened stays unchecked under this mechanism alone; flagged as a real open trade-off, not decided.

4 open questions filed, not assumed: hook path scope, which cfg_behaviour_rule class this belongs under (not 'development' -- this fires in standard sessions doing ordinary work too), the standing per-touch cost being accepted, and whether the register still runs as a slower deliberate pass for archive material this mechanism will never touch on its own.

NOT built -- this and the base glossary-mechanism-design need approving together (one mechanism, two halves: what the Glossary is, how it stays current) before any build-plan work starts.

**v11** (2026-09-02T04:49:10Z, Claude) state=re-assigned next_action=ready_for_approval assigned_to=Researcher
> **comment (set this version):** Addendum corrected in place per your second round of feedback -- scope widened to code/query/DB-exploration (the real chronic problem, per your own worked example), and the discovery behaviour fixed to always surface in chat first with no autonomous glossary edit at any size. Ready for your review alongside the base design.
> **context (set this version):** Continuous-validation addendum REVISED in place (same file, corrected not superseded -- still pre-approval) per your second correction this chat. Two real mistakes fixed:

(1) Scope was too narrow -- restricted to doc reads/writes. Corrected: the primary mechanism is now a standing discipline for code/script/query/DB-exploration work -- before writing any extraction/query that selects by a term with more than one live candidate meaning (your own test case: "extract all words of meaning X" -- lemma vs span.surface vs grain are three different, all-defensible answers), state explicitly in chat which one is intended BEFORE writing the code. The doc-touch hook survives only as a narrower, secondary aid for the prose case specifically -- not extended to code, since that would drown in false positives on ordinary words.

(2) Discovery behaviour was wrong -- first draft allowed a same-turn autonomous glossary edit for "small, unambiguous" cases. Corrected per your explicit instruction: first reaction on any non-match/ambiguity is ALWAYS a plain-chat surface to you, no exception for size. Expected common outcome: a more precise term gets picked for the task at hand and work continues -- glossary itself usually does NOT change. Only a genuinely new/missing concept becomes a glossary content change, and that goes through the existing prose CRUD cycle (export/edit/import) you already have -- nothing new built for it. No step is independent of you -- not the ambiguity call, not the term choice, not any glossary change.

3 open questions remain (where the standing discipline gets recorded so it's actually followed -- cfg_behaviour_rule vs interaction-preferences.md vs both; doc-hook path scope; whether the discovery register still runs for cold material this mechanism never touches). NOT built.

**v12** (2026-09-02T04:53:23Z, Claude) state=re-assigned next_action=ready_for_approval assigned_to=Researcher
> **comment (set this version):** Register retired, folded into the JSON's own _meta -- one artifact now. Confirmed all terms, including the contentious ones, are already entries in that same file, with the tension stated inline rather than parked elsewhere.
> **context (set this version):** Corrected per your instruction: the standalone glossary-discovery-register-v1-20260902.md is retired -- archived to Workflow/Catalogue/archive/. It never held term content (only scan-progress tracking: which files read, which not), but it was still a second artifact for the same effort, the same shape of mistake as wa_vocab_set/cfg_prose_concept. Folded its content into glossary-draft-entries-v1-20260902.json's own _meta (discovery_coverage block) -- one file now, not two.

Confirmed directly, with the existing entries as evidence, not just asserted: every term found so far, including the contentious/unresolved ones, is already in the JSON entries array -- there is no separate holding place for those either. cohabitation, role.span_classification, and owner_term.batch_processing_sense entries all state their own tension explicitly in body text (e.g. cohabitation: "THIS IS A REAL, UNRESOLVED TENSION between two authoritative sources... needs a researcher call, not a silent pick") rather than being deferred or parked elsewhere.

**v13** (2026-09-02T05:01:29Z, Claude) state=re-assigned next_action=ready_for_approval assigned_to=Researcher
> **comment (set this version):** Restructured the JSON into a full schema-mirrored write plan per your instruction -- every column, every row, explicit reasons for anything not written. Doing this properly surfaced a real error in the base design: adding the Glossary book is NOT schema-change-free as claimed -- book_label is CHECK-constrained and needs a real migration. Corrected in the design doc. This forces a genuine choice (migrate the CHECK vs file under an existing book) that I have not made for you.
> **context (set this version):** glossary-draft-entries-v1-20260902.json restructured per your instruction: replaced the flat 'entries' array with a full schema-mirrored write plan -- target_schemas (every column of bible_research.prose_section_type, bible_research.prose_section, iba.cfg_table, with type/notnull/PK) and proposed_writes (the actual row-by-row values: 1 new prose_section_type row, 36 prose_section rows, 2 cfg_table inactive-flip rows). Every column is present for every row -- written columns carry the real value, unwritten columns carry an explicit reason (e.g. registry_id: "NOT WRITTEN -- glossary entries are not word_registry-scoped"), not silently omitted.

REAL ERROR FOUND AND CORRECTED building this: glossary-mechanism-design-v1's claim that adding the Glossary book is "an ordinary content addition, not a schema change" is FALSE. prose_section_type.book_label carries a live CHECK constraint (CHECK (book_label IS NULL OR book_label IN ('Programme','Detail design','Findings','Essays'))) -- 'Glossary' is not a permitted value. SQLite has no ALTER for a CHECK constraint; adding it requires rebuilding prose_section_type (new table with the updated CHECK, copy all 1,039 existing rows, drop old, rename) -- a real schema migration on the live schema this design was built specifically to avoid touching. Corrected inline in the base design doc with a dated note, not silently fixed.

This forces a real choice, not decided here: migrate the CHECK constraint to add 'Glossary' as a permitted book_label, OR file Glossary entries under an existing book_label (Programme/Detail design/etc.) with a distinguishing section_label instead, avoiding the migration entirely. The write plan's prose_section_type row is marked BLOCKED on this choice.

Also surfaced along the way: source_stage is separately CHECK-constrained (no 'glossary' value exists either) -- proposed 'programme' as the closest existing fit specifically to avoid a SECOND migration on top of the book_label one, not because it's an ideal semantic fit. Flagged in the write plan's column notes, not assumed silently.

**v14** (2026-09-02T08:44:08Z, Claude) state=re-assigned next_action=ready_for_approval assigned_to=Researcher
> **comment (set this version):** Applied your review comments on the prose_section_type block -- book_label/chapter_no/lifecycle_tag/label-grouping/description all resolved and built live, JSON updated to match. Found and fixed a real live bug from the PRIOR migration along the way (broken FTS-sync triggers, confirmed no data loss). Full record in BUILD.md #219.
> **context (set this version):** Reviewed glossary-draft-entries-v1-20260902.json (bible_research.prose_section_type block). Resolved several open points in one pass, built and tested live -- full detail iba/app/BUILD.md #219.

RESOLVED per researcher review comments:
- book_label = "Word Index and Glossary" (not "Glossary" alone -- will also carry a future word concordance, not built now). CHECK migrated live to permit it (table rebuild, 108 rows preserved+verified). cfg_enum group prose_section_type_book_label gained a 5th row. cfg_prose.book_stage_map/.book_output_dir both gained an entry (["programme"] / "Workflow/Glossary" -- first-cut choices, flagged, not a confirmed design decision).
- chapter_no = 1 ("I do not see different chapters").
- lifecycle_tag = "Data Takeon" (the free-text value #1382's fix made possible).
- label/grouping: split into TWO prose_section_type rows per "you may decide to group further" -- glossary_programme ("Programme glossary", 33 settled entries) and glossary_contentious ("Contentious glossary", the 3 entries whose body already states an explicit unresolved tension: cohabitation, role.span_classification, owner_term.batch_processing_sense). Not split further than that -- two groups matches the evidence actually in hand.
- section_label = "Glossary" on both (the single Chapter, per #1381's model); label carries the Programme/Contentious distinction as the Section tier underneath.
- description: researcher's exact text applied to "Programme glossary". "Contentious glossary" got Claude's own parallel wording, explicitly flagged in the JSON as not the researcher's literal text.

REAL BUG FOUND AND FIXED IN THE SAME PASS, not by inspection -- by hitting it: widening the book_label CHECK failed on first attempt with a trigger error. Investigating why (not just retrying) found that escalation #1382's own migration (same session) had left prose_section's two FTS-sync triggers (prose_section_ai/au) referencing a table name ("prose_section_type_old") that no longer existed -- SQLite's ALTER TABLE RENAME auto-rewrites OTHER schema objects that textually reference the renamed table, and #1382's rename-away-then-recreate sequence silently retargeted both triggers at the temporary name. CONFIRMED LIVE: every INSERT/UPDATE on prose_section had been broken since #1382 committed, until caught here. Checked for actual damage: prose_section (949) and prose_section_fts (949) row counts matched exactly -- nothing had attempted a write in the broken window, no data lost, but a live defect sat undetected. Fixed immediately, verified with a real UPDATE.

A second attempt at THIS migration (trying the safer-looking create-new/drop-old/rename-into-place order specifically to avoid the same footgun) hit a DIFFERENT failure at the same root cause -- ALTER TABLE RENAME re-validates dependent triggers mid-operation and fails if the target name is transiently absent. Root-fixed, not patched again: drop the two triggers BEFORE touching the table at all, rebuild with no live trigger to get confused, recreate both fresh against the final name. Verified with a real insert->update->delete round trip confirming FTS actually stays in sync at each step, not just that statements execute.

**v15** (2026-09-02T08:57:34Z, Claude) state=re-assigned next_action=ready_for_approval assigned_to=Researcher
> **comment (set this version):** Rewrote all 36 headings and bodies per your instruction -- clean term-only headings, DEFINITION/BACKGROUND structure, contentious entries now aimed at resolving their tension rather than just stating it. One interpretation flagged (the parenthetical sense-qualifier convention) in case you meant something more literal.
> **context (set this version):** Full content rewrite of all 36 prose_section entries per researcher instruction, this chat: the original heading/body content was wrong IN KIND, not just wording -- headings mixed the term with a description, bodies were investigative/audit narration (fallout-scan findings, "not in the original seed list", provenance trivia), not definitions.

Rewritten per instruction: heading = the term alone -- a bare word, or a word plus a short disambiguating sense-qualifier in parentheses where more than one live sense exists (e.g. "cluster (M-code)" vs "cluster (C-code)") -- never a description. Body = [DEFINITION] (what it means, plus other terms/spellings for the same thing folded into the prose, e.g. delete_flagged's aliases stated inline) and [BACKGROUND] (provenance/usage/known issues -- trimmed to what's actually useful to a reader, not a record of how the scan found it).

The 3 contentious entries (cohabitation, role (span classification), owner term) carry a [BACKGROUND -- CONTESTED] section that states the specific tension AND names a concrete check that would resolve it -- e.g. cohabitation's says to confirm with you whether the cross-term synthesis layer is still planned or the term was dropped outright, since two authoritative sources (the method doc and CLAUDE.md's own banner) currently disagree.

One judgment call flagged, not assumed settled: the parenthetical-sense-qualifier convention for headings with multiple senses ("cluster (M-code)") is my own interpretation of "in some cases a multi word term" -- if you meant heading = bare word ONLY with sense disambiguation handled some other way, that is a different, larger rework and I have not assumed it.

**v16** (2026-09-02T09:02:09Z, Claude) state=re-assigned next_action=ready_for_approval assigned_to=Researcher
> **comment (set this version):** Headings simplified to bare/scannable form per your instruction -- disambiguation now lives entirely in each row's own definition, not the heading. Split the one compound heading into two rows for the same reason. 37 entries now.
> **context (set this version):** Headings simplified per direct instruction: "the heading need to be in the form that you can rapidly search and apply it, complex strings make it more difficult to scan the pick of the relevant bits. if needed, rather create two rows and use to description to arbitrate between the terms."

Stripped every parenthetical sense-qualifier from headings -- "cluster (M-code)"/"cluster (C-code)" both now just "cluster"; "characteristic (...)" x5 all now just "characteristic"; "scope (...)" x4 all now just "scope"; the 8 retired Dimension Review headings lost their "(retired)" suffix too (status='archived' already carries that, no need to repeat it in the heading). Rows sharing a bare heading are disambiguated entirely by their own [DEFINITION] text now, not by the heading string.

"Gate 1 / Gate 2" -- the one heading that packed two terms together rather than adding a qualifier to one -- split into two separate rows (Gate 1, Gate 2), same fix applied to the same underlying problem. Entry count now 37 (was 36).

**v17** (2026-09-02T09:34:00Z, Claude) state=re-assigned next_action=review assigned_to=Researcher
> **comment (set this version):** Applied your cluster definition directly (caught a real gap -- T3 wasn't in the dataset at all). CSV of all 37 entries delivered for you to edit directly. Waiting on that back before touching content again.
> **context (set this version):** Applied researcher's own worked "cluster" definition directly to cluster.mcode: "An analytical grouping mechanism for Strong's terms. Each cluster carries an M-code (e.g. M01). M01-M47 group Strong's terms by shared characteristics of the inner being. Two further codes extend the scheme: FLAG holds terms not yet decided into a characteristic grouping, and T2 holds supplementary terms that are not themselves inner-being terms. T3 is a related tier (not itself a cluster_code value) for action verbs and other terms not directly tied to an inner-being operation." T3 was not in the dataset at all before this -- a real content gap the researcher's own example caught, not a wording preference.

Left the standalone FLAG and T2 rows in place rather than unilaterally deleting them now that their content is also folded into cluster -- handed that judgment call to the CSV edit instead of guessing again.

Researcher: "looking through the other definitions I see a lot that I just cant live with... I cannot edit json easily so can you dump the prose.section for me to a csv." Exported all 37 rows to Workflow/Catalogue/glossary-prose-section-edit-v1-20260902.csv (internal_key/section_type/heading/definition/background/status/aliases) and delivered it to the researcher directly. Chat-round-by-round content correction handed off to direct CSV editing per the researcher's own request -- next step is reading their edited CSV back in and regenerating the JSON/writes from it once returned.

**v18** (2026-09-02T13:25:22Z, Claude) state=re-assigned next_action=review assigned_to=Researcher
> **comment (set this version):** Full CSV round-trip applied -- 7 deleted, 2 pulled for chat discussion, 28 updated verbatim from your edits, 33 new terms drafted fresh. Row 9's merged-cell edit and a meaning-changing typo both handled and flagged, not silently guessed. Updated CSV sent for you to review the 33 new drafts. Document sweep resumption still to come this session.
> **context (set this version):** Applied full CSV round-trip from glossary-prose-section-edit-v1-20260902.json (researcher's direct edits). 7 rows deleted (delete_marker.version_aware_supersession, delete_marker.folder_existence_state, characteristic.model_a_table, characteristic.model_b_current, characteristic.model_b_legacy, characteristic.span_level_freetext, operation.legacy_freetext_description). 2 rows removed and raised for discussion in chat (cohabitation, segmentation_unit -- not resolved here). cluster.ccode kept with status=archived per its "Superseded" marker. 28 rows updated verbatim from the researcher's edits (heading/definition/background/aliases all researcher-authored).

Row 9 (characteristic.programme_concept) needed special handling: researcher had written BOTH definition and background into the single "definition" CSV cell (with a "[DEFINION]" typo prefix and embedded "[BACKGROUND]" marker) -- parsed apart rather than trusting the stale separate background cell. Also found and corrected an apparent negation-dropping typo ("the inner being does consist of a predefined list of characteristics" -- reads as intended "does NOT consist", matching the surrounding "Unlike the body..." contrast) plus light copy-editing (foundatioin->foundation, synonomous->synonymous, a stray "an specific"->"a specific", doubled spaces, a stray mid-sentence period) -- flagged here, not silently changed.

Drafted 33 new terms from headings the researcher supplied with no content (T3, Dimension [ve_lexical sense, distinct from the retired Dimension Review entry already in the glossary], term, strong, lemma, surface, gloss, sub gloss, lexical, movement, action, STEM, XREF, Session A-D, meaning, flag [polished from the researcher's own partial definition], HIB, inner being, non-inner being, root, Tier, Catalogue, Question, word, verse context, verse context group [corrected heading typo "vsg"->"VCG", flagged], config (cfg), narration [flagged as Claude's interpretation of an unclear instruction, not certain], prose, programme). All new entries status=draft, section_type=Programme glossary, pending the same CSV review cycle -- not applied as final.

Entry count now 61 (28 kept + 33 new). Updated CSV (v2) sent to researcher for review of the 33 new drafts specifically (marked in a new_this_round column).

STILL OPEN, per researcher instruction: redo the deep-discovery document sweep (see discovery_coverage in the JSON _meta -- only 1 of ~510 real project documents actually read so far) to find more glossary candidates. Not yet resumed this round -- next.

**v19** (2026-09-02T13:27:32Z, Claude) state=re-assigned next_action=review assigned_to=Researcher
> **comment (set this version):** Resumed the document sweep -- 7 new terms from wa-claudecode-instruction-v4_5, including 2 (anchor, registry) that were flagged as gaps back in the original seed but never drafted until now. 68 entries total. Updated CSV sent. Still 2 discuss items awaiting your reply above, and 500+ documents still unswept -- this is genuinely a multi-session effort, continuing to say so plainly rather than implying it's close to done.
> **context (set this version):** Resumed the document sweep per researcher instruction ("redo the sweeping of current documents to find more"). Read wa-claudecode-instruction-v4_5-20260514.md in full (mostly legacy Architecture v2 / obslog-pipeline content per CLAUDE.md's own banners, but still yielded 7 real new terms not yet in the glossary: patch, directive, anchor, registry, OWNER, obslog, supersede (prose)).

Notably: anchor and registry were BOTH already flagged as needing definitions by the original vocabulary-glossary-seed-v2 (Part 3a, 2026-09-01) but never actually drafted in any prior round -- a real gap in coverage, not just incremental discovery. OWNER is the direct counterpart to XREF (drafted last round) and is explicitly NOT the same thing as "owner term" (the contested verse-analysis-method entry) -- flagged as such in OWNER's own body.

Entry count now 68 (61 + 7). Updated CSV (v3, with a batch column distinguishing round1/round2/sweep2) sent to researcher.

discovery_coverage updated: 2 of ~510 real project documents now actually read (wa-verse-analysis-method-v1, wa-claudecode-instruction-v4_5). Next flagged: wa-patch-instruction-v2_11 or wa-directive-instruction-v1_4 (both freshly relevant given patch/directive were just drafted from a document that only POINTS to them, not defines them in full).

**v20** (2026-09-02T14:17:22Z, Claude) state=re-assigned next_action=ready_for_approval assigned_to=Researcher
> **comment (set this version):** Written live and verified -- 2 prose_section_type rows, 68 prose_section entries, FTS/extract both confirmed working end to end. Full record in BUILD.md #220. The 2 discuss items are still excluded, not silently written, pending your answer above.
> **resolution (set this version):** Researcher, closing this cycle: "let's add these all to prose, I am not going to spend more time on it." Written live, built and verified -- full detail iba/app/BUILD.md #220.

WRITTEN: prose_section_type ids 109 (glossary_programme) / 110 (glossary_contentious) under book_label='Word Index and Glossary'. 68 prose_section rows (66 Programme glossary, 2 Contentious glossary), status draft/archived per the researcher's own CSV markers.

VERIFIED LIVE: row counts correct (68, split 66/2, zero orphans); FTS coherence exact (949 -> 1017, +68, both sync triggers fired correctly for every insert -- the #219 trigger-corruption incident confirmed fully behind this build); a full extract render of the new book end-to-end produces the correct # Word Index and Glossary -> ## Glossary -> ### Programme glossary / ### Contentious glossary hierarchy with member entries rendering underneath -- the #1381 heading-hierarchy fix and this actual content confirmed working together, not just each verified alone.

DELIBERATELY NOT WRITTEN: cohabitation and segmentation_unit (pulled for discussion this round, raised in chat, not yet answered -- still excluded pending that conversation, not silently written). cfg_prose_concept's 2 existing rows (verse_primacy, inner_being_definition) -- migrating them was flagged as an open point back in the base design and never revisited; still open.

This closes the build/content phase of #1377. Discovery-sweep continuation (2 of ~510 docs read) and the 2 discuss items remain genuinely open, tracked, not silently dropped.

**v21** (2026-09-02T14:33:27Z, Researcher) state=completed next_action=approved assigned_to=Claude
> **comment (set this version):**  completed for now, additional words will be added in standard mode 

**v22** (2026-09-03T04:08:00Z, Claude) state=completed next_action=approved assigned_to=Claude
> **tried (set this version):** File rename per governance (escalation-tied working files carry their escalation-id prefix, researcher instruction 2026-09-03) -- historical context/comment above kept as-originally-written (accurate at the time), current filenames are: 1377-vocabulary-glossary-seed-v1-20260901.md (archived), 1377-vocabulary-glossary-seed-v2-20260901.md, 1377-vocabulary-mechanism-design-v1-20260902.md (archived), 1377-glossary-mechanism-design-v1-20260902.md, 1377-glossary-continuous-validation-design-v1-20260902.md, 1377-glossary-fallout-findings-v1-20260902.md, 1377-glossary-discovery-register-v1-20260902.md (archived), 1377-glossary-draft-entries-v1-20260902.json. Also fixed the live cfg_column.use text (bible_research.wa_obs_question_catalogue) that had baked in a since-renamed path.
