# Open escalations

> Generated by `Escalation.ps1 -Action List`. 2 open escalation(s) (2 active, 0 paused).

| # | state | scope | at step | raised | question |
|---|---|---|---|---|---|
| 327 | raised | word `Dan` | passage.validate | 2026-07-28T08:30:09Z | Passage distribution in Dan: 16 passages, 7-45 verses/passage (average 21.38), 0 (0%) are single-verse. Is this distribution acceptable — no debate range (or raw span, if unscoped) looks like an outlier that should be reconsidered? Full per-book breakdown written to iba\app\reports\passage-quality.md. |
| 334 | raised | run `MANUAL-20260729_122829_764060` | manual | 2026-07-29T12:28:29Z | GUARDRAIL GAP (found 2026-07-29, IBA app): inactive/deleted flags are not enforced at runtime anywhere in the app -- they are pure metadata read only by configmaint.validate's coherence report. Evidence: cfg_work_package rows set-candidates/build-passages/candidate-quality/candidate-curation/seed-candidate-report all carry inactive=1, yet (a) Cfg.sequence() in iba/app/lib/cfg.py:115 selects from cfg_step by work_package name with no inactive filter; (b) iba/app/run.py never queries cfg_work_package at all -- it dispatches straight from the CLI-supplied package name into cfg.sequence(); (c) Set-Candidates.ps1 only checks _config_loaded()/_data_tables_exist(), not whether its own work package is active. Result: I was about to run Set-Candidates.ps1 -Book Obad (a retired step) and nothing in the app would have stopped it from executing and writing to candidate_seed/span_candidate -- only a manual DB query caught it. The same absence of enforcement likely applies to `deleted`/`inactive` flags on other cfg_* and data tables generally, not just this one work package.  Researcher's required fix (le Roux, 2026-07-29), all three parts, same unit of work: a) On iba app startup (init.py / Start-Iba.ps1), the FULL config must be read/loaded, not a partial or lazy read -- verify this is actually happening today, not just tables-exist/version checks. b) No run.py invocation may proceed without checking configs first -- run.py must look up cfg_work_package (and cfg_step) for the requested package/step and refuse (clear error, no partial execution) if inactive=1, before dispatching to any handler. c) Add a config-level setting/mechanism (e.g. a cfg_setting or a documented convention enforced in a shared helper) that guarantees deleted=1 (and inactive=1) rows are never processed/returned by ANY query across ANY table in the app -- not case-by-case per handler as today (configmaint.py is the only place currently checking inactive, and only for its own validation report).  This is a design/code gap, not a config-content question -- propose the code fix via the normal review path once scoped; do not silently patch it. |
