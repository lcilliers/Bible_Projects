# Escalation deep history

## #1842 — M49 Stage 1 batch failed: chunk cap too small
type=issue source=researcher

**v1** (2026-09-22T18:24:14Z, Claude) state=raised next_action=review assigned_to=Researcher
> **short description (set this version):** M49 Stage 1 batch failed: chunk cap too small
> **comment (set this version):** Launched Stage 1 (lexical.meaning) for M49 per your instruction (preview: 76 batches, 193 verses, ~\.22 estimated). Chunk 1/76 failed: model reply truncated mid-JSON (BadModelResponse, 'Expecting value: line 1 column 1'). Confirmed via _analytics/lexical-extracts/lexical-llm-usage.csv this was genuine max_tokens truncation, not a formatting quirk: 27044 input / 40000 output (full cap) / real cost \.6811 -- input token count is ~2x a typical M67 chunk (13-16k). The batch controller correctly stopped immediately (never continued past a real error) and no further spend occurred, but its own report/CSV wrongly shows actual_cost_usd=0 for this failed chunk despite the real \.68 spend already logged in the usage CSV -- a separate reporting bug, flagging alongside this. Should chunk sizing for lexical.meaning become expected-item-count-bounded rather than distinct-strong-count-bounded, now that M0.7/M0.8.1/D7.7.1/M0.6.5/M0.6.6 are cluster-agnostic (#1832/#1834)? Not building without your call -- M49 stays stopped until decided, since chunk 1 will fail identically on an unchanged retry.
> **context (set this version):** Root cause (assessed, not fully code-traced): lexical.meaning_max_strongs_per_batch=8 bounds DISTINCT strong count per chunk, not total question/occurrence volume. M0.7/M0.8.1/D7.7.1/M0.6.5/M0.6.6 became cluster-agnostic this session (#1832/#1834) -- a chunk's strongs now pull in every occurrence of that strong anywhere in the corpus, not just within the cluster, so a 'small' 8-distinct-strong chunk can still generate far more observation content than the old cluster-scoped design assumed. BUILD.md's own note on that build explicitly flagged this as 'a separate, unconfirmed concern' at the time -- this failure confirms it live. Distinct from #1840 (position-duplicate rows) though likely compounding it.
