# Escalation deep history

## #1768 — Front-loaded observations invisible before home cluster runs
type=task source=researcher

**v1** (2026-09-18T14:00:51Z, Claude) state=raised next_action=review assigned_to=Researcher
> **short description (set this version):** Front-loaded observations invisible before home cluster runs
> **comment (set this version):** Researcher, investigating G0189 in a raw observations query, reasonably read it as an orphan waiting on its home cluster -- correctly identified this as a widespread pattern worth deciphering properly: 'it is obviously important because there are already a long list of these in observations.' NOT a bug -- verified live: G0189's 2 existing observations already correctly carry cluster_code='M41' (its true home, via recordingpass.py's _effective_cluster_code), front-loaded by M49's own Stage 1 pass this session (1Thess.2.13 also contains G2168, M49's own member strong) per the #1723 front-loading design (word-level M0.1/M0.5 battery answered for EVERY M-code strong in a verse the first time any cluster's pass touches it, not just the pass's own home strongs). When M41 eventually runs its own Stage 1 pass, these 2 rows will NOT be touched/re-asked (_strongs_needing_battery already sees them as covered) -- M41's own pass will only add a NEW M0.6.5 relational observation, never front-loaded, always done fresh by the home cluster's own pass. Quantified the actual scope live: 92 clusters have never had their own Stage 1 pass run yet; 74 of those 92 ALREADY have front-loaded word-level coverage sitting in their data from OTHER clusters' passes -- 1390 observations across 557 distinct strongs total.
> **context (set this version):** The mechanism is correct and working as designed (#1723) -- this is a DISCOVERABILITY gap, not a data-integrity bug: nothing currently distinguishes, at a glance, 'this cluster has been analysed' from 'this cluster has a head start from front-loading but has never actually been processed' -- the researcher had to ask, and I had to manually trace cluster_strong + ib_node + cluster.status together to answer confidently. Proposed (not built -- researcher's own call on timing/priority): a small report (e.g. report.frontload_coverage, similar shape to BatchProgress.ps1 built this session) listing, per not-yet-processed cluster, how many of its member strongs already have front-loaded word-level coverage vs. zero -- doubles as a genuinely useful processing-order aid (a cluster with more pre-answered strongs is cheaper to fully process) as well as closing the confusion gap. Smaller/cheaper than #1765/#1766/#1767 -- could reasonably be built now if the researcher wants it, or deferred with the others.

**v2** (2026-09-18T17:09:13Z, Researcher) state=in-progress next_action=review assigned_to=Claude
> **comment (set this version):**  The point here is that the previous observations for a strong must be included in the data that is passed into llm for further analysis. Another point is that the observation created for a items in another cluster must be properly sign posted a) it is not showing as cross cluster implications, there is no reference to the cluster that analysed it, the tag is wrong 
> **context (set this version):**   
