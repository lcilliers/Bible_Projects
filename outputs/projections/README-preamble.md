# Reading projection — preamble (how to read these files)

Ships with every book projection. Full spec: `Workflow/methodology/WA-projection-schema-and-companion-spec-v1-20260714.md`.
Generated by `scripts/_build_projection_v2_20260714.py --book <id|all>` (read-only) from the re-read layer.

## The five files (per book, in `<book>/`)
| file | grain | use |
|---|---|---|
| `<book>_reading_view.csv` | **one row per reading** | the analyst file — flattened, 54 columns (incl. `linked_qualifiers`) |
| `<book>_nodes.csv` | one row per reading | technical: every raw span/ib_char/verse field |
| `<book>_edges.csv` | one row per span-id pair | **the movement graph** — coupling/bearer/target links (build the web from here) |
| `<book>_dimensions_long.csv` | one row per ve_lexical row | full fidelity — any pivot the reading_view loses |
| `<book>_qualifiers.csv` | one row per qualifier/standalone span | **v2: the modifying-context layer** — the evidence behind intensity/specifier/effect/device; each row links to its char(s) where a pair exists, else associate by `verse_ref` |

## The two state codes — DO NOT collapse to blank
- **`NONE`** = the reader **looked and found none** (`ve_lexical value='none'`). **This is evidence of silence.**
- **`ABSENT`** = **no row was read** for that dimension. **This is NOT evidence of silence — it is absence of reading.**
Reading `seat=NONE` and `intensity=ABSENT` as the same thing is the single most important error to avoid.

## Base meaning vs verse-derived nuance — three layers, kept distinct
Flattening does **not** collapse the base word-meaning into its contextual nuance. Read them together:
1. **Lemma (base)** — `lemma`/`strongs` + **`base_gloss`** — INVARIANT across every occurrence (e.g. H3045 `base_gloss="to know"`).
2. **Meaning-in-context** — `ib_char`, `char_key`, `read_sense_variants` — the derived nuance the word takes (H3045 → know / teach / regard / **feel-insensible** / …).
3. **Occurrence** — `span_id`, `sense`, `operation`, `reading`, `verse_ref` — this verse's reading (the finest grain; two same-nuance readings are still distinct rows).
To analyse difference: group by `lemma`, compare `base_gloss` against each reading's `sense`/`reading` to see how far context pulls the word.

## Key columns
- **`reading`** (was "discovery", ve_nr 114) — the evidence-anchored note: transliteration + capitalised verse-quote + meaning + the surfaced finding. **The primary evidence text.**
- **`discovery_flag`** (derived) — `present` if `reading` carries a surfaced finding, else `absent`. Filter on it to find the gems. *(heuristic — derived from text.)*
- **`coupling`** — the pairing **phrase**; the **graph edge** for it is in `edges.csv`.
- **`linked_qualifiers`** (v2) — pipe-list of `span:surface` for the qualifier/standalone spans explicitly paired to this char (the modifying evidence). Most qualifiers are *not* explicitly paired; find the rest by `verse_ref` in `qualifiers.csv`. **Use this to audit an `effect`/`intensity`/`specifier` of `NONE`:** a `NONE` beside a consequence-bearing qualifier is a candidate under-read, not assessed silence.
- **`role`** = characteristic / qualifier / standalone. **`locus`** = internal:ib-state / external:god / external:person. *(Corrected 2026-07-14: a coupling/locus transposition in 666 Psalms spans was fixed at source — DQ-01; locus is now a clean enum.)*
- **`char_candidate_tag`** — `emergent-read-2026` / `orphan-reread-2026` = a characteristic the old model missed (a finding in itself).
- **Salience:** `lemma_freq_book` (major vs minor characteristic in the book), `ib_instance_count` (corpus-wide).

## Derived / heuristic columns — treat as review-grade, not authored
- **`translit`** — parsed from `reading`/`sense` (~87% Proverbs; lower Psalms — the older-style rows). `translit_conf` = head / embed / none.
- **`object_kind`** — god / person / self / abstraction / other / none, inferred from `target`+`bearer`+`locus`. `other` = uncategorised (not asserted).

## Qualifier- and device-derived dimensions — assessed, self-interpretable, never silently `ABSENT`
These are **assessed from the qualifier spans / pairs around the characteristic**, recorded as `NONE`/`literal` (assessed, none) or a **self-interpretable value** (readable without the verse):
- **`intensity`**(109) — the degree qualifier ("greatly", "very", a doubled verb).
- **`specifier`**(110) — the narrowing qualifier ("of the LORD", "this").
- **`effect`**(111) — the result/consequence qualifier (also Phase-2 book level).
- **`direction`**(118) — the movement vector (toward-god / inward / outward / reciprocal / static).
- **`device`**(117) — the literary device the char is expressed through (literal / metaphor / simile / analogy / personification / paradox / hyperbole / litotes / metonymy / irony / symbolism / typology).
- **`vehicle`** — the analogy/simile/metaphor **image** (e.g. "a door on its hinges"), carried as a **first-class node + typed edge** (`edge_type=device`), not discarded as `standalone`.

**Status (2026-07-14): retrofit COMPLETE for Psalms + Proverbs.** Every read-2026 characteristic in both books now carries all five dimensions — **0 `ABSENT`** across `intensity`/`specifier`/`effect`/`device`/`direction`. Each value is either `NONE`/`literal` (assessed, none) or a self-interpretable phrase. From here `ABSENT` would signal only a genuinely unreadable case; if you see it, treat it as a data gap to flag, not silence. Derived by `_derive_retrofit_dims_v1` (prose-driven; pairs as secondary imagery control); read-back log: `outputs/projections/retrofit-rollback-log.md`. See `WA-projection-schema-and-companion-spec-v1` §C.2 / §E.

## Genuine gap (flagged, not silently blank)
- `hebrew_form` — empty: the stored `surface_en` is the **English** ESV word; the Hebrew surface is not stored (derivable via STEP).
