---
name: project_extended_lexical_model_refinement
description: "ACTIVE (2026-06-24): refining the LEXICAL to capture deep-dive understanding for reuse — lands in 01b (canonical) as v3; two-scope (per-verse + collection), object_type emergent (closes 01b §8), type-conditioned expectedness (=LRT canonical), re-run principles R1-R6; tested on tamim/cleanness (~81% mechanical); 5 additions pending; taking our time across more clusters"
metadata: 
  node_type: memory
  type: project
  originSessionId: c0477a92-7d79-40d5-89c9-c0bc761a3d86
---

GOAL (researcher, 2026-06-24): refine the lexical to be more complete/tested/representative of different types, capturing the DEEP-DIVE understanding **for reuse**. The findings ARE the lexical growing (not a separate layer). Core def = `Workflow/Instructions/01b-VE-field-reliability-and-rules.md` (canonical, settled v2); the current work folds into it as **v3**. Taking our time; the model WILL be added to further; run it through more clusters to refine.

**The extended model (design `outputs/markdown/validation/wa-lexical-extension-deepdive-into-01b-DESIGN-v1`):**
- TWO SCOPES: per-verse lexical (01b as-is) + per-term COLLECTION lexical (the deep-dive home; a value may come from an ADJACENT verse or be silent here — cite source verse; per-verse silence ≠ unit silence).
- NEW universal item **object_type** {characteristic·state·expression·qualifier·identity·bivalent-faculty} — EMERGENT from per-verse-VARYING evidence (valence-variation/how-predicate/binding), NEVER lemma-constants. **Closes 01b §8** (emergent-character debate). Selects the expected element-set.
- **Type-conditioned EXPECTEDNESS = the LRT made canonical** (P5 by object_type): a STATE must answer of-what/who-set/can-change/what-changes; a CHARACTERISTIC must have a faculty; an EXPRESSION its manner+what-it-expresses; etc. (full table design §4).
- NEW small items: mutability, transition-trigger, binding (extends compound), pole-relation.

**Re-run/regeneration principles (addendum `wa-lexical-extension-rerun-and-compound-design-v1`):** R1 split every collection value MECHANICAL-derived (regenerates) vs JUDGEMENT-captured (preserved, source=read|researcher); R2 three reset scopes (verse/term/model); R3 version each value + re-assess-with-DIFF when the model grows; R4 re-run unchanged inputs must be IDEMPOTENT = a drift-detector (non-reproducible mechanical value = judgement leak to fix); R5 parked clusters (M07/M10/M11/M01/M02) re-enter via the SAME pipeline at the live model version; R6 LRT recovery-as-RULE where possible.

**COMPOUND finding:** same co-term takes >1 role in ~0-1% (94% flat "partner") = a COARSE-FIELD ARTEFACT, not a finding (LRT-fail on compound). Refinement: richer PER-VERSE role-type (partner/qualifier/object/cause/manner/expresses/seat/pole-opposite) → then it varies by situation as the researcher hypothesised (confirmed by hand). 01b already anticipated this.

**TWO-TERM TEST (tamim=CHARACTERISTIC, tahor=STATE; `wa-m12-extended-lexical-test-tamim-cleanness-v1`):** both object_type calls fired from per-verse-varying evidence; **~81% mechanical-rule / 19% read / 0% researcher-only** (clears the maximise-mechanical aim). FIVE ADDITIONS pending before 01b v3: (1) intrinsic-faculty escape (a characteristic's required faculty may be the lemma's intrinsic faculty — resolves "needs a faculty" vs "no lemma-constant"); (2) bearer-class sub-tag on object_type (human-faculty-bearer=characteristic vs divine/object-bearer=quality; e.g. tamim "his way is perfect"=quality); (3) transition-trigger.manner slot {inner|ceremonial-rite|NONE} (homes the ta.her how-gap); (4) inner-vs-outer realm sub-register on STATE (clean-of-heart vs ritual-clean); (5) promote enriched compound roles to a real per-verse field (prerequisite for the binding-web).

**CONVERGED 2026-06-24 (autonomous session):** model v2 (5 additions) built; collection-lexical run on M13/M14/M16/M19/M20 (+ M12 test) = **SIX clusters**; **six object-types STABLE (no 7th on any)**; **~78-81% mechanical every cluster**; discipline held (no object-type off a lemma-constant). **BUILT BACK INTO 01b → v3, Part C** (the COLLECTION layer: two scopes, object_type [resolves §8], type-conditioned expectedness=LRT, collection items, re-run R1-R6, ve_lexical term-scope storage). FIRMED: transitivity (load-bearing, predicts bivalence), membership_scope, role-in-cluster (+`alien`), bivalent-object-discriminator, cognate-link, realm/manner vocab. Still [prov]: classifier, asah-override (not yet exercised). M20 (old 4 chars) refined by the model (held 3/4, split 4th). Discovery register `wa-lexical-discovery-register-v1` (Rounds 0-2 + folded log); progress log `wa-autonomous-progress-log-lexical-refinement-20260624`.

**GENERATOR BUILT + LIVE (2026-06-24, researcher OK'd DB build):** new additive table **`term_collection_lexical`** (term-scope: ve_label/value/derivation/model_version/source_ref; created without touching per-verse ve_lexical or schema_version). Generator **`scripts/_apply_generate_collection_lexical_20260624.py --cluster X [--live]`** reads DB per-verse ve_lexical (via verse_context.mti_term_id), derives object_type + items by deterministic rule, idempotent per cluster (R4), `--cluster Mxx` runs on ANY cluster. **LIVE on 6 clusters (M12/13/14/16/19/20): 1,035 rows** (object_type state 117/expression 55/bivalent 11/UNRESOLVED 5; +23 bivalence_candidate +12 characteristic_candidate). **Generalises** (dry-run sensible on unseen M17/M18/M21/M23/M28/M29/M33). Object_type rule TUNED vs the hand-built M12 map (caught + removed the faculty-as-driver trap mid-build): bivalent=real-bivalence(sinful&positive≥20%)+faculty-engaged (else valence-varying STATE w/realm split eg tum.ah); action→expression; status/quality→state; characteristic + faculty-gap-bivalence → read-promotable CANDIDATE flags (honest R1).

**KNOWN LIMITS / NEXT (build, needs steer):** (1) **faculty signal-list gap** under-detects bivalent (trust ba.tach: valence flips righteous/sinful but faculty=0 rows → flagged bivalence_candidate) — extend the faculty lemma-list = cheap high-value; (2) **A5 enriched per-verse compound** still pending (binding-web; live field ~90% flat "partner"); (3) **sweep the generator --live across all clusters** (ready; scope decision incl. parked/old); (4) read-pass to promote the candidate flags; (5) confirm [prov] classifier/asah on an identity/deed cluster. Cross-cluster lessons in `wa-cross-cluster-structure-lessons-m12-m13-m14-v1`; build log in `wa-autonomous-progress-log-lexical-refinement-20260624`. Cf. [[feedback_lexical_revelation_test_step3_gate]], [[feedback_expression_vs_characteristic_object_type]], [[project_cluster_review_backlog_and_m12_method_20260624]], [[project_measure_layer_persisted]].
