---
name: project_lexical_cycle_finalised_and_integrity_invariant
description: Lexical cycle instruction finalised (Stage 0/7A/7B); candidate-without-verse-record = DB integrity violation.
metadata: 
  node_type: memory
  type: project
  originSessionId: e78eb6e5-dae6-487a-a98b-121f066465fc
---

The authoritative lexical cycle instruction (`Workflow/Instructions/wa-characteristic-role-lexical-cycle-authoritative-v1-20260708.md`) was **finalised 2026-07-08** with three added sections: **§4A Stage 0** (no lexical outside a passage; the **candidate characteristic** span is the passage heart; whole-book passage layout precomputed deterministically), **§7A** (per-verse DB-update ledger — no triggers, the read maintains every table; keys on master span `id` never the strong; passage propagates only via `verse.passage_id`; `verse_evidence_index` deprecated for lexicals), **§7B** (two-state transition, `role_provenance='read-2026'` marks read-derived vs M64 backfill; `role IS NULL` = unread), **§7C** (explicit ordered pipeline chain seed→passage→[verse-record fix]→lexical→write-back→verify; DB writes per intervention a/b/c; completion defined+verified at verse/passage/book levels, book-level 3-count check = changeover acceptance test).

**Key governing ruling:** every `char_candidate` master span **MUST** have a corresponding active `wa_verse_records` (+ term + relations). A candidate **without** a verse-record is a **DB integrity violation** — halt and restore the verse-record and all relations (engine onboarding / per-book gate-1 path) **before** passaging/reading, not a coverage backlog and not a scope "union".

Passage rule bumped to **v2** (`wa-passage-completeness-rule-v2-20260708.md`, candidate-scoped) — supersedes the verse-record-scoped v1.

**★ CONSOLIDATED into THE single entry point (2026-07-11).** The cycle doc is now the one authoritative end-to-end spec; a "THIS IS THE SINGLE ENTRY POINT" block at its top maps the **authoritative set** (no overlap, each owns one domain): **cycle doc** = the process · **`wa-term-add-update-AUTHORITATIVE-pipeline-v1`** = add/update a term (register→extract→audit_word, every field; `new_word.py` DELETED) · **`wa-passage-completeness-rule-v2`** = passages (single-verse passages now allowed) · **`wa-db-integrity-definition-authoritative-v1`** = the validations **I1–I11**. Folded into the cycle: **single-verse passages**; **char-on-master** (`verse_span_index.characteristic` = read char in words, I11, §7A row 6); the **`ib_characteristic` normalised index** (§7D — one record per characteristic word, every span linked via `verse_span_index.ib_char_id`, I7; legacy→`ib_characteristic_legacy`); §7C(d) validations = I1–I11 (adds I2/I7/I10/I11 to book-close). `wa-corrected-charac-arc-reread-repeatable-process-v1` demoted to worked-example notes (subordinate to the cycle). **Rule going forward: if a step is unclear, the answer is in the cycle doc + its 4 sub-docs — never a new script or side doc.** Do not work around this set. Schema M66 added the two master columns + `ib_characteristic` index. See [[reference_term_add_update_authoritative]].

**Dimension authority (2026-07-08):** the authoritative dimension source is the **VE-lexical catalogue** `Workflow/Catalogue/wa-ve-lexical-catalogue-v1-20260702.md` — now completed with a **ve_nr master list §9** (101–116, incl. `specifier` 110 / `locus` 116). The cycle §3 **cites** the catalogue (doesn't restate). `wa-lexical-analysis-rules-reset-v1` is **CLOSED/archived**: its principles P0–P8 + mandatory discovery-lookout folded into cycle **§3A**, its item-framing already absorbed into the catalogue. `wa-synthesis-B-spec-reset-v1` (assembly half) is **kept** (stale but relevant). Instruction-archival register: `outputs/markdown/wa-instructions-archival-proposal-20260708.md` (Batch A of 12 self-declared-superseded docs archived; Batch B pending researcher confirmation). Changeover order: Psalms + Proverbs 1–6 first, then by book. Builds on [[project_ve_lexical_is_verse_first]] and [[project_term_driven_genre_aware_lexical_method]]; the integrity substrate that enabled this was the 2026-07-08 verse-record→master link repair (see [[project_per_book_corrective_pipeline]]).
