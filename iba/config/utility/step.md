# step — configuration component helper

> **Component:** `config/utility/step.json` · tier A · 4 item(s) · config_version 0.1.7

> GENERATED by `iba/scripts/cfg_helper.py` — a read of the json, so it cannot drift from it. **Do not edit this file**; change the component and regenerate.

**STEP client — access to the local STEP Bible server**

## What it configures

STEP is the application's ONLY external data source. This component holds what the code READS (options) and what the code MUST ENFORCE (checks). Nothing else.

## Code it serves

_Not declared._ No `implemented_by` on the component and no snippet named on any item — so nothing records which code this configures.

## Options it sets

The values a snippet reads. One row per item that carries any.

_None — this component declares no options._

## Checks it declares

What a snippet must enforce. `axis` C = deterministic code, I = needs the model.

_None — this component declares no checks._

## Items

### `checks` — 4 item(s)

| id | kind | subject | status | intent |
|---|---|---|---|---|
| `check.step.up` |  |  |  |  |
| `check.step.cap-exhausted` |  |  |  |  |
| `check.step.api-fit` |  |  |  |  |
| `check.step.response-components` |  |  |  |  |

## Unresolved

_Nothing outstanding._
