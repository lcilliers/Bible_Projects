# characteristics — configuration component helper

> **Component:** `config/process/characteristics.json` · tier B · 18 item(s) · config_version 0.1.6

> GENERATED by `iba/scripts/cfg_helper.py` — a read of the json, so it cannot drift from it. **Do not edit this file**; change the component and regenerate.

**Characteristics (seed · Screen 0 · role · registry)**

## What it configures

Decide WHAT IS BEING READ. This process seeds candidate spans, screens them for inner-being relevance, assigns each a role, and maintains the ib_characteristic registry. It is the study's gatekeeper: a span that gets past Screen 0 wrongly poisons every layer above it.

**Nodes**

- `process` — Screen 0, the seed, role assignment, char-driven read, ib_characteristic keying.
- `entities` — char_candidate · ib_characteristic · role. Definitional only.
- `output` — Review packs for the researcher.
- `validation` — The gates — including the integrity invariant that a candidate without a verse-record is a DB violation.
- `naming` — Naming parameters.
- `filing` — Filing parameters.

## Code it serves

- `authority` — researcher ruling 2026-07-15 — align the process files with the pipeline's terminology; the terminology belongs in the enums (ruling a)
- `module` — module.characteristics
- `note` — The PIPELINE says WHEN this process runs and in what units; THIS FILE says what its rules are. Two axes over one thing (plan §A.11): a module loads its governing rule-set before it runs, and this is that rule-set. Added 2026-07-15 so the two are navigable in both directions.
- `steps` — ['step.characteristics.screen0', 'step.characteristics.assign-role', 'step.characteristics.derive-ib-characteristic', 'step.characteristics.update-tables', 'step.characteristics.validate']

## Options it sets

The values a snippet reads. One row per item that carries any.

| item | subject | option keys |
|---|---|---|
| `char.screen0` |  | `statement`, `the_lens`, `failure_it_prevents`, `open` |
| `char.list-validates-never-imputes` |  | `statement`, `failure_it_prevents` |
| `char.infer-dont-extract` |  | `statement`, `why` |
| `char.candidate-requires-verse-record` |  | `statement`, `enforcement_note` |
| `char.driven-read` |  | `statement`, `failure_it_prevents` |
| `char.resist-grouping` |  | `statement`, `why` |
| `char.meaning-keyed` |  | `grain`, `phases` |
| `ent.char.ib-characteristic` |  | `grain`, `identity`, `holds`, `layer`, `disposability` |
| `ent.char.role` |  | `grain`, `surfaces_as` |
| `out.char.review-pack` |  | `format`, `must_include`, `rationale` |
| `gate.char.candidate-verse-record` |  | `scope`, `on_fail`, `enforcement_note` |
| `gate.char.screen0` |  | `scope`, `proposed` |
| `gate.char.role-domain` |  | `scope` |
| `gate.char.bearer-not-god` |  | `scope` |
| `gate.char.two-axes` |  | `scope` |
| `gate.char.seed-recall` |  | `scope` |
| `naming.char.ids` |  | `prefixes` |
| `filing.char.outputs` |  | `output_tree`, `archive_tree` |

## Checks it declares

What a snippet must enforce. `axis` C = deterministic code, I = needs the model.

| item | check | axis | severity | enforcement | status |
|---|---|---|---|---|---|
| `char.screen0` | screen0-human-ib | I | red | gate | LIVE |
| `char.list-validates-never-imputes` | direction-verse-to-list | I | red | gate | LIVE |
| `char.infer-dont-extract` | inferred-not-extracted | I | red | gate | LIVE |
| `char.candidate-requires-verse-record` | candidate-has-verse-record | C | red | db+gate | LIVE |
| `char.driven-read` | read-order-is-char | C | amber | gate | LIVE |
| `char.resist-grouping` | distinctions-preserved | I | amber | gate | LIVE |
| `char.meaning-keyed` | key-is-meaning | I | red | gate | LIVE |
| `ent.char.ib-characteristic` | entity-integrity | C | red | db+gate | LIVE |
| `ent.char.role` | value-in-domain | I | red | db:check | RECONCILE |
| `out.char.review-pack` | artefact-exists | C | amber | gate | LIVE |
| `gate.char.candidate-verse-record` | candidate-has-verse-record | C | red | db+gate | LIVE |
| `gate.char.screen0` | screen0-human-ib | I | red | gate | RECONCILE |
| `gate.char.role-domain` | value-in-domain | C | red | db:check | RECONCILE |
| `gate.char.bearer-not-god` | bearer-not-divine-name | C | red | gate | LIVE |
| `gate.char.two-axes` | axes-independent | C | red | gate | LIVE |
| `gate.char.seed-recall` | seed-recall | I | red | gate | RECONCILE |
| `naming.char.ids` | id-prefix | C | amber | gate | LIVE |
| `filing.char.outputs` | output-path | C | amber | gate | LIVE |

## Items

### `process` — 7 item(s)

| id | kind | subject | status | intent |
|---|---|---|---|---|
| `char.screen0` | principle |  | LIVE | The first question asked of every candidate span, before any other work: is this the HUMAN inner being? Everything downstream assumes this was answer… |
| `char.list-validates-never-imputes` | guardrail |  | LIVE | Keep the direction of inference right. The list is a check on what the verse produced, never a menu the verse is sorted into. |
| `char.infer-dont-extract` | principle |  | LIVE | The governing method of the entire programme. A focus point is latent and can never be observed directly — only inferred from the operations a verse… |
| `char.candidate-requires-verse-record` | principle |  | LIVE | The DB integrity invariant the researcher named explicitly: a candidate span with no verse-record is not a gap to fill later, it is a violation to re… |
| `char.driven-read` | principle |  | LIVE | Read characteristic-by-characteristic, using the characteristic as a lens, rather than sweeping spans in document order — which records reading-order… |
| `char.resist-grouping` | guardrail |  | LIVE | Protect the finding from the tidying instinct. Two occurrences of a word are not one thing seen twice — the DIFFERENCE between them is the discovery. |
| `char.meaning-keyed` | principle |  | LIVE | Key a characteristic on its MEANING-IN-CONTEXT rather than its lemma, because the same word carries different inner-being realities in different vers… |

### `entities` — 2 item(s)

| id | kind | subject | status | intent |
|---|---|---|---|---|
| `ent.char.ib-characteristic` | entity |  | LIVE | The characteristic registry — a HANDLE for gathering evidence, keyed on meaning-in-context. Explicitly disposable scaffolding, not a claim about real… |
| `ent.char.role` | entity |  | RECONCILE | The span's function — assigned BY the read at the sanity-check gate, never derived. Decides which ledger applies and therefore what a complete read i… |

### `output` — 1 item(s)

| id | kind | subject | status | intent |
|---|---|---|---|---|
| `out.char.review-pack` | output |  | LIVE | Put Screen-0 and role decisions in front of the researcher, because these are the [I] judgements the gates cannot fully carry. |

### `validation` — 6 item(s)

| id | kind | subject | status | intent |
|---|---|---|---|---|
| `gate.char.candidate-verse-record` | gate |  | LIVE | Enforce the integrity invariant at write time. |
| `gate.char.screen0` | gate |  | RECONCILE | Catch a God-act read as a human characteristic — the failure observed in live data on 07-14. |
| `gate.char.role-domain` | gate |  | RECONCILE | Refuse a role outside the canonical set. |
| `gate.char.bearer-not-god` | gate |  | LIVE | The mechanical half of Screen 0 — the part that does not need a model. |
| `gate.char.two-axes` | gate |  | LIVE | Keep candidacy and role independent — they answer different questions and conflating them hides both. |
| `gate.char.seed-recall` | gate |  | RECONCILE | Measure what the seed MISSED — the study's only defence against the failure it is named after: understating the inner being. |

### `naming` — 1 item(s)

| id | kind | subject | status | intent |
|---|---|---|---|---|
| `naming.char.ids` | pattern |  | LIVE | This process's naming parameters only. |

### `filing` — 1 item(s)

| id | kind | subject | status | intent |
|---|---|---|---|---|
| `filing.char.outputs` | pattern |  | LIVE | Where this process's artefacts are filed. |

## Unresolved

**4 RECONCILE** — value contested; dependent modules refuse to run.

- `ent.char.role` → recon.role-enum
- `gate.char.screen0` → open.char.screen0-mechanism
- `gate.char.role-domain` → recon.role-enum
- `gate.char.seed-recall` → open.char.seed-over-inclusive

**4 open question(s)**

- **`open.char.role-enum-blocked`** — The role vocabulary is contested across FOUR sources (recon.role-enum): catalogue §8, cycle §1, integrity/readiness, success-criteria — and `qualifier` was retired 2026-07-07 then reinstated 2026-07-12.
  - _recommendation:_ The filed recommendation stands: `characteristic · qualifier · standalone · uncertain`, with alias_map {process-qualifier -> qualifier, undecided -> uncertain}.
  - _resolve by:_ researcher ruling on recon.role-enum
- **`open.char.screen0-mechanism`** — `gate.char.screen0` is [I] — but Screen 0 is the judgement that decides the study's object, and the 07-14 audit found it failing in live data (Pro 8:28 'God made firm the skies' roled `characteristic`, with God as bearer).
  - _recommendation:_ B + A. The bearer!=God case that actually failed IS partly mechanical — a divine-name subject with a human-inner-being predicate is detectable, and would have caught Pro 8:28. Then adversarially verify what survives.
  - _resolve by:_ researcher decision
- **`open.char.disposability-vs-persistence`** — Characteristics are ruled DISPOSABLE SCAFFOLDING, yet ib_characteristic is a persisted, keyed registry that findings will reference.
  - _recommendation:_ A. It is the only option that makes disposability real rather than aspirational: if nothing durable references a characteristic id, the set can be re-cut at will — which the method demands, since patterns must EMERGE. Also matches 'the span-id is the join key' (plan §3.4.2).
  - _resolve by:_ researcher decision — architecturally significant; affects the findings process and Appendix B
- **`open.char.seed-over-inclusive`** — The seed is deliberately over-inclusive and non-exhaustive — Screen 0 then rejects. But there is no rule for what over-inclusive MEANS, so the seed's breadth is a judgement made per run.
  - _recommendation:_ Bias to over-inclusion and let Screen 0 carry the cost. A false positive is caught by a gate; a false negative is invisible forever. State this as a rule with a measurable seed-recall check.
  - _resolve by:_ researcher decision
