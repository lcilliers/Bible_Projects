# reconciliations — configuration component helper

> **Component:** `config/wide/reconciliations.json` · 9 decision(s) · config_version 0.1.7

> GENERATED by `iba/scripts/cfg_helper.py`. **Do not edit**; regenerate.

## What it configures

THE DECISION REGISTER. One item per concept that is defined divergently across the programme's sources, each needing ONE canonical value plus an alias/supersession map. These are researcher rulings, not research: every item already carries the variants found and a recommendation, so each is a ratify-or-overrule call.

## Code it serves

None directly. The kernel skips this file (`cfg_kernel.py` `load()`): a decision is not a rule. It gates other components — an item left unresolved keeps the rules that cite it at RECONCILE, and dependent modules refuse to run.

## Decisions to make

Each is a ratify-or-overrule call. `decision_status` uses `enum.decision_status`, not `enum.status`.

| id | concept | status | recommended |
|---|---|---|---|
| `recon.role-enum` | role (ve_nr 115) controlled vocabulary | OPEN | ['characteristic', 'qualifier', 'standalone', 'uncertain'] |
| `recon.mandatory-ledger` | genre x role mandatory dimension set | OPEN |  |
| `recon.ve-nr-master-list` | the authoritative ve_nr master list | OPEN | 101-118 (fold 117/118 into the cycle doc) |
| `recon.integrity-numbering` | the integrity invariant set numbering | OPEN | canonical I1-I13; retire the D1/D2 double-naming |
| `recon.resolution-vocab` | resolution-state vocabulary | OPEN | ['span', 'inferred', 'unknown', 'none'] |
| `recon.locus-vocab` | locus (ve_nr 116) vocabulary | OPEN |  |
| `recon.vocab-home` | where controlled-vocabulary values are authoritative | OPEN | the new configurator (this seed -> DB) is the single home; pull existing member values fr… |
| `recon.acceptance-sample` | the acceptance-sampling rule (validity acceptance) | OPEN |  |
| `recon.live-vs-legacy` | the paradigm supersession (which model is authoritative) | STANDING | RESET model is authoritative for analysis/lexical/passage/role; programme-prose is author… |

## Unresolved

**9 of 9** still open — each blocks whatever cites it.

- **`recon.role-enum`** — role (ve_nr 115) controlled vocabulary
- **`recon.mandatory-ledger`** — genre x role mandatory dimension set
- **`recon.ve-nr-master-list`** — the authoritative ve_nr master list
- **`recon.integrity-numbering`** — the integrity invariant set numbering
- **`recon.resolution-vocab`** — resolution-state vocabulary
- **`recon.locus-vocab`** — locus (ve_nr 116) vocabulary
- **`recon.vocab-home`** — where controlled-vocabulary values are authoritative
- **`recon.acceptance-sample`** — the acceptance-sampling rule (validity acceptance)
- **`recon.live-vs-legacy`** — the paradigm supersession (which model is authoritative)
