# Escalation deep history

## #838 — cfg_* is structurally coherent, but has findings needing yo…
type=issue source=configmaint related_activity=configmaint.validate from_id=-1

**v1** (2026-08-24T09:59:56Z, Claude) state=raised next_action=review assigned_to=Researcher
> **short description (set this version):** cfg_* is structurally coherent, but has findings needing yo…
> **comment (set this version):** coherence checks passed; these are advisory findings, not errors — approve to acknowledge as known/acceptable, reject to flag for action, or revise with a comment on what to check
> **context (set this version):** {"orphans": ["cfg_enum group 'record_change_log_change_type' (not looked up by name at runtime \u2014 no cfg.enum('record_change_log_change_type') or cfg_enum WHERE name='record_change_log_change_type' call site)", "cfg_enum group 'record_change_log_status' (not looked up by name at runtime \u2014 no cfg.enum('record_change_log_status') or cfg_enum WHERE name='record_change_log_status' call site)"], "needs_justification": [], "stale_filled_by": [], "stale_docs": [], "unregistered_lib_modules": [], "unregistered_project_scripts": [], "low_config_density_utilities": [], "orphan_book_order": [], "orphan_connection": [], "orphan_candidate_rule": [], "report_version_clutter": [], "escalation_ps_validateset_drift": [], "report_path": "iba\\app\\config\\CONFIG-REPORT-v138-20260824.md", "full_message": "cfg_* is structurally coherent, but has findings needing your judgement: 2 orphan config(s). Full detail (every item, by category) written to iba\\app\\config\\CONFIG-REPORT-v138-20260824.md \u2014 see the \"findings\" section."}
> **tried (set this version):** coherence checks passed; these are advisory findings, not errors — approve to acknowledge as known/acceptable, reject to flag for action, or revise with a comment on what to check
> **related activity (set this version):** configmaint.validate

**v2** (2026-08-24T10:19:54Z, Researcher) state=in-progress next_action=review assigned_to=Claude
> **comment (set this version):** review and prepare for ready for approval if needed.

**v3** (2026-08-24T10:22:11Z, Claude) state=re-assigned next_action=review assigned_to=Researcher
> **comment (set this version):** Reviewed. Both findings are the 2 new cfg_enum groups from #836's build -- record_change_log_change_type and record_change_log_status. Real reason they're flagged: change_type/status are enforced by the table's own CHECK constraint directly in apply_session_patch.py's _write_change_log(), not read via a runtime cfg.enum(...) call -- so the validator correctly can't tell automatically whether that's a mistake or deliberate. It's deliberate, and matches a shape already accepted once before: #833's wa-session-research-flags-retained-as-is rule documents the identical pattern (CHECK enforces in the DB, cfg_enum documents/governs it as the config-of-record, no runtime lookup) -- also stated directly in #836's own proposal sec 13 before building. Recommendation: Approve (acknowledge as known/acceptable), not Reject or Revise -- there's no code gap to fix, this is cfg_enum's documentation role working as designed for a CHECK-backed vocabulary. This is a decision_required item (resolution_kind), so I'm not running AnswerRun myself -- confirm Approve and I'll run it, or tell me if you want it handled differently.
> **context (set this version):** iba/app/config/CONFIG-REPORT-v138-20260824.md (findings section). Precedent: cfg_behaviour_rule wa-session-research-flags-retained-as-is (escalation #833). Source of the finding: escalation #836's build (GOVERNANCE.md sec 52).

**v4** (2026-08-24T10:30:14Z, Researcher) state=in-progress next_action=review assigned_to=Claude
> **comment (set this version):** noted  both these will go away when the scripts for updating change log is completed in a nother sessionpreare this escalation for approval, although it is likely to re-appear again until the script is completed

**v5** (2026-08-24T11:27:48Z, Claude) state=re-assigned next_action=review assigned_to=Researcher
> **comment (set this version):** Retracting the #833 comparison -- checked it properly instead of reusing it, and it doesn't actually match. #833's rule was about a table with NO writer at all (nothing in governed code touches it), so there was no enforcement question to speak of. #836's tables are actively written, every day, by real code -- the vocabulary genuinely is enforced, just by the CHECK constraint, not by cfg_enum. That's a different shape, and citing #833 let me treat the finding as already-settled when it wasn't. What the orphan-check is actually protecting against, properly stated: cfg_enum's whole job (governance.rules_must_be_config_driven) is to BE the thing that governs the vocabulary -- not a note describing a value that lives somewhere else. Right now nothing ties cfg_enum's rows to the CHECK constraint's own IN(...) list programmatically -- if either one changed without the other, nothing would catch the mismatch. They only agree today because I typed both by hand in the same build. Fuller facts, checked live, not glossed over this time: change_type is a hardcoded Python string literal at each of the 8 call sites in apply_session_patch.py (never patch-supplied, never derived from anything a caller controls) -- the CHECK constraint is the only real backstop against a future code change introducing a bad value; cfg_enum plays no active role today. status is hardcoded 'change_applied' in every INSERT across both the applicator and the migration script -- change_proposed/declined exist in cfg_enum and the CHECK purely as forward documentation for #835 (not yet built); zero code paths write them. Also worth naming: apply_session_patch.py has ZERO existing dependency on iba.app.lib.cfg anywhere in the file -- it's a pre-IBA script. #829 already proposes a cfg_write_grant treating it as IBA-governed, but nothing in it actually calls Cfg at runtime -- a broader, pre-existing gap, not unique to this finding, but relevant to how big a fix option (a) below actually is. Real options, not defaulting to one: (a) wire an actual cfg.enum() check into _write_change_log() before the INSERT -- closes the drift risk for real, at the cost of being the first place this particular script would depend on Cfg; (b) leave it CHECK-only and accept the narrower risk honestly (a future direct schema edit to the CHECK constraint, bypassing the Python code entirely, could drift unnoticed -- the code's own literals can't drift on their own since they're hardcoded and CHECK-verified); (c) something else you have in mind. Not recommending one over the other this time -- asking.
> **context (set this version):** iba/app/config/CONFIG-REPORT-v138-20260824.md. Governance: governance.rules_must_be_config_driven. Live facts checked: scripts/apply_session_patch.py (8 call sites), iba/app/migration/prose_change_log_build_v1_20260824.py.
