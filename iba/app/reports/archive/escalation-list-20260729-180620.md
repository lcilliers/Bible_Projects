# Open escalations

> Generated by `Escalation.ps1 -Action List`. 15 open escalation(s) (15 active, 0 paused).

| # | state | scope | at step | raised | question |
|---|---|---|---|---|---|
| 327 | raised | word `Dan` | passage.validate | 2026-07-28T08:30:09Z | Passage distribution in Dan: 16 passages, 7-45 verses/passage (average 21.38), 0 (0%) are single-verse. Is this distribution acceptable — no debate range (or raw span, if unscoped) looks like an outlier that should be reconsidered? Full per-book breakdown written to iba\app\reports\passage-quality.md. |
| 334 | raised | run `MANUAL-20260729_122829_764060` | manual | 2026-07-29T12:28:29Z | GUARDRAIL GAP (found 2026-07-29, IBA app): inactive/deleted flags are not enforced at runtime anywhere in the app -- they are pure metadata read only by configmaint.validate's coherence report. Evidence: cfg_work_package rows set-candidates/build-passages/candidate-quality/candidate-curation/seed-candidate-report all carry inactive=1, yet (a) Cfg.sequence() in iba/app/lib/cfg.py:115 selects from cfg_step by work_package name with no inactive filter; (b) iba/app/run.py never queries cfg_work_package at all -- it dispatches straight from the CLI-supplied package name into cfg.sequence(); (c) Set-Candidates.ps1 only checks _config_loaded()/_data_tables_exist(), not whether its own work package is active. Result: I was about to run Set-Candidates.ps1 -Book Obad (a retired step) and nothing in the app would have stopped it from executing and writing to candidate_seed/span_candidate -- only a manual DB query caught it. The same absence of enforcement likely applies to `deleted`/`inactive` flags on other cfg_* and data tables generally, not just this one work package.  Researcher's required fix (le Roux, 2026-07-29), all three parts, same unit of work: a) On iba app startup (init.py / Start-Iba.ps1), the FULL config must be read/loaded, not a partial or lazy read -- verify this is actually happening today, not just tables-exist/version checks. b) No run.py invocation may proceed without checking configs first -- run.py must look up cfg_work_package (and cfg_step) for the requested package/step and refuse (clear error, no partial execution) if inactive=1, before dispatching to any handler. c) Add a config-level setting/mechanism (e.g. a cfg_setting or a documented convention enforced in a shared helper) that guarantees deleted=1 (and inactive=1) rows are never processed/returned by ANY query across ANY table in the app -- not case-by-case per handler as today (configmaint.py is the only place currently checking inactive, and only for its own validation report).  This is a design/code gap, not a config-content question -- propose the code fix via the normal review path once scoped; do not silently patch it. |
| 335 | raised | run `RUN-20260729_171045_414-CONFIGMAINT` | configmaint.propose | 2026-07-29T16:10:45Z | Add raw.strong_base_pattern (module raw) as the single source of truth for the Strong's-code base/sub-letter split regex, closing a 3-way code duplication (GOVERNANCE.md sec5, found again 2026-07-29 in versespanmeaningreport.py). Code already falls back to the byte-identical literal if this row is absent/rejected, so approving just makes it authoritative rather than changing behaviour. |
| 336 | raised | run `RUN-20260729_172510_558-CONFIGMAINT` | configmaint.propose | 2026-07-29T16:25:11Z | Deactivate enum.passage_rule (2 values: char-continuity, maximal). passage.rule (the column this enum constrains) is NULL on every live row and has been since passage.build was retired 2026-07-26 (GOVERNANCE.md sec23) - nothing populates it. The candidate system's equivalent enums (candidate_decision/candidate_source/etc) were correctly deactivated in the same retirement pattern 2026-07-23; this one was missed. Matches that precedent exactly. |
| 337 | raised | run `RUN-20260729_172522_941-CONFIGMAINT` | configmaint.propose | 2026-07-29T16:25:23Z | Deactivate enum.passage_source (2 values: passage-build, single-verse-emergent) - same reasoning as the passage_rule proposal just raised: passage.source is NULL on every live row since passage.build's retirement, nothing populates it, and this is the second of two enum groups the 2026-07-26 passage retraction missed (the candidate systems equivalent enums were deactivated correctly). |
| 338 | raised | run `RUN-20260729_172725_648-CONFIGMAINT` | configmaint.propose | 2026-07-29T16:27:26Z | Add a cfg_on_fail row for raw.discover's new follow-related-not-built condition (2026-07-29 code fix: discovery.follow_related used to be an if-true:pass dead branch that changed nothing regardless of value; it now fails loudly instead of silently no-op'ing if ever flipped true). Without this row run.py already defaults unmatched conditions to report-stop, so behaviour is the same either way - this just makes the message/path explicit and reviewable rather than relying on the fallback. |
| 339 | raised | run `RUN-20260729_173449_887-CONFIGMAINT` | configmaint.propose | 2026-07-29T16:34:50Z | Add cfg_enum group narrative_required_channel (3 values total, this is value 1 of 3) - the three inner-being channel labels report.book_narrative_validate checks for. Currently hardcoded as REQUIRED_LABELS in handlers/narrative.py with no config counterpart at all; code already falls back to the byte-identical hardcoded tuple if these rows are absent, so approving just makes the fact config-editable, not a behaviour change. |
| 340 | raised | run `RUN-20260729_173514_413-CONFIGMAINT` | configmaint.propose | 2026-07-29T16:35:14Z | narrative_required_channel value 2 of 3 - see the sibling proposal just raised for full rationale. |
| 341 | raised | run `RUN-20260729_173524_468-CONFIGMAINT` | configmaint.propose | 2026-07-29T16:35:24Z | narrative_required_channel value 3 of 3 - see the sibling proposals just raised for full rationale. |
| 342 | raised | run `RUN-20260729_173548_453-CONFIGMAINT` | configmaint.propose | 2026-07-29T16:35:48Z | Add cfg_enum group word_status_built (2 values total, this is value 1 of 2) - which word_registry.status values count as already built (registry.py's exists()/create() use this to detect a real duplicate vs. an in-progress word). Currently a hardcoded BUILT tuple in handlers/registry.py despite cfg_status_flow existing for exactly this class of fact. Code falls back to the byte-identical hardcoded tuple if these rows are absent. |
| 343 | raised | run `RUN-20260729_173557_896-CONFIGMAINT` | configmaint.propose | 2026-07-29T16:35:58Z | word_status_built value 2 of 2 - see the sibling proposal just raised for full rationale. |
| 344 | raised | run `RUN-20260729_173635_282-CONFIGMAINT` | configmaint.propose | 2026-07-29T16:36:35Z | Add registry.duplicate_shared_threshold (module registry, default 1.0 = current 100%-only behaviour, byte-identical). Makes the duplicate-word warning threshold a config decision instead of a hardcoded constant - the researcher can lower it later (e.g. 0.8) via a normal propose if partial overlap should also warn, without a code change. |
| 345 | raised | run `RUN-20260729_173734_094-CONFIGMAINT` | configmaint.propose | 2026-07-29T16:37:34Z | Clear passage.rule's stale filled_by='passage.build' (an inactive step) and correct its use note to say the column is dormant, not describe live behaviour. This is the specific defect escalation #327/the passage-rules-audit traced back to: cfg_column never updated when passage.build was retired. |
| 346 | raised | run `RUN-20260729_173748_447-CONFIGMAINT` | configmaint.propose | 2026-07-29T16:37:49Z | Clear passage.source's stale filled_by='passage.build' - same defect/fix as the passage.rule proposal just raised. |
| 347 | raised | run `RUN-20260729_173807_210-CONFIGMAINT` | configmaint.propose | 2026-07-29T16:38:07Z | Clear passage.needs_review's stale filled_by='passage.build' - same defect/fix as the two sibling proposals just raised (passage.rule/source). |
