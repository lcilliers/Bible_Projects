# Open escalations

> Generated by `Escalation.ps1 -Action List`. 3 open escalation(s) (3 active, 0 paused).

| # | state | scope | at step | raised | question |
|---|---|---|---|---|---|
| 380 | raised | run `RUN-20260730_044143_606-CONFIGMAINT` | configmaint.validate | 2026-07-30T03:41:44Z | cfg_* is structurally coherent, but has findings needing your judgement: 14 utility module(s) with zero cfg.setting()/cfg.enum() usage: cfg_utility 'cfg' (iba\app\lib\cfg.py) has zero cfg.setting()/cfg.enum() call sites — confirm this is a legitimate zero (a helper whose caller resolves config for it) or a real completeness gap; cfg_utility 'cfgcheck' (iba\app\lib\cfgcheck.py) has zero cfg.setting()/cfg.enum() call sites — confirm this is a legitimate zero (a helper whose caller resolves config for it) or a real completeness gap; cfg_utility 'cfgload' (iba\app\lib\cfgload.py) has zero cfg.setting()/cfg.enum() call sites — confirm this is a legitimate zero (a helper whose caller resolves config for it) or a real completeness gap; cfg_utility 'cfgreport' (iba\app\lib\cfgreport.py) has zero cfg.setting()/cfg.enum() call sites — confirm this is a legitimate zero (a helper whose caller resolves config for it) or a real completeness gap; cfg_utility 'db' (iba\app\lib\db.py) has zero cfg.setting()/cfg.enum() call sites — confirm this is a legitimate zero (a helper whose caller resolves config for it) or a real completeness gap; cfg_utility 'dbsnapshot' (iba\app\lib\dbsnapshot.py) has zero cfg.setting()/cfg.enum() call sites — confirm this is a legitimate zero (a helper whose caller resolves config for it) or a real completeness gap; cfg_utility 'lexiconparse' (iba\app\lib\lexiconparse.py) has zero cfg.setting()/cfg.enum() call sites — confirm this is a legitimate zero (a helper whose caller resolves config for it) or a real completeness gap; cfg_utility 'passagetrack' (iba\app\lib\passagetrack.py) has zero cfg.setting()/cfg.enum() call sites — confirm this is a legitimate zero (a helper whose caller resolves config for it) or a real completeness gap; cfg_utility 'registryreport' (iba\app\lib\registryreport.py) has zero cfg.setting()/cfg.enum() call sites — confirm this is a legitimate zero (a helper whose caller resolves config for it) or a real completeness gap; cfg_utility 'retention' (iba\app\lib\retention.py) has zero cfg.setting()/cfg.enum() call sites — confirm this is a legitimate zero (a helper whose caller resolves config for it) or a real completeness gap; cfg_utility 'schemareport' (iba\app\lib\schemareport.py) has zero cfg.setting()/cfg.enum() call sites — confirm this is a legitimate zero (a helper whose caller resolves config for it) or a real completeness gap; cfg_utility 'seedreport' (iba\app\lib\seedreport.py) has zero cfg.setting()/cfg.enum() call sites — confirm this is a legitimate zero (a helper whose caller resolves config for it) or a real completeness gap; cfg_utility 'spanreport' (iba\app\lib\spanreport.py) has zero cfg.setting()/cfg.enum() call sites — confirm this is a legitimate zero (a helper whose caller resolves config for it) or a real completeness gap; cfg_utility 'strongreport' (iba\app\lib\strongreport.py) has zero cfg.setting()/cfg.enum() call sites — confirm this is a legitimate zero (a helper whose caller resolves config for it) or a real completeness gap |
| 392 | raised | run `RUN-20260730_061454_014-CONFIGMAINT` | configmaint.validate | 2026-07-30T05:14:55Z | cfg_* is structurally coherent, but has findings needing your judgement: 1 stale-doc finding(s), 13 utility module(s) with zero cfg.setting()/cfg.enum() usage. Full detail (every item, by category) written to iba\app\config\CONFIG-REPORT.md — see the "findings" section. |
| 397 | raised | run `MANUAL-20260730_053220_933245` | manual | 2026-07-30T05:32:20Z | The User Guide does not include instructions for all the reports  - fix it |
