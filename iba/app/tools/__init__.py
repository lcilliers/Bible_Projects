"""iba.app.tools — maintenance tools (not part of a work-package run).

These are operator utilities for keeping the app's own data tidy — e.g. removing test
data — distinct from the study operations (new-word, and later base/lexical/…). A full
delete OPERATION with its own gates is future work; these are the lightweight stopgaps.
"""
