"""iba.app.migration — ONE-OFF migration utilities, not standard IBA app methods.

Kept separate from the app's standard methods (ps/, handlers/, tools/) on purpose: these
run once to move the legacy study into the app, then retire. They are not part of the
ongoing operation set.
"""
