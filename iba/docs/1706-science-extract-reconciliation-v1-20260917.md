# Science-extract reconciliation — clusters vs. existing extracts vs. gaps

**Continues #1706/#1712.** Per researcher instruction, 2026-09-17: *"do a cross check between the
newly formulated clusters, the existing extracts and missing extracts."* Real, checked finding:
this isn't a simple "45 done, ~40 missing" count. The cluster scheme has been substantially
reworked since the 45 extracts were produced (2026-05-13) — many M-codes now name a **completely
different concept** than they did then (e.g. old `M10` = Guilt, live `M10` = Violence & Cruelty).
Matched by content/theme, not by filename number.

## What was fixed directly (mechanical, no judgement call)

5 files had a clean, single, unambiguous rename — same topic, different current M-code — moved and
relabelled (content unchanged, cluster-code references corrected throughout):

| Old file (2026-05-13) | Old code | New code | Current cluster name |
|---|---|---|---|
| guilt | `M10` | `M56` | Sin & Guilt |
| purity | `M12` | `M61` | Purity & Holiness |
| hope | `M18` | `M68` | Hope & Waiting |
| evil/wickedness | `M27` | `M58` | Wickedness |
| salvation | `M38` | `M79` | Salvation & Ransom |

## What's confirmed clean, no action needed (15)

Same M-code, same core theme: `M01` Fear, `M02` Anger, `M03` Grief, `M04` Joy, `M07` Shame, `M08`
Pride, `M09` Humility, `M14` Deceit, `M19` Trust, `M20` Doubt, `M28` Envy, `M34` Perseverance,
`M35` Testing, `M43` Prophecy, `M45` Transformation.

## Borderline — same M-code, but the live cluster's scope has narrowed (6)

The old file and the live cluster share a number and a core, but part of the old topic has split
off into its own separate live cluster since. These need a **revision pass** (tighten to the
current cluster's actual scope), not a full rewrite: `M06` (old: Hate/Contempt/Hostility → live:
Malice & Enmity), `M22` (old included Thanksgiving, now its own `M49`), `M23` (old included
Dominion, now its own `M72`), `M31` (old: Faith/Belief/Unbelief → live: Faith, narrower), `M39`
(old: Blessing/Favour/Grace → live: Gift & Favor; Blessing now `M80`, Grace now `M50`), `M46` (old:
Abundance/Prosperity/Wealth → live: Wealth & Riches).

## Genuinely orphaned — old topic, no confident live match (2)

`M17` (old: Counsel, Planning, Purpose) and old-`M37` (Calling, Election, Vocation — live `M37` is
now the unrelated "Firstborn & Foreknowledge"). Neither concept obviously survives under a single
current cluster name — flagging rather than guessing where the content should land. Kept as
reference material for the agents below where a partial overlap exists (e.g. `M17`'s "Purpose"
content for `M64` Will & Resolve), not silently discarded.

## Everything else — needs a fresh extract (remaining ~59 of 80 live clusters)

Every live cluster not listed above, including every `M47`–`M84` cluster (the old set never went
past `M46`) and `M10c`/`M32`. Old-file content is handed to the generating agent as background
*inspiration only* where a thematic cousin exists (e.g. old `M21` Prayer/Worship content informs
live `M36` Worship & Service and `M42` Prayer & Petition; old `M15`/`M16` Wisdom/Folly content
informs live `M15` Knowing & Understanding and `M16` Wisdom & Folly; old `M41`/`M42` Remembrance/
Speech content informs live `M81`/`M82` Memory/Reminder and `M65`/`M84` Speech/Outcry) — never
copied wholesale, since the live cluster's actual scope is frequently narrower or angled
differently than its predecessor.

## Generation dispatched

Per researcher go-ahead ("you can proceed to generate them in the background"), background agents
are now producing the 6 revisions + ~59 fresh extracts, following the same template as the 45
originals (Purpose statement; per-topic Science sections with Primary field / Glosses / Landmark
findings / Emergence / Reduction-absence / Location / External-influences; Outside-science
inventory), grounded in the programme prose's own science-positioning sections (Ch.1 "Science and
the Bible", Ch.2 "Science in action") and referencing the *new* `D9`/`D11`/`D12` catalogue question
codes (not the retired `T7.3`). Citations reflect the generating model's own knowledge of the
literature — same epistemic status as the original 45, not independently verified against a live
source in this environment; worth a research spot-check same as the originals would get.
