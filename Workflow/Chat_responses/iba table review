iba table review
2026-08-16

previous documents still in focus and relevant
# Session Log - 2026-08-15 - Project sanity check and alignment kickoff
C:\Bible_study_projects\outputs\markdown\project-review-response-2-20260815.md

This review is started after it was discovered that Claude CC handling of IBA Application and the entries in the table is incomplete and incoherent.  My intent, and what is happened is not aligned.

IBA App is still work in progress, and the build is not complete. My understanding is that the core App architecture and framework for operation is supposed to be completed and base layer/raw data App methods and resulting data should be complete. The development phase in progress is the debate/analytic exploration phase.  This effort is preceded by bringing the whole project operations under the domain of the IBA App process control, and a deep analysis of the existing data.

The main intent of the IBA app was to ensure that project methods and operations are controlled, and that each operation will be done exactly the same, irrespective of when it is run. This fundamental premise was formulated after discovering that Claude constantly re-invent the wheel, drift, and cannot be relied upon to be consistent.

Central to this, is the configurations. All rules, all code variables, all routines and methods, and all utilities and all re-used scripts must be registered and controlled in the configurations.  What CC did not do, is to build the safeguards to enforce compliance.

cfg_settings module governance already include:
"governance.governance_md_on_rule_change: researcher ruling 2026-07-22: no rule should exist only in GOVERNANCE.md; the config is the source of truth, GOVERNANCE.md is the overview of it
governance.past_precedent_investigation_signals_missing_config"  the fact that Claude missed this, is due to poor formulation of the rules, incomplete rules, and rule inconsistencies. It is now  recognised that the module governance in cfg_settings is incoherent and open for interpretation. In many cases the config entry was not made to signal a system wide rule, the config rule reads like it is related to a specific incident.  

This instruction therefore introduces corrective actions I took.  The export.cfg_settings shortcomings.csv include new columns, and new rows to allow CC to augment the cfg_settings table.  This will impact other config tables. I am not expecting to give you detail row for row config changes for the entire IBA app. The app design and codification is your role with my guidance. The level of my detail invention is an indication of the lack of delivering what I thought you are capable of.

All corrective action must go through escalations.  However, escalations in its current format is not fit for purpose. Therefore this instruction is seen as the process of resetting escalations. Once this is reset. Then  escalations can be used to elevate all the other actions, issues or reports.

Escalation actions.

note new config rows in cfg.settings
governance.escalation.scope,"all open items, discovery of anomalies, clarifications and other forms of escalation must be recorded in escalation using escalation rules",governance,0
governance.utility.config,"each utility must have its own config table in the cfg_* series to control all aspects of the utility",governance,0

note existing row in cfg.utilities
escalation,iba/app/lib/escalation.py,escalation.py — util.escalation. The only sanctioned researcher interaction.,0,0,

create new cfg.escalation table, then add the following config rules
the source of an escalation is one of a) code generated e.g. validation check b) raised by Claude c) raised by Researcher.  When code generated the value of the source column (previously 'word') must include the module as the source.
a duplicate of the same issue in the same state must not be raised again
running a module in the cfg.module or cfg.utility tables is blocked if it has unresolved escalations (state is one of raised, re-assigned)
escalation resolution takes precedence over any other activity. Open next_action_assigned_to = Claude must be addressed first before other activities.
Chat discussions must be actioned through escalations. The next action to work on arrives from an escalation. The actions and step identified must be recorded in escalations


Modify escalation columns
change column word to source and make it required. change the current values in the column (only used in one case) to new-word: + columnvalue.  
change column question to short_description. the current values can stay as is. change the use in the cfg.columns also.
change column preset to context. current values and cfg.columns.use is fine.
add new column Next_action_assigned_to : the value of this column is one of Claude or Researcher
add column for answer : the value of this column is a short description or reference for the action taken to resolve.
add column for related_activity : the value in this column is the process, module, or activity that the row relates to
change column type : the current use values is not clear: the values should be one of a) task b) run_error c) issue d) notice e) config 
change column state : the current options raised | answered | resumed is incomplete: should be raised | re-assign | on-hold | closed | withdraw | completed
add column answered_by: this is a required column and must be either Claude or Researcher

The following is a list of escalations to raise, when the restructure of the escalation procedure is completed.

On completion of the updates of the cfg_setting table and the other config tables, ensure that the implications of the configs are updated in all underlying modules, utilities and associated scripts, work through through Governance.md and ensure that all references in Governance are properly updated to adhere to the config and ensure that the User Guide is updated.

Every work package must be defined in cfg_work_package: ensure that there are NO work package that is not defined in this config. Ensure that the python run command check that the work package are registered. Every run must be registered in the run table.

Every code driven operation must be developed in such a way that all variables, rules, lookups are defined in the config. review the scripts across the project to ensure that this rule is complied with.

The following escallation actions can be processed
575 - are there any verse-lexicals for new testament books. I suspect not. this item and 576 must be retracted and running verse-lexicals for new testament books must be raised.
577 - noted. retracted. this is not a word to load.
593 - investigate why report.word_register is producing these errors. it is unexpected. see also 577
578,579,591,597,598,626,642,643 all should be resolved through the config work. these errors may raise other inconsistencies that must be dealt with.
632 is missing a config that was previously defined.


Not yet done from previous session.

Escalations #591/#597/#642 are fixed in substance but not formally closed (answered) in the escalation table yet.
BUILD.md doesn't have an entry for this fix yet (owed, per governance.build_md_on_code_change).
The "3 lib modules missing a cfg_utility row" finding from that last run hasn't been looked at at all.
Everything else from this session — CHARTER.md, the iba/config fork correction, the escalation backlog as a whole, /start-project's required-reading update — is written but not all cross-checked together.