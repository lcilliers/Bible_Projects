# Escalation design plan

## Resources

List all the source documents used to determine the requirements. Include not only the original plan and researcher comments, but also list the surrogate documents and resources that you are going to study to determine the requirements.

##Purpose

Describe the purpose of utility and how the utility will meet this purpose on a high level. e.g. The utility does the following .... by doing .....

## Type of entries
Describe the types of items (e.g. task, issue, crash) that is recorded in the utility.
e.g. a task is what? Why does it exist?

Describe how each type will work e.g. 
A task is raised by either.... the field entries for raising a task is ....  
Working on the task is activated by ....
Updates to the task is done by doing .......
when something need to be clarified , it is done like this ....
when this happens a separate item is created and linked to the main task like this ....
.......
describe all the elements that each type would go through, and the conditions for it, my list is not exhaustive, it is illustrative.

## transaction types

describe each separate transaction method

## tables and columns

list the tables and each column and the what is valid data in the column.

## Governance

list every config in governance that applies to this utility, and how it is complied with
check if there are missing governance configs (e.g. when is it applicable, when must it be used, what is not allowed)

## control items

type, next-action, state, answer, assignee, next-action-manual etc all have a material control function and the combinations materially matters.  Describe all these.

## automation

describe how automation supports each column, include all the variants 

## configs

the utility has multiple layers of configs. make sure every rule, every option, every selection is captured in configs. make sure that every item  has a meaningful value, and that the value describe the use, application, how, what, where, how properly

cross check everything.  if there are 5 items in Enum, you would expect the same list to be applied when to column is described, and the rules on selecting it is described, and the automation is handled, and the validation is done.

ensure there are no redundant configs

ensure the config values are not confusing or no longer relevant.

## validation

describe every validation
describe what happens when it goes wrong
describe the configs that is used in validation

## scripts

list every script that use or process escalation.
describe how the script works, what the purpose is, and which configs it use.
check back that every config is actually used and is acted upon in a script


## report

what configs are used in reports
describe the purpose of the report, and how the content of the report will support the purpose

